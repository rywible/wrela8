# Permanent Pixels fixture: check-pixels-material-edge

Protects: a nondepth material discontinuity at identical geometry q.
Deterministic geometry: the material boundary is the integer plane `x=0` with IDs `1` and `2`.
First active: P3.
P0 status: production Pixels stage is not implemented; implemented in task P3.9.

module lib.wrap

# plans/M9.md item B3 fixture: deriving(From) on a one-field struct.
# Exporter spelling is Wrap; importers may alias it.

pub struct Src:
    pub n: u32

pub struct Wrap deriving(From):
    pub inner: Src

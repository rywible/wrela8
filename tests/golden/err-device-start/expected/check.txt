error[unimplemented]: `DriverClaimedDevice[VirtioBlock].start()` — 03-hardware.md §9's final `-> Running` transition, whose input state (`FeaturesAcceptedDevice[VirtioBlock]`, then `QueuesConfiguredDevice[VirtioBlock]`) nothing produces yet: `negotiate` and `VirtQueue.configure` are the two transitions before it, and publication requires the `RunningDevice[VirtioBlock]` it would return. That whole span not checked yet at 20:26

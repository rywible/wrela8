error[comptime]: `Inner` is imported from module `lib.pk`, which declares `Src` — a name module `app.main` resolves differently; evaluating that module's bodies here could silently pick the wrong `Src`, so it is not supported yet (plans/M9.md item A1b)
  while evaluating `D`
  while evaluating `drive`

error[actor]: `g.start` cannot appear inside a loop: a group's child count is the number of *static* `g.start` sites (it is what `join_all`'s own array length and the group's admission accounting are built from), so one site running twice starts a child nothing is waiting for — lift the `g.start` out of the loop (M6 scope, plans/M6.md item H2) at 26:21

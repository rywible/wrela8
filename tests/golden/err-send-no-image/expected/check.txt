error[actor]: a bare `send` to `Logger.record` is not proven infallible — consume its `Result[unit, CallError[never]]` (bind it or `match` it) at 23:5
  the build closure declares no `@image` fn, so no mailbox capacity is known — every mailbox bound this proof needs is declared there (`img.actor(T, mailbox=N)`)
  02-language.md §9.4: `send` stands as a bare statement only where mailbox analysis proves admission cannot fail

module driver

@driver
pub struct StorageDriver:
    id: u32
    sector_count: u32

    init(mut self, sector_count: u32):
        self.id = 1
        self.sector_count = sector_count

    pub fn capacity(read self) -> u32:
        return self.sector_count

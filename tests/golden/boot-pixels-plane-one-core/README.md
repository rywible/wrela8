# Permanent Pixels fixture: boot-pixels-plane-one-core

Protects: deterministic single-worker execution against the four-worker plane frame.
Deterministic geometry: the same sealed 64×32 plane as `boot-pixels-plane`.
First active: P7.
P0 status: activated; implemented in task P7.13.

Both worker configurations pin the same exact debug visibility digest and
certificate telemetry totals.

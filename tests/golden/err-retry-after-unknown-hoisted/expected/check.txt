error[type]: `publish` re-issues an operation declared `idempotent=false` inside a `CompletionOutcome.Unknown` arm — 03-hardware.md §9: "Source must not auto-retry a non-idempotent operation on `Unknown`". The first attempt may already have taken effect, so retrying it can apply the operation twice. Either establish quiescence first (quarantine the device and pool, or go target-fatal — 03 §9), or, if re-running this exact operation is provably harmless, declare it `idempotent=true` at its `prepare_block` at 89:44

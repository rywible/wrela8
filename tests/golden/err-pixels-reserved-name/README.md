# Permanent Pixels fixture: err-pixels-reserved-name

Protects: the compiler-reserved surface fence (plan deviation 21). An ordinary
project module may neither define nor reference an identifier beginning
`__wrela_` or `RendererWorker`; the check runs on source tokens before
specialization, so a *reference* is rejected exactly like a definition. That is
the direction that could otherwise hijack a generated lowering by spelling.
Deterministic geometry: one user module names `__wrela_pixels_f32_to_bits`, a
generated intrinsic, inside an otherwise-valid sealed scene; rejected at 40:12.
First active: P7.
P0 status: activated; implemented in task P7.15.

module examples.err_x_for_take_mismatch

resource struct Packet:
    size: u64

    init(mut self, size: u64):
        self.size = size

pub fn use_it(take arr: [Packet; 2]) -> u64:
    total: u64 = 0
    for p in take arr:
        total += p.size
    return total

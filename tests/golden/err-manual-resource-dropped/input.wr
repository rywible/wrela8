module examples.err_manual_resource_dropped


resource(manual) struct Token:
    bits: u64

    fn mint(bits: u64) -> Token:
        return Token(bits=bits)

fn drop_it() -> u64:
    t = Token.mint(bits=1)
    return 0

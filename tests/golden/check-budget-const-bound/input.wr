module examples.check_budget_const_bound


const BOUND: u64 = 64


pub fn sum_to(n: u64) -> u64:
    total: u64 = 0
    i: u64 = 0
    @budget(bound=BOUND)
    while i < n:
        total = total + i
        i = i + 1
    return total

error[build]: `own[BlockControl] BlkReqStatus` names pool `BlockControl`, which this image binds with payload type `BlkReqHeader` (02-language.md §4: a pool name is bound to exactly one pool node, and `own[P] T` is a handle to a `T` allocated from *that* pool) — and this is a DMA pool, whose slot is exactly the 16 bytes of `BlkReqHeader` (03-hardware.md §3: a transfer payload's `T` is `@layout(dma)`, reported to the byte), so an `own[BlockControl] BlkReqStatus` handle would name a slot of the wrong shape for a device to read or write

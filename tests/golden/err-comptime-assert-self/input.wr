module examples.err_comptime_assert_self

struct Box:
    n: i64
    fn check(self) -> i64:
        comptime assert self.n > 0, "n"
        return self.n

@test pub fn go(): assert Box(n=1).check() == 1, "ok"

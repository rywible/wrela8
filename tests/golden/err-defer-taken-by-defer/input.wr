module test

resource struct Packet:
    size: u64

    init(mut self, size: u64):
        self.size = size

pub fn use_it(take p: Packet) -> u64:
    defer:
        v = p.size
    defer:
        q = take p
    return 0

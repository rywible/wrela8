module examples.err_scope_use_after_if

pub fn classify(n: u64) -> u64:
    if n == 0:
        label: u64 = 10
    elif n == 1:
        label: u64 = 20
    else:
        label: u64 = 30
    return label

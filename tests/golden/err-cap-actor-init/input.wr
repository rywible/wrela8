module examples.err_cap_actor_init


struct VirtioBlock:
    id: u32

@actor
pub struct Cache:
    n: u64

    init(mut self, take cap: DeviceCap[VirtioBlock]):
        self.n = 0

    pub fn count(read self) -> u64:
        return self.n

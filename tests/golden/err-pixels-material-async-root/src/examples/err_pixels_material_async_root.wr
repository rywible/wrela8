module examples.err_pixels_material_async_root

from core.render import MaterialSample, SurfaceContext

enum MaterialId:
    Surface

@material
async fn shade(surface: SurfaceContext[MaterialId]) -> MaterialSample:
    return surface

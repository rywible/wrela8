module examples.check_actor_private_handle_helper

@actor
pub struct Relay:
    peer: Actor[Relay]

    pub async fn deliver(mut self) -> u64:
        return self.forward(self.peer)

    fn forward(read self, target: Actor[Relay]) -> u64:
        return 0

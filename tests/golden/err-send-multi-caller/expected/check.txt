error[actor]: a bare `send` to `Logger.record` is not proven infallible — consume its `Result[unit, CallError[never]]` (bind it or `match` it) at 26:9
  a message site targeting `Logger` (`record` in `Fanout.once`) is not provably executed at most once per root turn — `Fanout.once` is reachable from more than one static call site, from a loop, or through a recursive cycle
  02-language.md §9.4: `send` stands as a bare statement only where mailbox analysis proves admission cannot fail

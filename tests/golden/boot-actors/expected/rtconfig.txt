module __image_runtime

# generated by wrela; do not edit (dump --stage=rtconfig)

pub const N_CORES: usize = 1
pub const N_TURNS: usize = 3
pub const N_TURNS_LEN: usize = 3
pub const N_ACTORS: usize = 2
pub const N_DRIVERS: usize = 0
pub const READY_QUEUE_CAPACITY: usize = 3
pub const GROUP_ARENA_CAPACITY: usize = 0
pub const GROUP_SLOTS: usize = 1
pub const TURN_STRIDE: usize = 1024
pub const RTDATA_BYTES: usize = 3488

@layout(runtime, endian=little)
struct TurnArea:
    busy: u64
    suspended: u64
    resume_ready: u64
    @offset(0x40) ambient_group: u64
    @offset(0x3ff) _tail: u8

@layout(runtime, endian=little)
struct GroupSlot:
    in_use: u64
    capacity: u64
    active_children: u64
    deadline_ns: u64
    cancelled: u64
    parent: u64
    join_waiter: u64
    @offset(0x38) owner_turn: u32
    @offset(0x5f) _tail: u8

@layout(runtime, endian=little)
struct RuntimeTables:
    turns: [TurnArea; N_TURNS_LEN]

@placed(0x40510000)
pub static RT: RuntimeTables

@layout(runtime, endian=little)
struct GroupArena:
    slots: [GroupSlot; GROUP_SLOTS]

@placed(0x40510da0)
pub static GROUPS: GroupArena

error[build]: layout: actor `Worker` declares no `init`, so this image wires `led=...` to its field of that name — and boot has nothing to call: it zero-fills the whole state slot and stops (05-library.md §9's literal-constructor path). The wired value materializes as 1, which is not the zero the state-fill leaves, so the actor would boot with a value this image did not declare. Failing closed (plans/M7.md item W's residual, owned by item D) rather than reporting success over a wrong answer. Give `Worker` an `init` that takes it, or drop the argument.

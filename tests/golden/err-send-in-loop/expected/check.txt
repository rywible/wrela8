error[actor]: a bare `send` to `Logger.record` is not proven infallible — consume its `Result[unit, CallError[never]]` (bind it or `match` it) at 16:9
  a message site targeting `Logger` (`record` in `in_loop`) sits inside a loop or a closure body, so it can execute more than once per root turn
  02-language.md §9.4: `send` stands as a bare statement only where mailbox analysis proves admission cannot fail

# Permanent Pixels fixture: err-pixels-topology-branch

Protects: fixed-topology rejection `P003` even when both runtime arms use the
same primitive topology.
Deterministic geometry: branch arms select exact sphere/object IDs `1` and `2`.
First active: P1.
P0 status: activated; implemented in task P1.5.

# Permanent Pixels fixture: err-pixels-topology-branch

Protects: fixed-topology rejection `P003` with both runtime branch shapes shown.
Deterministic geometry: branch arms select exact shape IDs `1` and `2`.
First active: P2.
P0 status: production Pixels stage is not implemented; implemented in task P2.3.

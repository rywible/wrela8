module examples.err_pixels_param_mismatch

from core.field import Field, Rgb, Vec3, mark, plane
from core.render import (
    AoConfig,
    CameraBounds,
    LightConfig,
    MaterialSample,
    ProbeConfig,
    RenderProfile,
    RgbRange,
    ScalarRange,
    SurfaceContext,
    ToneCurve,
)

struct SceneParams:
    value: f32

struct OtherParams:
    value: f32

enum MaterialId:
    Ground

@driver
struct DummyDisplay:
    tag: u32

@field
fn world(p: Vec3, read params: OtherParams) -> Field:
    return mark(
        plane(p, Vec3(x=0.0, y=1.0, z=0.0), offset=params.value),
        object=0,
        material=MaterialId.Ground,
    )

@material
fn shade(surface: SurfaceContext[MaterialId], read params: SceneParams) -> MaterialSample:
    return MaterialSample(color=Rgb(r=0.0, g=0.0, b=0.0), roughness=0.5)

@image
fn build() -> Image:
    img = Image(name="err-pixels-param-mismatch", target=Target.wrela_machine_v1)
    display = img.driver(DummyDisplay)
    renderer = img.renderer[SceneParams](
        field=world,
        material=shade,
        display=display,
        width=64,
        height=32,
        refresh_hz=60,
        shade_hz=60,
        profile=RenderProfile.AaaByteExact,
        tone_curve=ToneCurve.Linear,
        near=0.1,
        far=10.0,
        world_min=Vec3(x=-1.0, y=-1.0, z=-1.0),
        world_max=Vec3(x=1.0, y=1.0, z=1.0),
        camera_bounds=CameraBounds(max_motion=0.0),
        light_config=LightConfig(capacity=0),
        exposure_range=ScalarRange(min=0.0, max=1.0),
        environment_range=RgbRange(
            min=Rgb(r=0.0, g=0.0, b=0.0),
            max=Rgb(r=1.0, g=1.0, b=1.0),
        ),
        ao=AoConfig(enabled=false),
        probes=ProbeConfig(enabled=false),
        initialization_deadline_ms=1,
    )
    img.on_failure(policy=Failure.Halt)
    return img.seal()

error[unimplemented]: the runtime test tier could not compile this program: actor `Sink`'s own `init` takes `led: Actor[Ledger]` with no `led=...` argument in this image — 05-library.md §9 allows an actor handle to be substituted by type there, but boot materializes only the arguments the image wires by name. Wire it explicitly, or wait for handle substitution.

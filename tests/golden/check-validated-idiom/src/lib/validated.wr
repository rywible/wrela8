module lib.validated


pub resource(manual) struct Validated:
    value: u64

    pub fn validate(raw: u64) -> Validated:
        return Validated(value=raw)

    pub fn into_value(take self) -> u64:
        return take self.value

module examples.err_image_dma_pool

struct Payload:
    id: u32

pool Payloads

@image
pub fn build() -> Image:
    img = Image(name="x", target=Target.wrela_machine_v1)
    buffers = img.dma_pool[Payload](name=Payloads, device=1, count=4)
    return img.seal()

error[build]: the image report cannot be produced: 1 registered `@layout_assert` fn(s) (check_limits) — layout does not exist until the hardware/placement milestone (M5), and this compiler never asserts against nothing (decision 10); `--stage=image` still dumps the raw graph for such an image, only the report itself refuses

error[build]: the image report cannot be produced: 1 registered `@layout_assert` fn(s) (check_limits) — registered @layout_assert fns cannot run yet: the stdlib ImageReport reflection type does not exist (gap image.report.layout-asserts); this compiler never asserts against nothing (decision 10); `--stage=image` still dumps the raw graph for such an image, only the report itself refuses

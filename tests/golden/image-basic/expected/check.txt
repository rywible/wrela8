Module path=examples.image_basic
  Enum Feature data
    variant Flush
    variant RingReset
  Struct NicHw data
    field id: u32
  Struct Blk resource
    field id: u32
    field queue_depth: u32
    init(mut self, queue_depth: read u32) -> unit
    fn ping(read self) -> u32
  Struct Store resource
    field count: u32
    field disk: Actor[Blk]
    init(mut self, disk: read Actor[Blk]) -> unit
    fn get(read self) -> u32
  Pool Buffers
  Fn build() -> Image

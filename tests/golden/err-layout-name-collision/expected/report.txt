error[build]: two modules in this build closure declare a `@layout` type named `Ctl` — `a.main` and `z.other`. A `@layout` type's identity is its bare name here (no type name resolves across a module boundary in this compiler), so a pool or a driver naming `Ctl` would silently get whichever one this table happened to keep, and 03-hardware.md §3's exact bytes are the bytes a device reads. Rename one of them

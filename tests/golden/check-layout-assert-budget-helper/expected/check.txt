Module path=image_report
  Struct ImageReport data
    pub field machine_revision: u32
    pub field entry: u64
    pub field pages_base: u64
    pub field pages_size: u64
    pub field stacks_base: u64
    pub field stacks_size: u64
    pub field code_base: u64
    pub field code_size: u64
Module path=examples.check_layout_assert_budget_helper
  Struct Store resource
    field n: u32
    init(mut self) -> unit
    fn get(read self) -> u32
  Fn under_budget(size: read u64, cap: read u64) -> bool
  Fn budget(report: read ImageReport) -> unit
  Fn build() -> Image

module examples.err_recursion_method


struct Walker:
    depth: u64

    fn step(self, n: u64) -> u64:
        if n == 0:
            return self.depth
        return self.step(n - 1)

@test(runtime)
fn drives_the_cycle():
    w = Walker(depth=1)
    _ = w.step(3)

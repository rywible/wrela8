error[type]: field `TurnTable.turns: [TurnArea; N_TURNS]` has a length that is not an integer literal; a `@layout` type's size and offsets are checked before name resolution and nothing in them is evaluated, so an array field's length is a literal — a `const`-named length is inference by another name, the same rule `@offset(n)` already states (03-hardware.md §3, plans/M10.md decision 580) at 30:5

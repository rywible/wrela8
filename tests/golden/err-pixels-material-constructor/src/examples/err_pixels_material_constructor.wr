module examples.err_pixels_material_constructor

from core.field import Rgb
from core.render import MaterialSample

fn forge() -> MaterialSample:
    return MaterialSample(color=Rgb(r=0.0, g=0.0, b=0.0), roughness=0.5)

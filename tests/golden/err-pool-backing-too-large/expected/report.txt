error[build]: this image's pools reserve 67108864 bytes of backing, past the 16777216-byte ceiling this compiler will place — pool backing is zeroed image bytes (like the actor runtime tables), and guest DRAM is 1 GiB (06-machine.md §2). Failing closed rather than emitting an image no machine can load

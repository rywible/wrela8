Module path=image
  Fn mk() -> u32
  Const MAILBOX: u32
  Struct Store resource
    field count: u32
    init(mut self) -> unit
  Fn build() -> Image
Module path=shapes
  Struct Pair data
    field a: u32
    field b: u32
    fn sum(read self) -> u32
FieldVisibilityCensus constructs=2 reads=0 writes=0 patterns=0 fields=2
  construct use=image decl=shapes struct=Pair field=a
  construct use=image decl=shapes struct=Pair field=b

module examples.layout_island

fn compute(a: u64, b: u64) -> u64:
    return a + b

fn run(body: fn() -> u64) -> u64:
    return body()

fn use_it() -> u64:
    a = run(||:
        x = compute(
            1,
            2,
        )
        x + 1)
    return a

module examples.err_pixels_material_parameter_count

from core.render import MaterialSample, SurfaceContext

struct Params:
    value: u32

enum MaterialId:
    Surface

@material
fn shade(
    surface: SurfaceContext[MaterialId],
    read params: Params,
    extra: u32,
) -> MaterialSample:
    return surface

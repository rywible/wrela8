error[build]: the runtime test tier could not lay out the test image: actor `Sink` wires `mode=...` to `Sink.init`'s own `mode: Mode`, but the value is an enum value — boot passes arguments in registers (`x1..`), and this compiler has no register representation for that shape. Failing closed rather than passing a zero.

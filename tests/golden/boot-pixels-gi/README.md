# Permanent Pixels fixture: boot-pixels-gi

Protects: probe initialization, update, interpolation, host model, and guest digest.
Deterministic geometry: the guest oracle has one 2x2x2 integer clipmap level;
the compiled flagship constructor remains pinned at three 16x8x16 levels.
First active: P10.
P0 status: activated; implemented in task P10.9.
P10 status: active deterministic probe-GI boot fixture.

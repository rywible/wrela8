error[build]: `driver#0` binds a device to `BlkDriver`, but `BlkDriver.init` takes `regs: Mmio[VirtioIrqMmio]` — an `Mmio[L]` is not minted at the image binding at all: the sealed transport hands one out from an already-claimed device (`claimed.map_partition(L)` — 03-hardware.md §2/§9). Declare it as a `@driver` field and assign it inside `init`, which is where 03 §1's own worked constructor puts it. Failing closed rather than substituting an unminted capability

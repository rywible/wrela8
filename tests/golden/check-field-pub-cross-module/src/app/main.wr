module app.main

from lib.open import Open

fn probe() -> u32:
    o = Open(value=1)
    o.value = 2
    return o.value

const N: u32 = probe()

@test
pub fn t():
    assert N == 2, "pub field cross-module"

error[unimplemented]: lowering a typed MMIO access, bring-up transition, or IRQ operation (03-hardware.md §2/§6/§9) inside an `async fn`: the synchronous path emits these (plans/M7.md items H1/G), and a driver's own `init` is synchronous. The async register readers are 03 §6's ISR and §7's bottom-half task — until the remaining item-G surface lands for async, this is not implemented yet

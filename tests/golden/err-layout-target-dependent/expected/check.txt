error[type]: field `Descriptor.len: usize` has a target-dependent width; a `@layout` type's bytes are exact on every target — use a sized integer (`u8`/`u16`/`u32`/`u64`, or their signed forms) (03-hardware.md §3) at 7:5

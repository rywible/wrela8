error[build]: `img.dma_pool[Record](name=Records, ...)` — `Record` is `@layout(wire)`, not `@layout(dma)`. 03-hardware.md §3 gives each kind its own meaning: `dma` is device-visible memory checked against the target ABI, `mmio` is a register map, `wire` is target-independent persistent bytes, `runtime` is the machine's own tables (§3.1)

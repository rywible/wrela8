module actor

@actor
pub struct Store:
    count: u32
    disk: u32

    init(mut self, disk: u32):
        self.count = 0
        self.disk = disk

    pub fn get(read self) -> u32:
        return self.count

error[build]: this image declares device-reachable pools for 2 different devices — `ControlA` (device#0), `ControlB` (device#1). The report's `BlkPool name= base= size=` mapping line carries no device, and the VMM maps every one of them for its single device model, so pool bytes declared reachable from one device would be reachable from another (03-hardware.md §3: all memory a device can reach originates from *its* bound pools; plans/M7.md decision 5). Failing closed until the report can say which device a window belongs to (plans/M7.md item E, which owns the `BlkDevice`/`BlkQueue` lines that format needs anyway).

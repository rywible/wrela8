module lib.counter


pub struct Counter:
    pub n: u32

    pub fn zero() -> Counter:
        return Counter(n=0)

    pub fn get(read self) -> u32:
        return self.n

    pub fn bump(mut self):
        self.n = self.n + 1

    pub fn into_n(take self) -> u32:
        return self.n

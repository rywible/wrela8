module examples.mwir_enum_match


pub enum Shape:
    Circle(u64)
    Square(u64)
    Point

pub fn area(s: Shape) -> u64:
    match s:
        case .Circle(r):
            return r * r
        case .Square(side):
            return side * side
        case .Point:
            return 0

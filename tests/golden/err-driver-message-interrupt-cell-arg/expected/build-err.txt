error[build]: `@driver` `BlkDriver` is declared with `mailbox=`, so its `pub` method `BlkDriver.adopt_pending` is a message shape — and parameter #1 carries `InterruptCell[u32]`. 03-hardware.md §6: `InterruptCell[T]` is "the sole ISR/ordinary-code channel", interrupt-atomic with respect to every vector that may touch the cell — a channel between this driver's ISR and this driver's own ordinary code. A mailbox is a different channel between different principals, and a cell that crosses it is a second, unordered one. Export the value the cell holds, not the cell

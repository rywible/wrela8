module examples.err_method_generic_unsatisfied


struct Sector:
    id: u64

struct Table:
    fn apply[T](read self, x: T) -> u64:
        return x.hash()

pub fn use_it(t: Table, s: Sector) -> u64:
    return t.apply(s)

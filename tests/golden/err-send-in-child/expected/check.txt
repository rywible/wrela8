error[actor]: a bare `send` to `Logger.record` is not proven infallible — consume its `Result[unit, CallError[never]]` (bind it or `match` it) at 18:5
  a message site targeting `Logger` (`record` in `child`) sits inside a `g.start` child, which decision 5 excludes
  02-language.md §9.4: `send` stands as a bare statement only where mailbox analysis proves admission cannot fail

error[sema]: recursive call: `down` calls itself. 04-compiler.md §1 rejects unbounded recursion in the call graph — this machine has no stack guard (per-core stacks are packed contiguously in high DRAM, so an overrun silently corrupts the next core's frames rather than faulting), so every call depth must be statically bounded. Rewrite the cycle as a `@budget(bound=N)` loop at 15:16

error[build]: the runtime test tier could not lay out the test image: actor `Sink` declares `init` with 1 parameter(s), and this image declares an instance of it — boot can only call a zero-argument `init`, so the actor's state would silently stay zero-initialized instead of carrying its wired arguments. Init-argument materialization is not implemented (plans/M7.md); failing closed rather than booting a wrong answer.

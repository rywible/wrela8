# Permanent Pixels fixture: check-pixels-area-light

Protects: deterministic soft-shadow integration without false-lit or false-shadowed output.
Deterministic geometry: the rectangular emitter has integer half extents `1` and `2`.
First active: P9.
P0 status: activated; implemented in task P9.8.
P9 status: active; the sealed light topology is rectangular-area rather than
the earlier point-light placeholder.

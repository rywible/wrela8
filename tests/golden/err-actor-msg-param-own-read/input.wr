module examples.err_actor_msg_param_own_read


pool Packets

resource struct Packet:
    pub n: u64

@actor
pub struct Sink:
    pub fn ingest(read self, p: own[Packets] Packet) -> u64:
        return p.n

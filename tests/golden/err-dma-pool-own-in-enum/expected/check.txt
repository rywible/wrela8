error[type]: enum `SlotHolder` declares `own[BlockControl] BlkReqStatus` in a variant payload; a pool handle there is not implemented (plans/M7.md item E). The rule that gives `own[P] T` its meaning — `T` is the payload type the image bound `P` with (02-language.md §4, 03-hardware.md §3) — is checked over fn signatures, struct fields and generic instantiations, and an enum payload reaches none of them, so accepting this would leave the handle's own pool binding unchecked. Hold it in a struct field instead at 53:10

module examples.catalogue

from core.bytes import Bytes
from core.time import now, ms as Millis
pub from core.result import (
    Result,
    Err as ResultErr,
)

const LIMIT: u64 = 0x1000_0000

pool Widgets

pub fn increment(mut counter: u64) -> u64:
    return counter + 1

resource struct Ledger:
    entries: u64

comptime if target.debug:
    const TRACE: bool = true

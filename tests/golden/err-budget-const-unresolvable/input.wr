module examples.err_budget_const_unresolvable


struct Other:
    n: u64


pub fn spin(n: u64) -> u64:
    total: u64 = 0
    i: u64 = 0
    @budget(bound=Other)
    while i < n:
        total = total + i
        i = i + 1
    return total

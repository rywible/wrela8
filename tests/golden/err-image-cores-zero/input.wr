module examples.err_image_cores_zero


@image
pub fn build() -> Image:
    img = Image(name="cores-zero", target=Target.wrela_machine_v1, cores=0)
    img.on_failure(policy=Failure.Halt)
    return img.seal()

module test

resource struct Packet:
    size: u64

    init(mut self, size: u64):
        self.size = size

pub fn move_by_runtime_index(take arr: [Packet; 2], i: usize) -> Packet:
    return take arr[i]

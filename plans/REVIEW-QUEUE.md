# Review queue — normative edits made without a human

Audit trail (GOAL.md rule): every normative `docs/language/` edit made
during autonomous orchestration gets one line here — file §section,
one-line summary, commit sha. Newest last.

- 05-library.md §9 — seal spelling reconciled to `img.seal()` (02 §12.1's example and virtio-storage.wr:639 already used the method form; 05 §9 alone said `seal(take builder)`) — c0bce9e
- bench/thresholds.toml check_golden_median_us — raised 40000 → 400000 at M6-A (b6fd80f): the check-lane bench times sema over the whole golden corpus, which grew 25 → 154 entries since the lock; measured release-mode median ~13ms (was ~13ms at lock time over the smaller corpus in debug-adjacent conditions); raise is corpus growth + headroom, not an algorithmic regression. Recorded here per the threshold-raise rule (before/after numbers above).
- 02-language.md §12.2 — @test(runtime) fns may declare Actor[T] params; runner supplies the unique declared instance's handle (M6's execution surface needed a doc-backed handle path) — f6161f3
- OBSERVATION, no code change (M6 item H) — `--stage=mwir` and `--stage=flowwir` each silently omit the half of the program the other owns: on a mixed sync+async file, mwir renders only the sync fns and flowwir only the async ones, with no marker saying the rest exists elsewhere. `--stage=asm` (the union) is complete, and the partition predicate (`TypedFn::is_async`) is identical on both sides, so no fn can currently fall out of both — this is a review-surface honesty question, not a live defect, which is why it was recorded rather than coded (GOAL.md: extra ideas outside exit criteria go here, never into code). Worth a human call because the golden diff *is* the review surface: a future misclassification would move a fn between two goldens and neither diff would look like an error. Cheapest dumb fix if wanted: each dump prints a trailing one-line-per-key section naming the fns the other stage owns.
- 06-machine.md §1 (+ README / 01-model / 04-compiler core-count sentences) — 4 → 3 vCPUs per ROADMAP 2026-07-24; stacks region 4 MiB → 3 MiB; ledger `machine.cpu.three-vcpus` — 28a9bc1
- bench/thresholds.toml `[compiler]` — METHODOLOGY CHANGE, not a raise: the three corpus-sized lanes now lock **microseconds per entry** instead of a whole-corpus absolute median, per GOAL.md's "the compiler-lane locks are losing resolution" (its own commit, before/after numbers, this line). Before: `full_corpus_median_us = 60000` (locked at 58 entries), `check_golden_median_us = 400000` (re-locked at 154), `eval_tests_median_us = 120000` (locked at 3). After: `full_corpus_per_entry_us = 1000`, `check_golden_per_entry_us = 4000`, `eval_tests_per_entry_us = 20000` — each 10x the quiet measured per-entry median (90us over 421 entries / 337us over 361 / 1829us over 37), the same rounding rule the file always used. Motivation, measured: at 361 check-lane entries the absolute median was ~122ms against a 400ms lock, so a per-entry regression of nearly 3x could have landed untripped. `[build]` and `[guest]` keep absolute locks — one fixed workload each, no corpus to divide by. Mutation-tested: `check_golden_per_entry_us = 100` fails the lane with the per-entry sentence and exit code 1. Human call wanted on the honest caveat now recorded in the file: per-entry is corpus-*size* invariant but not corpus-*content* invariant (this lane drifted 260us -> 337us as the added goldens got individually heavier). — 1d5f5c3 (the amend that added this line; the identical tree was first committed as 3d0caf0)
- OBSERVATION, no doc change (M8, orchestrator + item R) — **no rung of the ROADMAP ladder owns 04-compiler.md §1's Progress paragraph.** The unified wait-for graph (actor turns, tasks, replies, receipts and their bottom-half producers, admission slots, permits, pools, cleanup and recovery nodes), its acyclicity, its "diagnostics print the full cycle", and the priority/deadline inheritance derived from it are normative and wholly unimplemented. M8 is multicore, M9 the stdlib, M10 the runtime in wrela, M11 the cost contract, and the ladder ends there — none of them names it. It has never bitten because no cycle is currently *reachable*: `@image` wiring is the only way to hand out an `Actor[T]` and its arguments evaluate in declaration order with no forward reference, so the handle graph is acyclic by construction (M6 item D's recorded finding, re-verified 2026-07-25: `a = img.actor(A, peer=b)` above `b = img.actor(B, peer=a)` is `error[name]`). That is a floor that holds only while handles cannot be rebound, and nothing states it as a rule. Recorded as ledger gap `compiler.progress.wait-for-graph` (65e85c6) so the ledger stops implying §1 is covered; whether the ladder grows a rung for it is a human call. — 65e85c6
- SUPERSEDED, recorded rather than dropped (M8 item C1, a5702b3) — that branch independently proposed re-locking `full_corpus_median_us` 60000 -> 600000, having measured the same dilution from the other side (52860us at 421 entries, 61360-62122us hot at 423, against a 58-entry lock that was never re-measured). The per-entry methodology change above landed first and made the absolute key nonexistent, so the raise was dropped at merge and its measurements folded into `bench/thresholds.toml`'s own comment instead. Two independent agents reaching for the same lock the same day is the evidence that GOAL.md's standing note was right about this one. — merge of m8-item-c1
- 05-library.md §2 (+ §9 `img.driver` bullet) — a `@driver` is messageable exactly when its declaration carries `mailbox=n`; its `pub` methods are the whole exported surface and carry no capability, sealed queue value, protocol state or receipt in either direction; its ISR and `@task` bottom half are never admissible; device completions keep their own path. Lifts M7 decision 22 / M6-D driver-mailbox floor (plans/M8.md item D, decisions 12–15); ledger `library.driver.messageable` — 993dc90
- 05-library.md §2 — widened the messageable-driver sentence to add "receipt, or **interrupt cell**" to what may not cross a driver mailbox. `InterruptCell[T]` is not sealed authority (M7 decision 17) so every containment rule was blind to it, but 03 §6 calls it "the sole ISR/ordinary-code channel" — a channel between one driver's ISR and its own ordinary code, not between principals. Found by an orchestrator capability-escape sweep; plans/M8.md item D decision 23; same clause `library.driver.messageable` — 321ab12
- 05-library.md §2 — narrowed the receipt half of the messageable-driver sentence to the **parameter** direction ("no message parameter carries a receipt") and named 03 §5's handoff reply as the exception. Item D wrote that sentence under a floor item E lifts; as written it contradicted 03 §5, which blesses `take p: P -> Receipt[P]` on a public driver method by name and says admission installs the caller-owned receipt — a sentence that only means anything for a *messaged* driver. plans/M8.md item E, decisions 32–33; ledger `library.driver.messageable` — 69bbd1c
- 02-language.md §9.4 — added one paragraph exempting 03 §5's handoff calling convention from the `CallError` composition table: a handoff call's result is `Receipt[P]` as declared. Doc-contradiction tiebreak (GOAL.md): §9.4 describes the ordinary awaitable, §5 defines a named convention for one signature and states its result; the specific rule wins and §9.4 now says so. Also load-bearing rather than cosmetic — with the composition in place the convention is unreachable from source, because `flowwir_lower` does not lower a `match` containing an `await` and `CallError` is not source-nameable so `?` cannot open the `Result`. plans/M8.md item E, decision 32; ledger `actors.calls.callerror-composition` — 69bbd1c
- OBSERVATION, no normative edit yet (M9 item OO) — struct field `pub` is parsed and ignored; `DeclField` drops `is_pub`; cross-module construction/read of non-`pub` fields is pervasive in multi-module goldens (measured: 44 constructions + 30 reads across 27 packages; only 12 `pub` fields tree-wide). OO stopped on large blast radius rather than migrate. If a human chooses enforcement, 02 §2 needs an explicit sentence after "Declarations are module-private unless `pub`." Proposed: "Struct fields are module-private unless `pub`; only the declaring module may construct, read, write, or pattern-bind a non-`pub` field." Until then 05 §5's "opaque" Instant and `stdlib/core/time.wr`'s header are aspirational. — 53dcabd
- OBSERVATION, no normative edit yet (M9 item I) — 02-language.md §2 lists the fixed prelude as only `Option`/`Some`/`None`/`Result`/`Ok`/`Err`/`panic` and says "Everything else is imported." Code also auto-resolves without import: (1) time constructors and `Duration`/`Instant` (item E decision 300, kept by item I decision 470 so ~194 `seconds(...)` goldens need no import reshape); (2) five stdlib enums `Target`/`Restart`/`BootError`/`DriverMode`/`CompletionOutcome` (item I decision 472 — same golden-stability reason; definitions now in `stdlib/core/*.wr`); (3) compiler type/intrinsic bare names (`Actor`, `Image`, `group`, `now`, …) which are language surface, not imports. Docs win by default would require imports and move the golden suite; code is kept. Proposed replacement for the §2 prelude paragraph: "A fixed prelude is always in scope: `Option`, `Some`, `None`, `Result`, `Ok`, `Err`, `panic`. Scalar type names are builtin. Compiler-recognized intrinsic and hardware type names resolve without an import. The toolchain may also auto-bind selected `core` names (`Duration`/`Instant` and their constructors; `Target`/`Restart`/`BootError`/`DriverMode`/`CompletionOutcome`) without a source import; everything else is imported." — 19a08bb
- 02-language.md §9.5 — `storage.read(path)?` → `storage.read_file(path)?`: `read` is a keyword and cannot be a declared method name (`syntax.diagnostics.parse-errors`). **The `?` is kept.** J2a's edit also dropped it, to dodge the CallError-not-source-nameable residual §9.3 `:604` hits; the orchestrator restored it (decision 519) — the keyword problem does not motivate removing error propagation from an example whose subject is deadline and cancellation, and §9.5 is now counted as a second instance of that residual rather than edited around it. Rejected: allowing keywords as method *declarations* (access after `.` already permits `.read()`). — 754c2e331fb10a5f5dfdfbb293d1b3a9f4a0ccdd
- 03-hardware.md §6 — fence comment: ISR via irq.bind in driver init (corpus preamble carries the bind; `init` cannot open a ```wrela fragment). — fd1dd8bbb41acd5127f3d67933ba136bfc93a5f3
- 02-language.md §7.2 — Lookup worked example mixed `return None` (Absent) with `return Err(error)` (Failed) in one function. Resolved to Result by **nesting, not collapsing**: the enclosing function reads as `Result[Option[T], IoError]`, so the whole repair is the single line `return None` → `return Ok(None)`, leaving `use(value)` and `return Err(error)` untouched. Tiebreak: Failed/Err is the load-bearing arm, so Result wins; but absence stays `None` rather than becoming an I/O error. (J2c first wrote `return Err(IoError.OutOfRange)` for Absent and `return Ok(value)` for Found; the orchestrator reverted both — the first changes what the doc means, the second rewrites an unrelated prose placeholder.) plans/M9.md item J2c decision 533.
- OBSERVATION, no normative edit (M10 planning reconnaissance, 2026-07-26) — a **live defect** was found in shipped M8-C2 code and fixed in the same commit: `layout::caller_core`'s `None => 0` fallback made the cross-core checkpoint guard vacuous for free *functions* (as opposed to free *turns*), so a loop hoisted out of a core-1 actor method into a free fn built and then hung at boot by clearing core 0's pending word. Fixed conservatively (fail closed on unattributed), pinned as `golden/err-cross-core-checkpoint-free-fn`, ledger `actors.scheduling.per-core-loop`. **Two items a human may want to weigh in on.** (1) The fix now refuses an unattributed free fn containing a loop in *any* multi-core image, even when every caller is provably on core 0 — a false-refusal the orchestrator chose over a transitive-caller-core analysis, on GOAL.md's "dumbest fail-closed option" rule; the more precise fix is a real call-graph reachability pass and is a fair thing to prefer instead. (2) Observed but **not fixed and not witnessed**: the group deadline raise at `layout.rs:862-864` writes `1` rather than OR-ing bit 0, which would drop IRQ/wake bits on any multi-vector build. That is a separate defect; it is recorded here rather than fixed opportunistically inside a planning pass.
- 03-hardware.md §3 — the `@layout` kind list goes **three → four**: `runtime` (the machine's own tables, §3.1) joins `dma`/`mmio`/`wire`. Required by ROADMAP before any M10 migration; plans/M10.md item A, decision 561. The four diagnostics that hardcoded the three-kind set moved in the same commit, so the compiler cannot advertise a closed set it no longer has. Note that `hardware.layout.exact-bytes`' note quotes the old sentence verbatim ("with three kinds …"); the note was **not** rewritten (ledger append-only for this item) and the new clauses say so — a human may want to correct it. — d8cd86bc97891b7e76c338d7ec43a512c46bd096 (the id of this identical tree before these four audit lines were appended to it by amend; the 1d5f5c3 line above records the same amend-vs-first-commit split)
- 03-hardware.md §3.1 (new) — defines `@layout(runtime)` (every §3 rule unchanged, plus the one allowance of nested/array `@layout(runtime)` fields; not device-visible; no capability) and `@placed(ADDR)` (binds a module-level `static` of a `@layout(runtime)` type to a fixed comptime address, at most one per address, legal nowhere else). **Written with a stated tension rather than papered over**: §3.1 presupposes a `static` declaration and revision 0.1 has none — `static` is not a keyword, `ast::Item` has no such variant, and the word appears in no other normative rule — so the section says so in bold and the compiler refuses `@placed` wherever it is written. The example is a ```text fence, not ```wrela (unimplementable by construction at this item; the corpus sema-checks every wrela block). Ledger `hardware.layout.runtime-class`, `hardware.layout.placed-static`, `hardware.layout.placed-verified`, all `gap`. — d8cd86bc97891b7e76c338d7ec43a512c46bd096 (the id of this identical tree before these four audit lines were appended to it by amend; the 1d5f5c3 line above records the same amend-vs-first-commit split)
- 02-language.md §13 — attribute table: the `@layout` row's kind list gains `runtime`; a new `@placed(addr)` row; and one sentence after the table recording that revision 0.1 declares no `static`, so the construct 03 §3.1 defines `@placed` on is not in this language yet and the attribute is refused wherever written. Deliberately **one** sentence: this item does not invent statics, and it does not turn on §13's own (still unenforced anywhere) "unknown attributes are errors" rule — that is its own, much larger, item. — d8cd86bc97891b7e76c338d7ec43a512c46bd096 (the id of this identical tree before these four audit lines were appended to it by amend; the 1d5f5c3 line above records the same amend-vs-first-commit split)
- docs/language/README.md — the `@dma`/`@mmio`/`@wire` collapse row's kind list gains `runtime`, tracking the 03 §3 change above. — d8cd86bc97891b7e76c338d7ec43a512c46bd096 (the id of this identical tree before these four audit lines were appended to it by amend; the 1d5f5c3 line above records the same amend-vs-first-commit split)
- DECISION 580, recorded here because plans/M10.md is owned by a sibling agent this run (M10 item A2) — **an array field's length is an integer literal, and nothing else.** 03-hardware.md §3.1's own example spells `turns: [TurnArea; N_TURNS]`; that is refused, and `golden/err-layout-runtime-const-len` is the example verbatim, pinned as a rejection. Reasoning: `check_layouts` runs before name resolution (`sema/mod.rs`) and evaluates nothing, and `parse_offset_attr` already refuses a `const`-named offset in so many words ("a `const`-named offset is inference by another name"); an array length is the same question, so accepting one and refusing the other would be two answers to one rule. **Rejected alternative (ii): a narrow evaluation path for array lengths only** — scanning the module's own items for `const N = <literal>`. It stays a pure function of the ast, so the purity claim survives on its letter, but it is a second name resolver that can disagree with the real one on imports, shadowing and `comptime if`-guarded consts, and `sema/types.rs` already carries a note ruling out exactly that hazard in this pass ("one list, in one place — several copies could disagree; one cannot"). **Rejected alternative: moving `check_layouts` after name resolution** — its doc block gives two reasons it runs early, one of which (03 §3's capability rule must bite before a capability name resolves at all) is load-bearing for `golden/err-layout-wire-capability`. The end state a human may prefer instead: a second, later *layout-completion* pass that re-runs sizing once consts are evaluated, keeping the early pass exactly as pure as it is. Until that lands, §3.1's example is aspirational and the compiler says so at the declaration.
- OBSERVATION, no doc change (M10 item A2) — **03-hardware.md §3.1 was not amended to record decision 580's literal-only array length**, even though its example is now something the compiler refuses. §3.1 has the precedent for exactly this shape of admission (its bolded "Revision 0.1 has no `static` declaration" sentence), and the honest edit is one parallel sentence. It was not made because `xtask/src/corpus_sema_context.rs` and `corpus_sema_census.rs` both key a doc block by `(path, start_line)` and four keyed 03-hardware.md blocks sit *below* §3.1 (lines 151, 221, 265, 299) — inserting a sentence silently detaches their preambles and turns pinned accepted disagreements into reported decay, attributing the failure to the wrong cause. That is plans/M10.md **item A3** verbatim, and `crates/xtask/` was owned by a sibling agent this run, so the edit could not be paired with the re-keying it needs. Do the doc sentence in, or immediately after, item A3.
- OBSERVATION, no code change (M10 item A2) — **a `runtime` layout can be declared and reported but still cannot be *used* for M10's actual purpose**, and the literal-only length is the reason. The turn/mailbox/group tables the milestone is migrating are sized per image (`reply_ring_capacity`, `n_turns`), so a literal length cannot spell them; every table shape that varies by image needs decision 580's follow-up before it can be declared as one `@layout(runtime)` type instead of hand-computed offsets. Sequencing note for whoever picks up items E/F: the field types now exist, the *lengths* do not.
- 03-hardware.md §3.1 — one sentence added to the `@layout(runtime)` paragraph: an array field's length "is an integer literal or the name of a module-level `const` whose comptime value is one or more, so a table sized per image is one declaration too; `@offset(n)` stays an integer literal, because an offset the compiler must evaluate is inference by another name." Lands the sentence item A2 could not (it needed A3's content-keyed corpus tables first) and makes §3.1's own example compile: plans/M10.md item A2b, decision 581, a second layout-completion pass after const evaluation while `check_layouts` keeps its pre-name-resolution purity. Two things a human may want to weigh. (1) The sentence states "one or more", i.e. **a zero-length array field is illegal** — decided rather than discovered (a zero-byte field is the fail-open 03 §3 exists to prevent, and it matches the answer a literal `0` already got); if a zero-length table is ever wanted, this sentence is what has to change. (2) A **new compiler bound with no normative basis**: `MAX_LAYOUT_BYTES` = 16 MiB on one `@layout` type's total size, added because a `const` length can now spell `2^40` from one line and `2^43` bytes fits a `u64` unnoticed. It is reasoned from the machine's 1 GiB (06-machine.md), not from any §3 rule, and it is a fail-closed floor like `MAX_LAYOUT_NEST_DEPTH`; a human may prefer it stated in the docs or set elsewhere. Ledger `hardware.layout.runtime-class` (still `test`). — d2c5913 (the id of this identical tree before this line was appended to it by amend)
- bench/thresholds.toml `[guest]` — WORKLOAD CHANGE (not a raise): `boot_hello_median_us = 600000` on `tests/golden/boot-hello` (zero actors; measured median ~50-78ms almost entirely HVF create/1 GiB zeroing) replaced by `boot_actors_median_us = 700000` on `tests/golden/boot-actors` (two actors, mailbox FIFO, await park/resume, non-reentrancy — the M6 item F flagship). Absolute lock kept: GOAL.md's "lock microseconds per entry" known-risk was the *compiler* lane (already done); guest times one fixed workload. Measured quiet median ~67000us across five runs (medians 66644/70885/70084/67609/67287us); lock is 10x quiet, rounded up. Exact-count assertions retained. plans/M10.md decisions 661–664. — 65f9f79

- 03-hardware.md §3.1 — removes the bold "Revision 0.1 has no `static`" sentence; `static NAME: Type` with required `@placed(ADDR)` is now in the language; the example fence is ```wrela (with `const N_TURNS`) at `MACHINE_INFO_BASE` rather than aspirational ```text. Named-field scalar R/W and "no indexing yet" are stated. plans/M10.md item A2c, decisions 586–589. — (this commit)
- 02-language.md §13 — post-table sentence: revision 0.1 now has module-level `static`; `@placed` is legal only on a `@layout(runtime)` static. Replaces the "declares no static / refused wherever written" sentence. plans/M10.md item A2c. — (this commit)
- DECISION 667 (M10 item A2d) — auto-injected `core.runtime` is omitted from BuildInput and check/typed dumps unless explicitly imported, so always-loading does not churn ~83 boot reports' Input lines. Explicit `from core.runtime import …` stays honest. Companion to decisions 582–585; force-root emit still moves code section sizes (+28 B probe) and those reports were re-pinned with zero transcript changes. — (this commit)
- ROADMAP.md M10 `@brk` paragraph — corrects the false claim that `BRK_LINE_APPEND_OVERFLOW`/`BRK_LINE_COMMIT_OVERFLOW` become ordinary array-bounds checks with a better diagnostic; measured, that path recurses through the console formatter (plans/M10.md decision 590). Replaced by status-return dissolution (592) plus abort re-entrancy latch (591). Normative correction, not a language-doc edit. — (this commit)
- 03-hardware.md §3.1 — indexing into a placed array field is no longer "a later rule": bounds-checked load/store at `ADDR + field_offset + index * element_stride` via `__wrela_abort_val`. plans/M10.md item B1, decision 668. — (this commit)

- ORCHESTRATOR ERROR, disclosed rather than rewritten — **a 387-line change to `ROADMAP.md` was committed under an unrelated message.** `daf72ca` ("M10 item C follow-up: pin the abort tail's floor words") was staged with `git add -A`, which swept up a pre-existing uncommitted edit rewriting the M11 section (`### M11 — The cost contract` → `### M11 — The cycle proxy`, 158→201 lines, plus smaller edits in later sections). That edit was already in the working tree at the start of the session and is not the orchestrator's work; it looks like human drafting in progress, and a further revision of the same section was uncommitted again afterwards and has been left untouched. Standing doctrine therefore changed inside a commit whose message describes only a unit test, with no audit line at the time. Not rewritten, deliberately: `daf72ca` is the base of three later commits and of the branches items F0/E4 were built on, so a history rewrite would strand them for a cosmetic gain. What a human may want: confirm the M11 rewrite in `daf72ca` is wanted as committed, and re-split it into its own commit if the history matters. Process fix already in force: explicit paths only when staging, never `git add -A`. — daf72ca
- ESCALATION, needs human sign-off — **plans/M10.md's exit criteria cannot be met as written, and the gap is now a measured number.** The criteria require that "the only functions in `crates/wrela-compiler/` emitting instruction words are the entry stub, the checkpoint/vector stub, and the halt tail", combined word count ≤ 30, and that `build_rt_enqueue`/`build_rt_run_one`/`build_group_child_poll` "do not exist". ROADMAP M10 is stronger and sits above the plan in ground truth: those routines "become **wrela source**, compiled by the same backend as user code", with a floor of "exactly three things … roughly twenty instructions". Item F0's census (`emitted_a64_census.rs`) measures the reality after items C/D/E1–E4: floor **26** unique words (inside the plan's ≤30, above ROADMAP's ~20), **176** words of image-static Rust specialization sanctioned by frozen decisions 613/620 but admitted by neither document, **708** words still hand-emitted and owned by remaining items F/F2/G/H/I, and **23** unclassified. The 708 is on-plan and shrinks as those items land. The **176 is the real conflict**: decisions 613 and 620 satisfy items D and E3 with Rust functions that emit A64 word by word (`codegen::emit_rt_enqueue`, `emit_rt_run_one`), which relocates ROADMAP's "Rust-shaped hole" from `layout.rs` to `codegen.rs` rather than removing it, for a stated reason (the select/poll/drain target sets are image-static and a generic body needs a pointer-or-table type M10 refuses to invent). Those decisions are frozen and have not been relitigated. The fork a human should settle: either (i) accept per-image Rust specialization permanently and amend ROADMAP M10's claim plus this plan's criteria to name and cap it, or (ii) treat the 176 as scaffolding a later milestone must dissolve — which needs a comptime-specialization capability wrela does not have today. The orchestrator has deliberately **not** chosen: ROADMAP is standing doctrine, and an agent quietly rewriting the milestone's own success claim to match what it built is the exact failure this file exists to catch. M10 must not be marked COMPLETE until this is settled. — (this commit)

- ROADMAP.md ladder — inserts **M11 — The image runtime, generated and generic** (facts-only generated config module + generic `runtime.wr`; dissolves ImageStatic specialization). Former M11 cycle proxy → **M12**; former M12 optimization playground → **M13**. Active-plan pointer → `plans/M11.md`. Settles the REVIEW-QUEUE escalation's dissolve fork (option ii). The shortened cycle-proxy prose that was uncommitted after `daf72ca`'s accidental M11 rewrite is the text now committed as M12. Ledger note updates: `compiler.costs.predicted-vs-measured` (owner M12), `sema.bounds.loops` (narrowed by M11 item A), `compiler.progress.wait-for-graph` / `machine.display.golden-frames` (ladder-end sentences). plans/M10.md → COMPLETE. — (this commit)

- 02-language.md §8.1 / §13 (+ 04-compiler.md §1 Progress) — freezes the sync-loop `@budget(bound=N)` discharge (plans/M11.md decision 721): statement attribute immediately preceding `for`/`while`, `N` comptime-known integer ≥ 1, missing → `error[sema]`, acceptance = hidden trip counter (fail-closed runtime bound, no cost model). Async checkpoint and ISR halves unchanged / still gap. Ledger `sema.bounds.loops` note narrowed; status stays `gap`. — (this commit)

# M6 plan: actors on one core

**Status: ACTIVE.**

First deliverable of the milestone, per the planning convention. The docs
carry the substance — [02-language.md](../docs/language/02-language.md)
§9 (actors, turns, messages, calls/admission, groups) / §10 (teardown) /
§12.2 (async tests), [04-compiler.md](../docs/language/04-compiler.md)
§2 (scheduling semantics) / §4 (cancellation mechanics),
[06-machine.md](../docs/language/06-machine.md) §4 (checkpoint-injected
vectors) / §5 (park/deadline protocol) / §8 (record/replay) — and
[ROADMAP.md](../ROADMAP.md) M6: turns, mailboxes, `send`, groups,
deadlines, cancellation, checkpoint injection — **core 0 only**. Flips:
`actors.turns.non-reentrant`, `actors.send.statement-requires-proof`,
`machine.interrupts.checkpoint-injection`. The recorder's schedule
representation must be an enumerable choice sequence (ROADMAP's recorded
design constraint — non-negotiable shape).

## Goal

An image with real `@actor` structs runs on the wrela VMM: `@test(runtime)`
fns (sync or `async`) are the root turns; they call actor methods through
image-minted handles, `await` replies, `send` one-way messages, open
`with group(...)` scopes with capacities and deadlines, and observe
cancellation only at awaits and checkpoints. Turn-per-actor
non-reentrancy, FIFO admission, bounded mailboxes, and deterministic
teardown are all pinned by transcript goldens booted on HVF. Replay comes
alive as an enumerable choice sequence (on one core it has exactly one
admissible schedule — the representation still must carry choices, not
just a log).

## The honest scope line (decide once)

M6 is actors-on-core-0, not the whole async chapter. IN: `@actor` structs
with `async fn`/`fn` public methods; `Actor[T]` handle calls composing
`CallError[E]` per 02 §9.4; `await`; `send` (statement and
must-consume-Result forms); `with group(capacity=..., deadline=...)`,
`g.start`, `g.join_all`, implicit request-groups; ambient lineage;
deadline inheritance/narrowing; cancellation at awaits/checkpoints;
`defer`-based cleanup in reverse order; mailbox bounds (declared);
checkpoint emission + vector delivery + park/wake with the VMM; `now()`/
`ms()` as compiler-recognized intrinsics backing deadlines (the doc's own
§9.5 vocabulary; clock MMIO already exists). OUT (each fails closed,
named): pools/`own` in messages and actor-scoped `pool` runtime (M7 with
devices), receipts/drivers/ISRs (M7), `race` (sealed contract — after
group teardown is proven), `@detached`, `@task` installed roots (the
runtime-test root substitutes at M6), service slots (stdlib), placement
inference and `core=` ≠ 0 (M8), mailbox capacity *derivation* (declared
`mailbox=` is the bound; derivation is later), `must_service_within`,
restart/supervision *runtime* (the graph is checked since M4; restart
mechanics are M7+ — an actor abandon at M6 is image-fatal with a
diagnosable transcript line, recorded honestly).

## Exit criteria

- `cargo xtask check` green with: async sema goldens (accept + err
  corpus), FlowWir + mwir + asm dumps for async bodies, actor boot
  transcripts (turn ordering, send, groups, deadline-cancel) pinned,
  the choice-sequence record format pinned, all existing lanes still
  clean.
- `actors.turns.non-reentrant`, `actors.send.statement-requires-proof`,
  `machine.interrupts.checkpoint-injection` flip gap → test.
- The two clock/replay citability gaps (`machine.clock.trap-logged`,
  `machine.replay.clock-log`) flip — `now()` gives them real
  golden-visible guest reads.
- Deep fuzz all lanes (now seeing async-bearing corpus) 3+ fresh seeds;
  diff-eval extended or honestly scoped for async tests (decision 10).
- `plans/M7.md` does not exist yet.

## Shape decisions (frozen by goldens — decide once)

1. **Execution surface stays `@test(runtime)`** — now sync or `async`
   (02 §12.2's own words). The generated harness starts the image
   (actors constructed in dependency order from the M4 graph, mailboxes
   opened atomically per 04 §9), then runs each test as a root turn on
   the core-0 event loop. Tests still run strictly sequentially — one
   root turn at a time; a test's awaits interleave *actor* turns, which
   is the whole point.
2. **FlowWir materializes** (04 §8's named layer; M5 fused it away for
   sync code): async fns lower AOT to state machines in statically
   reserved frame slots (04 §2) — FlowWir is the typed, suspension-
   explicit form between the typed tree and mwir: states, awaits as
   suspension edges, lineage/deadline plumbing explicit, cleanup edges
   explicit. `wrela dump --stage=flowwir` before features, golden-
   covered. Sync fns keep the M5 path unchanged (existing goldens must
   not move).
3. **One event loop, one core, priority bands** per 04 §2: bands exist
   in the representation (device/normal/background) but only "normal"
   is populated at M6; FIFO per mailbox by admission order; scheduling
   key = FIFO head's (priority, effective deadline); ties by the
   deterministic round-robin cursor. The loop, mailboxes, frames, and
   admission slots are all statically laid out (no allocation): every
   actor's mailbox capacity, frame-slot arena (one slot per declared
   in-flight activation), and the scheduler's own tables are sized at
   build time into the data section — the report grows the accounting
   (sizes only, facts).
4. **Non-reentrancy is structural**: one turn owns the actor until
   return/error/abandon; an await parks the turn, marks the actor
   busy-suspended, admits nothing. Pinned by the flagship golden: actor
   A awaits B while a second message to A sits queued; the transcript
   proves the second turn starts only after the first completes
   (`actors.turns.non-reentrant`'s flip witness).
5. **`send` proof discipline** (02 §9.4): a bare `send` statement is
   legal ONLY where admission provably cannot fail; otherwise the
   `Result[unit, Rejected[...]]` must be consumed. M6 ships the
   dumbest sound proof: admission cannot fail iff the target mailbox's
   declared capacity ≥ the whole-image count of static send/call sites
   targeting it AND every such site sits outside any loop and outside
   any `g.start` child (so each executes at most once per root turn)
   AND root turns are serial (decision 1 guarantees this at M6). If
   that condition doesn't hold, the bare statement is a named
   `error[actor]` (new fixed category, added deliberately) telling the
   author to consume the result. If the executed goldens show this
   proof too weak to ever fire, the flip still stands on both sides
   (accept golden where proven, err golden where not) — never widen
   the proof without the analysis staying obviously sound. Record the
   shipped condition verbatim in the ledger clause.
6. **Checkpoints**: codegen emits a checkpoint at every loop back-edge
   and every turn boundary (await resume points are checkpoints by
   construction). No bound proving, no elision — a checkpoint is a
   short fixed sequence (load pending word, test, branch to the
   scheduler's service path). `sema.bounds.loops`' fate: read its
   clause text at execution; if it demands proved bounds it stays gap
   with an updated note (bound proving is deferred cleverness); the
   *checkpoint* half is what M6 delivers.
7. **Vector delivery** (06 §4): per-core pending word page (the
   machine contract grows it — wrela-machine constants, coherent with
   existing pages); the VMM raises by store + wake; the guest observes
   only at checkpoints/parks via mask–arm–recheck. M6's real injector
   is the **deadline service**: a parked guest writes its next deadline
   to the info page (06 §5); the VMM wakes it at that deadline; an
   expired group's cancellation is delivered as a vector observed at
   the target's next checkpoint. Flip witness for
   `machine.interrupts.checkpoint-injection`: a golden where a child
   spins in a bounded loop past its group's deadline and the transcript
   proves cancellation landed exactly at a checkpoint (deterministic
   iteration count — replay-identical), not mid-instruction.
8. **Cancellation/teardown subset** (02 §9.5, 04 §4): cancel closes
   admission, cancels children, runs ready `defer` cleanup in reverse
   source order, resolves `Cancelled` to the parent when the cleanup
   graph is empty. No device receipts exist yet, so the teardown graph
   is defers + child joins only. `CallError` variants erased per
   whole-image analysis where provable; at M6 erase only the trivially
   erasable (no deadline anywhere → no DeadlineExceeded, etc.),
   recorded.
9. **The recorder's choice sequence**: the record file becomes a
   sequence of explicit choice points — at M6, each admission event is
   recorded as (mailbox, chosen sender) even though exactly one
   candidate exists on one core, plus clock reads and deadline wakes.
   Replay consumes choices in order and diverges loudly on mismatch.
   The FORMAT (choice-tagged, enumerable — a later milestone can swap
   the chooser for an enumerator) is the deliverable ROADMAP's
   constraint names; pin it as a golden (`--record` of a fixed boot,
   with clock values normalized out of the pinned form — pin structure,
   not timestamps: the golden records a REPLAY file of a recorded boot,
   which is fully deterministic byte-wise given the recording... decide
   the dumbest pinnable form at execution and record it).
10. **diff-eval and async**: the evaluator does not execute async (M3
    scope). Comptime-legal tests stay the diff-eval corpus exactly as
    at M5; async runtime tests are backend-only and get no evaluator
    twin — recorded in the clause note, not a weakening (nothing that
    CAN run in both tiers stops being compared).
11. **`now()`/`ms()`**: compiler-recognized intrinsics (prelude-style,
    the img.* precedent): `ms(n)` pure comptime-legal duration
    constructor; `now()` runtime-only (illegal in comptime contexts —
    legality arm), lowering to the clock MMIO load. This flips
    `machine.clock.trap-logged` + `machine.replay.clock-log` with real
    golden-visible reads (deterministic transcripts must not PRINT raw
    clock values — tests use now() only for deadlines/durations
    compared against ms() constants, never printed).
12. **Abandon = image-fatal at M6** (no restart runtime): an actor
    turn that aborts (assert/overflow) prints the FAILED line (test
    root) or a named actor-fault line (actor turn), then the image
    exits nonzero deterministically. Restart mechanics arrive with
    M7+; the M4-checked supervision graph is carried in the report
    unchanged. Recorded so nobody mistakes it for the doc's restart
    semantics.

## Work items, in order

Progress (ticked in the same commit as the work):

- [x] A. Async sema: typing the actor surface (02 §9.1–9.5 subset) — done
  2026-07-24. Recorded decisions (ledger clauses
  `actors.calls.callerror-composition`/`actors.messages.value-restrictions`
  carry the full mechanism; summarized here per the plan's own "tick A +
  sub-notes for every recorded decision"):
  - **Free async fns**: in-scope (02 §5's own worked example; §9.5's
    `g.start(fetch_part, ...)` names exactly a same-module top-level
    `async fn`). Out of scope, named: an `async fn` with no receiver
    *inside* a struct (an associated fn — neither an actor method nor a
    spellable `g.start` target), `golden/err-async-free-fn`.
  - **`await`/`send`/`with group` all require an `async fn`/method
    context** (`FnCtx::in_async`) — a plain `fn` "never suspends" (02
    §5); `golden/err-await-outside-async` pins `await`'s own case.
  - **`g.start` callee set**: a bare same-module top-level `async fn`
    name, or `self.method` naming an `async fn` method on the enclosing
    struct — both `async fn` (a sync callee is named-rejected, M6
    scope). No bound-method-value machinery needed (`TypedExprKind::GroupChild`
    recognizes both shapes directly).
  - **`g.join_all()`'s array length**: the number of *static* `g.start`
    call sites found in the `with`-body (`bodies::compute_group_children`,
    a pure recompute — not incremental `FnCtx` state, since `matches.rs`/
    `flow.rs` each re-derive their own separate `FnCtx` and never replay
    the whole preceding body through `bodies::check_expr`), not the
    group's declared `capacity`. Every child in one group must share one
    declared return type (M6 scope, named if violated).
  - **Erasure (decision 8) shipped**: none — every composition keeps the
    full five-variant `CallError[E]`; no whole-image analysis proves a
    variant unreachable yet. Recorded, not silently narrowed.
  - **Bare `@test` on an `async fn` (no `(runtime)`)**: a hard build
    error, not a soft "FAILED" test line — `eval::check_test_legality`
    (M4-B) already required every *declared*-`Comptime` `@test` to be
    comptime-legal, checked at `--stage=check`/`--stage=typed` time, and
    an `async fn`'s own color is now unconditionally illegal for
    comptime (02 §12) regardless of body content — this item's `is_async`
    rule is the first thing that ever makes that path fire.
    `golden/err-test-async-bare-illegal`.
  - **`Actor[T]` retirement**: resolves as a real type
    (`sema::types::resolve_named`'s new arm + `validate_actor_handles`,
    a whole-module post-declare pass requiring `T` to be `@actor`/
    `@driver`, `DeclStruct::is_actor`, this item's own addition distinct
    from `is_resource_fiat`). `golden/image-basic`'s `Store.disk`
    placeholder (`u32`) becomes `Actor[Blk]`, both field and init-param;
    `eval::image_checks`'s handle substitution needed **no code change**
    (`is_handle_type_name` already matched by bare outer type name only).
    `expected/image.txt`/`expected/report.txt` byte-identical.
  - **CallError representation**: `Type::Named("CallError", [TypeArg::Type(E)])`,
    not a dedicated `Type` variant (mirrors `Actor`/`Group`/`Instant`/
    `Duration`/`Rejected` — the established M6 builtin-pseudo-type
    vehicle); its five variants recognized by name wherever a scrutinee
    says `CallError` (`bodies::variant_payload_types_for`/`matches::shape_of`),
    the `builtin_enum_variants` precedent extended to non-empty payloads.
  - **Cross-await path rule (02 §9.2)**: the dumbest sound
    approximation — a straight-line forward scan (`bodies::check_cross_await`)
    threading one `seen_await` flag, conservatively shared across
    sibling branches (over-rejects a little, never under-rejects); a
    nested `Field`-chain access rooted at anything but `self`, found
    after an `await`, is rejected. `golden/check-await-self-path` (accept)
    / `golden/err-await-external-path` (reject).
  - **Message-value restrictions (02 §9.3)**: `mut`/closure/`Actor[T]`-handle
    arguments to an actor call or `send` are named rejections; `take` of
    a resource is M7, fail closed with a distinct message; `take` of
    plain data and `Static[T]`/plain-data arguments are ordinary.
  - **`send` bare-statement floor (decision 5)**: every bare `send`
    statement is rejected at item A — the proof analysis that can make
    some infallible is item G's job. `golden/err-send-bare-unproven`.
  - **`now()`/`ms()`**: both `TypedExprKind::Intrinsic` (the `img.*`/
    `seconds` precedent); `ms(n)` is real, comptime-legal, evaluated
    (`eval::interp::eval_intrinsic`'s new arm); `now()` is a dedicated
    new `eval::legal` illegal-reason arm, runtime-only.
    `golden/err-now-in-comptime`.
  - Bench note: `bench/thresholds.toml`'s `check_golden_median_us`
    re-measured and re-locked (25 -> 154 golden entries since the
    original M2 lock, ordinary corpus growth, not an algorithmic
    blowup — see that file's own dated note).

  **Post-review fix (2026-07-24, verification's own probe)**: a public
  actor method's own parameter list is the message shape and its return
  type is the reply shape (02 §9.1/§9.4) — `Actor[T]` there was only
  rejected lazily at the first call site, never at the declaration that
  makes the violation possible. Fixed in `sema::types::declare`'s own
  `validate_message_shape` (alongside `validate_actor_handles`): a `pub`
  method of an `@actor`/`@driver` struct may not declare a parameter or
  return type containing `Actor[T]` at any nesting (array/struct-of-handle
  included, via `type_contains_actor_handle`'s recursion into a named
  struct's own fields). `init` stays exempt (image wiring, not a
  message). **Decision on non-`pub` methods**: legal — 02 §9.2's "calls
  on self are ordinary calls" means a private method is never reachable
  through `Actor[T]` at all (the identical `mf.is_pub` gate the call-site
  checks already enforce), so its parameter list is never a message
  shape; `golden/check-actor-private-handle-helper` pins this as a real
  accept case. New goldens: `golden/err-actor-handle-param` (the
  verification probe itself), `golden/err-actor-handle-reply` (return
  position), `golden/check-actor-private-handle-helper` (the non-`pub`
  accept case). `golden/err-actor-handle-in-message` re-goldened to the
  earlier (declaration-time) diagnostic — its own call-site shape
  (`Hub.forward`'s `await self.outer.relay(target=self.inner)`) is now
  unreached (`declare()` fails first), recorded in its own file and in
  `actors.messages.value-restrictions`'s ledger note, which also proves
  directly (not merely asserts) that the old call-site arm in
  `bodies::check_message_args` is unreachable for this exact shape now —
  kept as an internal defense-in-depth guard, not deleted, not claimed
  live-tested beyond what it shares with the mut-loan/closure checks.
  `cargo xtask check` green; fuzz sema 200_000 iterations × 2 seeds,
  clean.
- [ ] B. FlowWir: state machines + --stage=flowwir
- [ ] C. Runtime core: event loop, mailboxes, turns, park/wake (guest)
- [ ] D. Lowering/codegen: FlowWir → mwir → A76 + checkpoints
- [ ] E. VMM: pending words, deadline wakes, choice-sequence recorder
- [ ] F. Groups, deadlines, cancellation end-to-end + flagship goldens
- [ ] G. send-proof analysis + err corpus + fuzz extension
- [ ] H. Sweep + soak + finalization

### A. Async sema
Lift the fail-closed set for the IN-scope surface: `async fn` actor
methods, `await` (typed per 02 §9.4's CallError composition), `send`
both forms, `Actor[T]` in init-wiring positions (M4's ImageDecl typing
grows the real parameter-type story — the C-item placeholder comment in
image-basic retires), `with group(...)` + `g.start`/`g.join_all`,
`defer` (if not yet typed), cross-await access rules (02 §9.2: self-
rooted whole-value paths may span await; external-rooted may not —
err corpus). The OUT-scope list keeps its fail-closed arms with M6-
accurate messages. Opens: `actors.calls.callerror-composition`,
`actors.messages.value-restrictions` (+ goldens); the `actor` category.

### B. FlowWir
Decision 2. Typed suspension-explicit IR, stable dump, goldens for:
plain async method, await chain, group with two children, deadline
scope, defer cleanup. Opens: `compiler.lower.flowwir-stable`.

### C. Runtime core (guest side, generated)
Decision 3: static scheduler tables, mailbox rings, frame arenas,
admission/selection/park/wake, the turn driver, abandon path (decision
12). Emitted via encode.rs like the M5 harness (no asm strings). The
report grows per-actor accounting lines (facts only).

### D. Lowering/codegen
FlowWir → mwir (state-machine dispatch, frame-slot access, suspension
save/restore) → A76; checkpoint emission (decision 6); asm/mwir goldens
for the async family mirror B's.

### E. VMM + recorder
Decision 7 (pending page, deadline wake service) + decision 9 (choice-
sequence record/replay v1). Conformance unit tests with hand-built
guests where useful (M5 precedent). Flips machine.clock.trap-logged,
machine.replay.clock-log (decision 11 lands with A/D).

### F. Groups/deadlines/cancellation end-to-end
Decision 8 + the flagship goldens: boot-actors (two actors,
non-reentrancy witness — flips actors.turns.non-reentrant), boot-send
(proven bare send + consumed-Result send), boot-deadline-cancel
(decision 7's witness — flips machine.interrupts.checkpoint-injection),
boot-group-join, boot-cancel-cleanup (defer order pinned).

### G. send-proof + adversarial err corpus
Decision 5 shipped exactly; flips actors.send.statement-requires-proof.
Fuzz: the eval/lower lanes' seed corpus grows the async goldens; the
sema lane's roundtrip covers the new syntax (async fn/await/send/with
group all already parse — verify pretty/reparse holds for every new
golden).

### H. Sweep + soak + finalization
Haiku sweep over: reentrancy edges (self-calls, A→B→A cycles — handle
edges may be cyclic!), mailbox-full boundaries, deadline races (expiry
exactly at a checkpoint), cancellation during cleanup, group capacity
0/1/max, send in every illegal position. Deep soak all lanes; exit
audit; evidence block; mark complete.

## Non-goals (M7+ territory)

Everything the honest-scope line lists as OUT, plus: no partial-order
reduction or schedule exploration (the coverage pass owns it — M6 only
guarantees the enumerable representation), no cross-core anything, no
device vectors beyond the deadline service, no comptime evaluation of
async bodies (evaluator scope is unchanged), no mailbox derivation, no
actor-chatter diagnostics (02 §9.7 — stdlib-era tooling), no new report
sections beyond the static accounting facts.

# M9 plan: the stdlib

**Status: ACTIVE.**

First deliverable of the milestone, per the planning convention. The docs
carry the substance — [05-library.md](../docs/language/05-library.md) is
this milestone's chapter almost end to end, with
[02-language.md](../docs/language/02-language.md) §1.1 (literals), §2
(the fixed prelude), §7.4 (operator desugaring, `?` and `from`) and
[04-compiler.md](../docs/language/04-compiler.md) §2 (`@layout_assert`)
as its dependents — and [ROADMAP.md](../ROADMAP.md) M9:

> Lands: the `Format` contract and f-strings (dead in sema since M2),
> `Result`/`from` conversion, the collection/format/time surface of 05,
> and the `ImageReport` reflection type `@layout_assert` needs to run at
> all. Two structural consequences worth more than the features:
> `sema/prelude.rs` is **deleted**, and the doc corpus becomes
> sema-checkable. Flips: `comptime.fstring.bounds`,
> `image.report.layout-asserts`, `values.resource.protocol-consumption`.

## Where this milestone actually starts (measured, 2026-07-25)

Stated as numbers so the first item cannot begin by rediscovering them.

- `stdlib/` is **one file**: `stdlib/README.md`. There is no `core`, no
  module, no wrela source compiled from it — ever, by anything.
  **Corrected 2026-07-25 (orchestrator, before item A was dispatched):
  there *is* a loader path.** `crates/wrela-compiler/src/loader.rs` has
  resolved `from core.X import ...` into a located `stdlib/` tree since
  M4 item A, with a two-candidate lookup (a `stdlib` sibling of the
  package root, else the toolchain tree via `CARGO_MANIFEST_DIR`) and
  `["core", ...]` module addressing. Item A does not build that; it
  builds what sits on top of it. The line above is left standing with
  its correction attached rather than rewritten, because "the plan
  measured wrong" is itself evidence about how these starting-state
  blocks get written.
- **The type namespace does not consume imports at all** (measured by
  hand, 2026-07-25, at `d1292c6`). This is disclosed, not a bug —
  `sema/mod.rs`'s `check_program_typed` doc says so in as many words:
  "`types::declare`'s own type-name table stays module-local, so writing
  an imported `struct`/`enum` name as an explicit *type annotation* ...
  still fails with the ordinary `error[type]: unknown type` (a real,
  narrow, reported gap)". Measured shape, both for a plain sibling
  module and through the `core` alias:

  | Imported thing | Value position | Type position |
  | --- | --- | --- |
  | `pub const` | works | n/a |
  | `pub fn` | works (called) | n/a |
  | `pub struct` | works (`Pair(a=1, b=2)`, field read) | `error[type]: unknown type` |
  | `pub enum` | works (`Color.Red`, `match`) | `error[type]: unknown type` |

  Repro, three files, no image, `wrela dump --stage=check`:
  `stdlib/tiny.wr` = `module tiny` + `pub struct Pair: a: u32 / b: u32`;
  `src/main.wr` = `module main` + `from core.tiny import Pair` +
  `pub fn v(p: Pair) -> u32: return p.a` → `error[type]: unknown type
  \`Pair\` at 5:13`. Drop the annotation and construct it instead and the
  same program checks clean. M4 could disclose this gap because no M4–M8
  program ever needed to *name* an imported type. Every M9 item does:
  a stdlib whose types cannot appear in a signature is not a stdlib.
- `crates/wrela-compiler/src/sema/prelude.rs` is **218 lines** of
  hardcoded names in four fixed arrays (`SCALARS`, `OPTION_RESULT`,
  `LITERAL_SURFACE`, `IMAGE_BUILDER`), whose own header calls itself "not
  the stdlib" and names this milestone as its executioner.
- f-strings are `error[unimplemented]` at **three** sites in
  `sema/bodies.rs` (lines 1338, 1496, 2141 at `13a3b41`).
- `cargo xtask corpus` reports `lexed 31, parsed 24, fragments skipped 7`
  — the normative chapters are parser inputs, not typed ones.
- `values.resource.protocol-consumption` is **already `test`**. ROADMAP
  names it as an M9 flip; it was flipped earlier. M9 owes it nothing but
  a re-check that it still holds once the stdlib supplies the types it
  talks about. Recorded here so the milestone is not credited for work
  it did not do.
- The two clauses M9 genuinely flips are therefore `comptime.fstring.bounds`
  and `image.report.layout-asserts`, both `gap` today.

## Goal

The standard library exists as **wrela source in `stdlib/`**, compiled by
the same pipeline as user code, dumped at every stage, and pinned by the
same goldens. The compiler stops carrying a hardcoded stand-in for it.
The normative chapters stop being lexer inputs and start being
type-checked ones, which is what makes ground-truth rule 1 ("if code
disagrees with the docs, the code is wrong") mechanical rather than
aspirational.

## The honest scope line (decide once)

IN: a `stdlib/core` that the compiler loads as ordinary wrela source, and
the loader path that finds it; the `Format` contract, `deriving(Format)`,
and f-strings with their summed `String[..N]` bound; `from` conversion
and `deriving(From)` as `?` consumes it; the collection surface of 05 §7
(`List`, `SlotMap` and its three-field key discipline, array
`map_take`/`try_map_take`, the two iteration forms); the time surface of
05 §5 (`Duration`, `Instant`, `now()` as a recorded effect); the
`ImageReport` reflection type and `@layout_assert` actually running;
deleting `sema/prelude.rs`; making the doc corpus sema-checked.

OUT (each fails closed, named, with the owner that would take it): SIMD
(05 §8.1 — a closed vector set and a NEON lowering contract, which is a
backend milestone's worth of work and is named by neither ROADMAP M9 nor
this plan's exit criteria); `race` (05 §4 — a sealed contract with
build-proven slot reservation and full loser teardown; `group` already
exists from M6 and `race` is not a stdlib type but a compiler contract);
`SlotMap.reserve()/slot.resolve()`'s turn-splitting service idiom (05 §4
— it generates a later turn, which is M10's runtime territory); a second
device family; runtime-in-wrela (M10); the cost contract (M11).

**Deliberately undecided until item C measures it:** whether
`Validated[F, T]` / `Secret[T]` (05 §6) land here. M8's carry-forward
table recorded them as "stdlib-era (ROADMAP M9)", but ROADMAP's own M9
paragraph does not name them and `values.marked.mechanism` rejects them
today. They are ordered as item G *after* `Format`, because `Secret` is
defined by having no `Format` and cannot be specified before `Format`
exists. If item G balloons past one session it splits per the
item-balloons rule, and the residue is named in the plan rather than
half-built.

## Shape decisions (frozen by goldens — decide once)

1. **The stdlib is wrela source, not a Rust table.** `stdlib/core/*.wr`
   is compiled by the ordinary pipeline. A stdlib type that cannot be
   written in wrela is a finding about the language, to be recorded and
   escalated in this plan — never a reason to reach back for a hardcoded
   entry. The exception set is exactly the intrinsics 05 §9 already names
   as compiler-recognized (`Image` and the builder surface), which are
   recognized *by name* in sema and whose surface may be declared in
   wrela without a body.
2. **The prelude becomes a loaded module, and then stops existing.**
   `sema/prelude.rs` is deleted in one commit, at the end, after every
   name it lists has a real definition. It is not whittled down in
   fifteen commits; a half-deleted prelude is two sources of truth.
3. **The corpus flip lands behind a flag first.** Per GOAL's standing
   risk note, which this plan restates so it is not rediscovered:
   sema-checking the docs will surface a batch of real doc/compiler
   disagreements at once, and a batch that size is exactly where
   ground-truth rule 1 gets quietly inverted to make the build green. So:
   land the flip off by default, fix each disagreement in its own small
   cited commit with the docs winning by default, and only then make
   corpus sema-checking mandatory in `check`. **Never `golden --update` a
   batch that has not been read case by case.**
4. **A doc/compiler disagreement is resolved in favour of the doc**
   unless the doc is internally inconsistent, in which case the tiebreak
   is GOAL's: prefer the reading consistent with the ledger and existing
   goldens, make the minimal doc fix, same-commit clause, REVIEW-QUEUE
   line. Every such fix is its own commit. The count of them is evidence
   and goes in the closing block.
5. **f-strings are bounded at build time or they are a build error.**
   05 §6: `f"..."` sums each operand's `max_formatted_len(spec)` into
   `String[..N]`. There is no dynamic fallback and no truncation; a
   `Format` implementation that cannot state a bound cannot be used in an
   f-string. `panic`'s formatter keeps its own fixed maximum and a
   message that cannot fit stays a build error.

## Numbered decisions

Shape decisions 1–5 above are frozen. Numbered decisions taken *during*
the milestone start at 6 and are appended here as they are taken. Per
the parallel-agent lesson from M8, every agent brief states the agent's
reserved decision-number block up front; two agents independently taking
"decision 12" cost a day in M8.

Reserved: 6–7 orchestrator (item A split); 8–15 item A1/A2 (A1 took
8–12, A1b took 13–15, so A2 draws from 77+); 16–23 item
B; 24–31 item C; 32–39 item D; 40–47 item E; 48–55 item F; **56–59 item
AA** (the intrinsic-surface closure guard); **60–67 item BB** (`?` at
comptime; BB took 60–63); **68–75 item CC** (non-receiver `mut`
write-back; CC took 68–75); **76 orchestrator** (CC's property golden);
**77–85 item A2** (stdlib loaded; A2 took 77–85); **86–89 item DD**
(aliased imported methods; DD took 86–89); **90–99 item EE** (the
backend's import closure + `diff-eval`'s scope; EE took 90–99);
**100–104 item FF** (aliased types at codegen); **105–120 item B**
(`from` conversion / `deriving(From)`; B took 105–108); **121–135 item
B2** (enum methods; B2 took 121–135); **136 orchestrator** (B2's sum-receiver
property golden); **137–146 item B3** (`deriving(From)` generates a real
`from`; B3 took 137–146); **147–154 item GG** (the re-key covers a whole
signature; GG took 147–154); **155–164 item HH** (the importer's type
universe; HH took 155–164); **165–178 item II** (no reachable
`internal error:`; II took 165–172, 173–178 remain reserved for this
family); **179–190 item JJ** (enum-payload reachability; JJ took
179–186, 187–190 remain reserved for this family); **220–229 item H**
(`ImageReport` + `@layout_assert`; H took 220–224); 191+ allocated at
dispatch.

6. **A is split into A1 (imported types in type positions) and A2 (the
   stdlib is loaded).** See the starting-state measurement above.
7. **A2 moves a name with no special-casing; `Option` moves under item
   I.** See item A2's own note.
8. **The type namespace consumes imports through one raw-AST arity
   table.** `imports::declared_type_shapes`/`imported_type_shapes` build
   `local name -> generic-parameter count` from raw AST on *both* sides —
   this module's `import` statements and the exporting module's
   `struct`/`enum` headers. `types::declare_with_imports` merges it into
   its own `shapes` table and `bodies::build_module_ctx` merges it into
   `ModuleCtx::shapes`, which is the same table body annotations resolve
   against. Raw AST on both sides is the whole point: the table is
   complete before *any* module's `declare` runs, so no module's type
   table waits on another's and cycles stay free (`sema/mod.rs`'s splice
   note is preserved, not rewritten). Rejected: threading the exporting
   module's finished `DeclStruct` in, which would have ordered the
   modules and broken cycles outright.
9. **An imported type resolves to `Type::Named(<local name>)`** — the
   possibly-aliased spelling the author wrote, not a qualified path. It
   is the key the splice already installs in `ModuleCtx::structs`/`enums`,
   so there is one name table and one spelling, and the `check` dump keeps
   printing what the source says. Rejected: a qualified `mod.Type`
   spelling, which would have needed a second lookup path in every
   consumer of `Type::Named`.
10. **Data-vs-resource classification becomes a whole-closure pass.**
    A local struct with a field of an imported `resource`/`@actor` type is
    a resource, which no module-local pass can compute — and answering
    `Data` for a name it cannot see is a silent wrong answer, not a
    fail-closed one. `types::classify_closure` therefore recomputes every
    module's classification after the declare loop and before the splice,
    reusing the one existing algorithm with its memo/`in_progress` keyed
    `(module, name)`. Cycles are unaffected for the same reason the splice
    leaves them alone: this reads output that already exists. A by-value
    cycle closing across modules gets the same `is infinitely sized`
    diagnostic it already gets within one.
11. **The arity table is built over *specialized* modules, everywhere.**
    A `struct`/`enum` declared inside a module-level `comptime if` only
    exists once `specialize` has run, so a raw-AST table would list fewer
    type names than sema's does. Sema already had the specialized modules
    to hand; the post-sema backend re-derivations (`layout.rs`,
    `mwir.rs`) re-run `specialize` themselves — this file's own
    recompute-rather-than-thread convention — rather than being handed a
    thinner table. Found by probing: the raw-AST version accepted an
    imported `comptime if`-declared type at `--stage=check` and would
    have rejected the same program one layer down as a `LayoutError`.
    Two passes disagreeing about what type names exist is the failure
    this decision exists to prevent.
12. **`Actor[T]` and capability type arguments stay module-local.**
    `types::validate_actor_handles`/`validate_capability_types` still
    require `T` to name a struct declared in the same module; an imported
    `@actor` struct there is rejected, loudly, by the existing named
    diagnostic. Cross-module actor handles are a milestone question
    (02 §9.1 + M6's own scope), not something A1 opens as a side effect of
    a type-name table.
13. **The comptime evaluator gets one read-only imported-declaration
    window, keyed by local name.** `TypedProgram::imported`
    (`typed::ImportedDecls`) carries the `const`/`fn`/`struct`/`enum` a
    module imports, under the possibly-aliased spelling the typed tree
    already uses (decision 9), plus the exporter's instantiations.
    `sema::mod::splice_imported_decls` fills it *after* every module's
    own `bodies::check`/`generics::check` — read-only reuse of finished
    output, so no module's evaluation waits on another's and cycles stay
    free. Rejected: merging into `TypedProgram::consts`/`fns`/`structs`/
    `enums` directly, which `lower`/`flowwir_lower`/`layout`/`codegen`
    all iterate to emit each declaration exactly once — that would emit
    an imported fn once per importer.
14. **`eval::check_comptime` and the two `eval::legal` whole-program
    checks move to their own loop, after the splice.** They cannot run
    inside the per-module typing loop any more, because the splice needs
    every module's `TypedProgram` complete. The visible consequence is
    diagnostic *ordering* in a closure with errors in more than one
    module: the first diagnostic is now the earliest in pass order, then
    module order — which is what the single-module pipeline this mirrors
    already does. No existing golden moved.
15. **A1b makes the declarations a module *names* evaluable, and says
    so out loud for everything else.** The evaluator walks one
    `TypedProgram`'s flat name tables and has no notion of which module
    a body came from, so an imported body is evaluated against the
    *importing* module's tables. Two shapes follow. (a) A name only the
    exporting module has is absent: precomputed into
    `ImportedDecls::unresolvable` and reported by name
    (`err-import-comptime-private-decl`), never `internal error:`.
    (b) A name the exporter has *and the importer resolves differently*
    would silently resolve to the importer's — a wrong value, not a
    missing one — so a body-bearing splice from a shadowing module is
    **withheld**, turning it back into a named miss
    (`err-import-comptime-shadowed-decl`). Shadowing is tested by
    *value*, not by declaring module: `bodies::check` injects the six
    fixed prelude enums into every module, so every pair of modules in
    every build "declares" all six. Rejected: making the evaluator
    module-aware (evaluating each body against its own module's
    program), which is the real fix and changes every `eval::` entry
    point's signature — owned by A2, which is where the stdlib starts
    calling into itself. Also left standing: `eval::legal::classify`
    stays module-local, so M4 item B's pinned refusal of a cross-module
    callee *from an `@image` fn* is unchanged.
56. **The intrinsic surface is 44 keys, and all four `format!` sites are
    statically knowable.** The fear in item AA's own statement — that
    membership might depend on variables scattered through a
    10,000-line file — did not survive reading. Each of the four
    templated keys is bounded by a guard within a few lines of its
    construction site: `Image.{mname}` by `check_call_index`'s
    `mname == "device" || "pool" || "dma_pool"`; `Mmio.{op}` by
    `check_mmio_access`'s `if !matches!(op, "read" | "write")` early
    return; `InterruptCell.{method}` and `Image.{name}` by the match
    arms their construction sites sit inside. Census below.
57. **The guard is a cargo unit test over compiler *source*, not a
    golden.** There is no artifact to dump — the live set exists only
    while a program is being checked — so the honest oracle reads
    `sema/bodies.rs` as text (`include_str!`, so a missing file is a
    build error rather than an empty scan that passes). Rejected: a
    second hand-maintained table inside `bodies.rs` for the test to
    compare against, which is two sources of truth and exactly the
    silently-passing guard AA exists to avoid. The written-down list
    lives in `crates/wrela-compiler/src/sema/intrinsics.rs`, one place,
    justifications attached.
58. **Hand-resolved `format!` expansions are backed by locking the
    guard's source text.** Resolving a template's range by hand is a
    census act, not a mechanical one, so the closure test alone would
    not notice a *widened* guard (a fifth `InterruptCell` method added
    to its match arm produces a new key from an unchanged construction
    site). `FORMAT_KEY_SITES` therefore carries each bounding guard's
    exact source line and asserts it is present verbatim exactly once.
    Brittle to reformatting those four lines, deliberately: the
    failure is loud and says re-resolve the range.
59a. **The census keys on the weak marker and must classify every
    occurrence** (taken 2026-07-25 after the orchestrator's independent
    verification; numbered 59a rather than 60 to stay inside AA's
    reserved block — 60+ is still unallocated). Decision 57's scan
    matched the whole line `kind: TypedExprKind::Intrinsic {`, and the
    orchestrator evaded it three ways, each passing all six tests: a
    bare construction with no `kind:` prefix; a `kind:`-prefixed one
    with a path-qualified type; and — the serious one — a bare
    construction **inside `bodies.rs` itself**, a false negative in the
    primary closure test. Same class as the field-reorder hole AA had
    already closed, so it gets closed the same way.

    The scan now keys on `TypedExprKind::Intrinsic {` with any prefix
    and any qualification, takes each occurrence's brace-balanced field
    region, and classifies it: a `key:` bound to a string literal or a
    `format!` is a census entry; **no** `key:` is an ordinary pattern
    match and is ignored; a `key:` bound to anything else **panics**.
    That last arm is new surface coverage, not just a plugged hole — a
    key computed from a variable was previously invisible and is now a
    named failure.

    Rejected: the orchestrator's suggested whole-crate occurrence count
    (`occurrences == constructions + allowlisted non-constructions`).
    It closes the same three holes, but every pattern match on this node
    is a locked number, so any ordinary downstream work — a new `match`
    arm in `lower.rs`, exactly what item E and item H will write — bumps
    a count. A ratchet whose numbers get bumped routinely stops being
    read, and this file's whole purpose is being read. Classifying by
    structure costs ~60 more lines and churns only when someone writes a
    genuinely ambiguous site.
59. **The ledger may name a unit test as `unit:<fn name>`.** `xtask
    ledger` accepted only `tests/` paths and `xtask:<command>`, so a
    clause backed by a cargo test had no way to be marked `test`
    honestly. Added and verified mechanically (the function must exist
    under `crates/`), rather than marking the clause `gap` while a real
    guard runs on every `check`. Rejected: relocating the guard into
    `tests/` purely to satisfy the validator, which would move the
    census list away from the code it locks.
59b. **`unit:` requires the `#[test]` attribute, not merely a function
    of that name** (taken 2026-07-25, orchestrator's second review; same
    numbering reasoning as 59a). Decision 59's validator checked for
    `fn <name>(` anywhere under `crates/`, so `unit:main` — this
    workspace's own `xtask` entry point — satisfied a clause and counted
    it among the tested ones. The sibling reference type does not allow
    that: a `golden/foo` ref must exist as a real directory under
    `tests/`. A reference type satisfiable by a non-test lets a clause
    claim coverage it does not have, and the ledger is ground-truth
    rule 2 — the same silent-acceptance class item AA exists to prevent,
    one layer up, and it would have been inherited by every future
    `unit:` clause. The check now requires a literal `#[test]` followed,
    past whitespace only, by `fn <name>(`. Deliberately not an attribute
    parser: a test carrying a second attribute between the two
    (`#[should_panic]`) reports as missing, which is a false negative
    and the safe direction. Found by review, not by the author — worth
    recording because a new mechanism's *own* first use is exactly where
    nobody looks for the weakness.
59c. **A commented-out `#[test]` does not satisfy a `unit:` ref** (taken
    2026-07-25, orchestrator, third review — fixed in the orchestrator's
    own hands rather than a fourth round trip, since it is one
    condition). 59b's check is textual, and a textual scan is happy to
    find `#[test]` inside `// #[test]`. Measured: a `// #[test]`
    directly above `fn orch_fake2()` satisfied the clause and
    `cargo xtask ledger` reported `141 clause(s), 132 tested` clean.
    This is the *realistic* version of the 59b hole rather than the
    contrived one — commenting out an attribute is the ordinary way to
    disable a flaky test, and doing so would have silently left every
    clause citing it counted as covered. The check now walks back to
    the line start and rejects a `//` before the attribute. A
    `/* #[test] */` block comment was already rejected, but for an
    unrelated reason (the `*/` breaks the whitespace-only run) —
    verified rather than assumed. Residue unchanged from 59b.

    The pattern across 59, 59b and 59c is worth stating once: each
    check was verified against the failure its author had in mind (a
    typo, then a wrong-kind name, then a missing attribute) and fell to
    the one they had not. That is an argument for adversarial review of
    new oracles specifically, not for distrusting this one.
60. **`?`'s success arm is decided by the operand's static type, never
    by the variant index.** `eval::value` numbers both builtin sums from
    zero, so an index carries no information about *which* sum it came
    from: index 0 is `None` in one and `Ok` in the other, index 1 is
    `Some` in one and `Err` in the other. `eval::interp`'s `Try` arm now
    reads `inner.ty` — `sema::bodies::check_try` has already proved it
    is exactly `Option[T]` or `Result[T, E]` *and* that the enclosing fn
    returns the same sum — and anything else there is a named internal
    error rather than a guess. The two `payload.pop().expect(...)` calls
    became fail-closed `abandon`s in the same edit; a panic is not a
    diagnostic. Rejected: A1b's own suggestion of branching on
    `expr.ty`, evaluated rather than adopted — `expr.ty` is the
    *success* type (`u32` for a `Result[u32, u32]`) and cannot
    distinguish the two sums at all. The suggestion was right about
    where to look and wrong about which field.
61. **A missing `from` conversion is a named gap, not an `internal
    error:`.** Nothing generates a `from` method for `deriving(From)`
    (item B owns that), and `from` is a reserved word, so no user can
    declare one either — which means *every* `?` needing a conversion
    reaches the evaluator with no body to call, from ordinary source.
    It said `internal error: `from` conversion `WrapError.from` not
    found`; by house rule an `internal error:` is a bug report, not a
    diagnostic. It now fails closed by name and cites item B, matching
    what the backend has always said for the same shape. Pinned as
    `err-try-from-underived`, which stops being an `err-` case when B
    lands — the intended signal. Rejected: rejecting the shape in sema
    instead, which would have re-pinned `check-bodies` (its
    `convert_wrapped` is a legal program under 02 §7.5, merely one this
    compiler cannot finish yet) and taken a decision that belongs to B.
62. **The eval fuzz lane treats an `"internal error: "` message as a
    bug, like every other lane.** CLAUDE.md describes all lanes as
    checking this and the `lower`/`async` lanes have since M7 — the
    latter's own doc comment names `eval/interp.rs`'s ~50 such guards as
    the class it exists to falsify — but the lane that actually
    *evaluates* comptime code never did: a `check_typed` `Err` passed as
    long as its category was in the fixed set, and an `internal error:`
    inside a `@test`'s `FAILED` verdict passed as long as the line was
    well-formed. Decision 61's diagnostic is exactly what it would have
    shrugged at. `eval_outcome_carries_no_internal_error` checks all
    three surfaces an abandonment can reach, reusing the identical
    `starts_with` rule rather than growing a second mechanism. Rejected:
    building a new lane or a new generator for `?`-shaped programs —
    item BB's four `check-try-*` goldens are single-file `input.wr`
    cases and are therefore fuzz seeds from the commit they land in,
    which is the honest fix for a seeding gap (see the item's own "why
    fuzz missed it").
63. **The backend was already right, and the divergence is recorded
    against `compiler.eval.matches-backend` rather than only against the
    new clause.** Both lowering paths destructure the operand's
    `Type::Result` before comparing the tag, and reject `Option`
    outright by name — so `?` on `Result` has always lowered correctly
    and `?` on `Option` has never lowered at all. `xtask diff-eval`
    exists for exactly this class of divergence and reported full
    agreement every run, because no golden carried a comptime-legal
    `@test` whose evaluation crossed a *failing* `?`: an oracle over a
    corpus is exactly as strong as the corpus, and "N tests agree" is
    evidence about the N. That sentence is now in the clause's own note,
    where the next reader of `diff-eval`'s output will meet it.
68. **`run_call` hands back every non-receiver `mut` parameter's final
    value; write-back is the call site's job.** `CallOutcome` carries
    `mut_params: Vec<(usize, Value)>` (param index, final value) alongside
    `final_self`. `eval_call` writes each into the matching call-site
    place through the existing `place_mut`. Rejected: mutating the
    caller's place in place during the call (aliasing the callee frame
    into the caller env), which would make the interpreter's frame model
    disagree with the backend's copy-in/copy-out ABI and bury the
    mirroring rule inside `bind_params`.
69. **A defaulted `mut` argument fails closed by name.** Eliding a `mut`
    parameter that has a default leaves no call-site place to mirror
    into; silently dropping the write-back is the defect this item
    exists to close, and inventing a temporary place would make the
    mutation unobservable — both wrong. Diagnostic:
    `writing back \`mut\` parameter \`<name>\` through a defaulted
    argument is not supported`. Rejected: rejecting defaulted `mut`
    parameters in sema, which would be a language change 02 §5.1 does
    not require and would break any source that already writes one.
70. **`CallValue` (fn values and closures) writes `mut` params back
    too.** Pre-evaluating every argument into a `Value` before the call
    loses the place; the silent-drop defect would survive on the one
    path 02 §5.1's own examples use for `fn(mut Item)` callbacks. The
    closure / fn-value arms now take from `place_mut` for each `mut`
    parameter and write the final value back after the body returns.
    Rejected: failing closed on all `CallValue`+`mut` shapes, which
    would turn `check-x-closure-mut-receiver`'s own call pattern into a
    comptime error the moment anything evaluated it.
71. **Operator methods and `?`'s `from` conversion refuse a non-empty
    `mut_params` list rather than drop it.** Those node shapes have no
    call-site place list (`OpCall` evaluates both operands to values;
    `from` is `take source`). A `mut` non-receiver there would mean the
    callee's signature disagreed with 02 §7.4; refusing by name keeps
    the "never silently skip" rule. Rejected: ignoring `mut_params` on
    those paths, which reintroduces a silent drop on a shape that
    should be unreachable.
72. **`Inst::Call::self_write_back` becomes `write_backs:
    Vec<(usize, Temp)>`.** One list, args-indexed, covering a `Mut`
    receiver (index 0) and every non-receiver `mut` parameter. Dump
    spelling: `write_backs=[0:t4,2:t7]`. Rejected: keeping
    `self_write_back` and adding a parallel `arg_write_backs` field —
    two fields for one rule, and the dump would have two spellings for
    the same fact.
73. **Lowering a `mut` argument through a nested field/index place fails
    closed by name.** Only a bare local is addressable today — matching
    `mut self`'s own restriction. The evaluator already writes Field/
    Index places back (CC1); the backend residual is
    `passing a \`mut\` argument through a nested field/index place is
    not implemented yet`. Rejected: projecting to a scratch temp and
    writing that back, which would leave the real place untouched and
    reintroduce the silent-drop defect under a different spelling.
74. **`mut` parameters — including scalars — travel by pointer.** The
    existing aggregate bare-pointer ABI extends to every args-index in
    `write_backs`. The callee prologue copies in and saves the pointer;
    the epilogue copies out. Rejected: returning mut finals as extra
    results (a second ABI path, and nothing for the existing `mut self`
    epilogue to share) or call-site post-`BL` copies of scalars (the
    callee's mutated value is gone once its frame is torn down).
75. **`MwirFn`/`FlowWirFn` params carry `AccessMode`.** The dump prints
    `:mut`/`:take` when the mode is not `read` (`params=[t0:mut,t1]`).
    Codegen's prologue/epilogue read the mode directly rather than
    re-deriving it from `write_backs` on every call site. Rejected: a
    parallel `param_modes: Vec<AccessMode>` field that could disagree
    with `params` about length.
76. **A merge review that finds no defect lands its probe as a golden**
    (orchestrator, 2026-07-25, verifying item CC). The ten shapes written
    to attack CC's write-back mechanism all passed, and all ten agreed
    across both tiers under `diff-eval`. The tempting conclusion is that
    they were therefore not worth keeping. The opposite is true: they are
    a two-tier lock on properties nothing else in the corpus states —
    that a callee which never assigns its `mut` parameter does not
    clobber it, that an early `return` before the assignment does not
    publish it, that a `defer` after it is included, that two `mut`
    parameters do not cross-write, and that an unassigned sibling field
    survives. Those are exactly the properties a later optimization of
    the write-back path would break, and CC's own cases would not catch
    any of them. So `check-mut-param-properties` lands, and the standing
    rule is: **a review probe that survives is coverage, not a receipt.**
    Rejected: recording the probe's ten shapes in this file as prose
    only, which is a description of a test rather than a test, and which
    the next person to touch `write_backs` would never run.
77. **A2 moves `IoError`, not a `LITERAL_SURFACE` name and not
    `Option`.** Measured at `db5703f`: `LITERAL_SURFACE =
    ["Static","Str","Bytes"]` are all resolved to builtin compiler types
    in `types.rs` (`Type::Str` / `Type::Static` / `resolve_bytes`) and
    cannot be declared in wrela source at all — the plan's own
    contingency. Of the six `builtin_enum_variants` enums, `IoError` is
    the smallest genuinely-movable one: one unit variant, ordinary wrela
    data, used in ~7 goldens as a value construction (never as a type
    annotation in source), and not load-bearing for `?`/`take`/
    `InterruptCell`. Rejected: `BootError` (~50 annotation sites),
    `CompletionOutcome` (Unknown-arm special-casing + virtqueue tag
    order), `DriverMode`/`Target`/`Restart` (image-builder /
    const-generic entanglement), and anything in `OPTION_RESULT`
    (decision 7 / item I).
78. **`stdlib/core/` is the `core` package root.** `from core.X import
    ...` maps onto `stdlib/core/X.wr` (sibling `pkgroot.parent()/
    stdlib/core` first, else `CARGO_MANIFEST_DIR/../../stdlib/core`).
    Rejected: keeping files at `stdlib/X.wr` (shape decision 1 says
    `stdlib/core/*.wr`) or requiring `from core.core.X` (a double
    prefix the docs never spell).
79. **Stdlib loading is explicit — only when imported.** 02-language.md
    §2: "A fixed prelude is always in scope: `Option`, `Some`, `None`,
    `Result`, `Ok`, `Err`, `panic`. ... Everything else is imported."
    `IoError` is not in that list, so moving it requires an import.
    Rejected: always-loading `stdlib/core/*.wr` into every build and
    auto-injecting their pub names (would preserve today's always-in-
    scope surface for `IoError` but contradict the doc sentence, and
    would put `Module path=io_error` into every check dump).
80. **A sibling `stdlib/` without `core/` is `stdlib not found`, not a
    missing-module diagnostic.** Sibling `stdlib/` shadows the
    toolchain fallback; if it has no `core/` subdirectory the loader
    fails closed by name (`error[build]: stdlib not found: ...`).
    Rejected: falling through to `CARGO_MANIFEST_DIR` (would make
    `err-stdlib-missing` unreachable) or an environment variable (the
    loader's own "no env, no search path" rule).
81. **`--stage=typed`/`mwir`/`flowwir`/`asm` and `wrela test` share
    `check_closure` with `check`/`image`/`report`.** Without this, any
    golden that imports `IoError` and pins typed/mwir/test would hit
    the single-module entry's fail-closed. Rejected: leaving those
    stages single-module-only and restructuring every IoError consumer
    to avoid typed/mwir (check-receipt-handoff pins both).
82. **`lower::variant_index` / `flowwir_lower::variant_index` read
    `prog.imported.enums`.** Same rule A1b already applied to
    `eval::interp`. Without it, an imported `IoError.OutOfRange`
    failed closed as a "generic enum instantiation" — the wrong cause.
    Rejected: copying imported enums into `prog.enums` at splice time
    (two maps that could disagree about the same name).
83. **`golden/err-import-typed-stage` is retired.** The CLI surface it
    pinned no longer exists; `check_typed`'s API-level fail-closed
    remains and is pinned by `unit:check_typed_rejects_imports`.
    Rejected: keeping the golden as a path-disagreement case (already
    covered by `err-import-path-disagreement`) or forcing the CLI back
    onto the single-module entry for import-bearing files.
84. **Flat `input.wr` goldens that gain a `core` import become project-
    shaped.** Adding an import forces `load_closure`, which enforces
    path agreement — `input.wr` declaring `module examples.foo` fails.
    The seven IoError consumers move to `root` + `src/examples/<name>.wr`.
    Rejected: renaming modules to `module input` (would move every dump
    line that cites the module path).
85. **`mwir::size_of` keeps a hardcoded unit-tag arm for `IoError`.**
    `IoCompletion.status` synthesizes `Type::Named("IoError")` in
    programs that never import it; sizing must not depend on the decl
    being in the closure. Rejected: always-loading the stdlib just to
    size a compiler-synthesized field (decision 79).
86. **An aliased import's declaration is re-keyed to the local spelling
    at both splices.** When `local != target_name`, the ModuleCtx splice
    runs `types::rekey_decl_struct_name` (and rewrites `DeclEnum::name`)
    on the cloned declaration, and the TypedProgram splice runs
    `typed::rekey_struct_name` on the cloned `TypedStruct`. Decision 9
    already said the local name is the one key; the splices installed
    under that key but left the body's own `Type::Named` / `decl.name`
    carrying the exporter's spelling. Rejected: a fallback at method
    lookup that tries the alias and then the original (two sources of
    truth about which spelling is canonical — the bug, not the fix).
87. **Construction synthesizes `Type::Named` from the local lookup key,
    not `decl.name`.** `check_struct_construction` takes the name the
    caller looked up under; enum construct uses `bname` the same way.
    After decision 86, `decl.name` equals that key for an aliased
    import, but the caller's key is the authoritative spelling (it is
    what the source wrote). Rejected: leaving construction on
    `decl.name` and relying solely on the ModuleCtx rewrite — a future
    path that clones a `DeclStruct` without re-keying would silently
    regress the diagnostic to the exporter's name.
88. **A genuinely absent method names the author's spelling.** The
    diagnostic already interpolates `Type::Named`'s own name; once that
    name is local (86–87), `type \`Duo\` has no method \`nope\`` falls
    out. No separate diagnostic path. Rejected: a special-case that
    rewrites the message string after the fact.
89. **DD extends `sema.names.imported-types` rather than opening a new
    clause.** The rule is still decision 9's one local spelling; method
    lookup / construction / pattern construction were unfinished
    consumers of that rule, not a distinct normative claim. Rejected:
    a new `sema.names.import-aliases` clause that would duplicate
    decision 9 and invite the next consumer to pick the wrong clause.
90. **`lower`/`flowwir_lower` look up *and emit* through
    `TypedProgram::imported`, under the local spelling.** Lookups
    (`resolve_fn`/`resolve_struct`/`field_index` and the flowwir
    mirrors) read \"own, else imported\", exactly as `eval::interp`
    does after A1b. Emission still does not merge into
    `TypedProgram::fns`/`structs` (decision 13 rejected that — those
    tables are \"what this module declares\", and `try_layout_program`
    lowers every module, so merging would emit an imported fn once per
    importer). Instead `lower_program` also walks `imported.fns`/
    `structs`/`instantiations` and emits under the local Call-key
    spelling, so a single-module dump of the root (and a root-only
    test-image build) carries `Pair.sum` / `twice` / `Duo.sum` without
    inventing a whole-closure merge at every dump site. An unaliased
    name that also exists in the exporter's own lower may collide at
    `merge_mwir_programs` last-wins — already disclosed there.
    Rejected: merging imports into the declaration tables (decision 13's
    rejected alternative, restated); rejected: dump-time merge of every
    module's MWIR as the *only* fix (would leave root-only test-image
    builders broken and would not give aliased Call keys a body under
    the local spelling).
91. **A miss names the import closure, never \"an unresolved generic
    instantiation\".** `missing_callee` / `missing_struct` prefer
    `imported.unresolvable`'s own note (decision 15), else say \"not
    declared in this module and not present in its import closure\".
    The generic-instantiation wording remains only for a genuine
    `FnInstance`/`MethodInstance` miss. Rejected: keeping the old
    message and fixing only the three measured shapes (the lie is what
    sent two prior agents to the wrong file).
92. **`diff-eval` loads the import closure the way `bin/wrela.rs` does.**
    `typecheck_for_diff_eval` mirrors `check_closure` /
    `produce_report_and_image`: no imports → `check_typed`; any import
    → `load_closure` + `check_program_typed`. Deliberately a parallel
    copy, not a shared library surface — the same disclosed convention
    those two call sites already document. Rejected: the minimum-only
    fix (count and name the silent skips without handling them) —
    from A2 the entire stdlib is multi-module, so an oracle that cannot
    see it is not an oracle for this milestone. Rejected: inventing a
    third sharing convention.
93. **`build_runtime_test_image` takes the whole closure's modules.**
    `layout::merge_layout_ctx` needs every module's AST so an imported
    struct's fields size; lowering the root alone is enough for the
    code (decision 90). Rejected: enriching layout from
    `imported.structs` inside lower (wrong layer — layout is a separate
    pass with its own ctx).
94. **The summary line gains `import-skips`.** A residual that
    genuinely cannot build is counted and named; zero today for the
    preferred path's reach. Rejected: folding import failures into
    `lowering-skips` (a materially different reason — the case never
    reached lower because the oracle refused to load it).
95. **Aliased imported types still fail closed at codegen, and that is
    recorded rather than papered over.** `merge_layout_ctx` keys
    layouts by the exporter's AST name (`Counter`/`Color`); the typed
    tree and MWIR use the local alias (`Cnt`/`Hue`). Newly-included
    `check-import-alias-method`/`check-import-alias-pattern` therefore
    print `codegen for unknown struct/enum \`Cnt\`/\`Hue\` in this
    layout context` and count as lowering-skips — the valuable outcome
    of widening the oracle, not a divergence (evaluator and guest never
    disagree; the guest image never builds). Rejected: narrowing
    `diff-eval` back to single-module to hide them; rejected: fixing
    layout-under-alias in the same item without a dedicated golden for
    the layout enrich (items B–I will hit this the moment a stdlib type
    is imported under an alias into guest code).
96. **EE's golden is one case covering all three measured shapes.**
    `check-import-lower`: fields + method + plain fn, `@test` each,
    settle `71142`, mwir + asm + image dumps. Rejected: three separate
    goldens (the hole was one missing window, not three bugs).
97. **Ledger: a new `compiler.lower.import-closure` plus a note on
    `compiler.eval.matches-backend`.** The backend's import closure is a
    distinct normative claim from the evaluator's (A1b's clause); the
    oracle's silent-scope defect is the sharper version of BB's \"an
    oracle over a corpus is exactly as strong as the corpus\" and
    belongs on the same clause. Rejected: a third clause for
    `diff-eval`'s import accounting alone.
98. **Fuzz lanes stay single-file.** A1b recorded that no lane can
    construct a multi-module closure; EE does not widen them. The
    post-roadmap coverage pass owns lanes. Rejected: teaching the
    lower lane to emit a synthetic two-file closure (out of scope, and
    would not have found the layout-under-alias hole either — that
    needs a real alias).
99. **Cycles stay free by construction.** The imported window is
    read-only reuse of finished output (decision 13); lower never waits
    on another module's lowering. Rejected: ordering modules so the
    exporter lowers before every importer (would break
    `check-import-comptime-cycle`'s shape at the backend layer).
100. **`merge_layout_ctx` installs each aliased import under the local
    spelling, once, after the own-decl merge.** Own decls are keyed by
    the exporter's AST name; the typed tree / MWIR / codegen look up the
    author's spelling (decision 9). For every `from M import T as A`
    with `A != T`, copy the exporter's layout entry to key `A` and
    re-key self-`Type::Named` inside field/payload types
    (`types::rekey_type_name`). Unaliased imports need nothing — the
    exporter module's own build already contributed under `T`. Rejected:
    a lookup-time `get(local).or_else(|| get(exporter))` at
    `size_of`/codegen (exactly what let this bug reappear one layer
    down, three times — two sources of truth about which spelling is
    canonical *is* the bug).
101. **Only act when `local != target_name`.** An unaliased import's
    local spelling is already a LayoutCtx key from the exporter's own
    module. Re-inserting under the same key is noise and would invite
    a future reader to think both spellings are being "resolved".
    Rejected: always install under local even when equal.
102. **Ledger extends `sema.names.imported-types` (plus a note on
    `compiler.lower.import-closure`).** The rule is still decision 9's
    one local spelling; layout/codegen were the unfinished consumer EE
    decision 95 left named. Rejected: a new
    `compiler.layout.import-aliases` clause that would duplicate
    decision 9 and invite the next consumer to pick the wrong clause
    (same rejection DD made at decision 89).
103. **No `err-*` golden: there is no genuine residue.** The measured
    miss fails closed naming the layout context; once the install
    lands, the same programs lower and agree. Rejected: inventing an
    err case to look thorough.
104. **Fourth-layer audit is part of the item, not a follow-up.** Check
    report renderer, `verify_section_sizes`, placement/`mwir::size_of`,
    boot harness, and VMM `merge_layout_ctx` call sites for any further
    exporter-spelling key below codegen. Finding "none" is a result
    when the audit was really performed. Rejected: declaring the
    sequence closed without looking ahead (how three layers were found
    one at a time).
105. **`from` after `fn` is a legal function name.** 02 §7.4 spells the
    conversion as `TargetError.from(take e)`, and `from` is reserved for
    `from path import Name` (§2). The parser already distinguishes the
    two positions: `parse_imports` only accepts `from` as the first
    token of a statement (or `pub` then `from`); `parse_fn_item` reads
    the name only after `fn` has been consumed. Those never overlap, so
    `expect_fn_name` accepts the keyword `from` and keeps rejecting
    every other keyword as a name. Rejected: renaming the method in the
    docs (the table and 05 §8 are explicit); rejected: making `from` a
    soft keyword everywhere (wider than the conflict requires);
    rejected: inventing a different spelling like `convert_from`
    (docs win).
106. **`deriving(From)` is a structural wrap, not a synthesized
    TypedFn.** Enums carry no methods (02 §7.2 / `TypedProgram::enums`
    is a variant-name list); inventing an `enum_assoc_fns` map just so
    `resolve_fn` can find a one-line body would be architecture for its
    own sake. Check already validates the single-field/single-variant
    shape and hangs `CalleeKey::Method(name, "from")` on the `Try`
    node; when `resolve_fn` misses, both tiers wrap (`Value::Enum(0,
    [e])` / `Value::Struct([e])`, or `MakeEnum`/`MakeAggregate`). An
    explicit user `from` still resolves and is called. Rejected:
    synthesizing TypedFn bodies and teaching every resolve site about
    enum methods; rejected: changing `Try`'s typed shape.
107. **`err-try-from-underived` is retired, not kept as an err case.**
    BB decision 61 said explicitly that when B lands the case stops
    being an `err-` case — that is the intended signal. Its shape moves
    into `check-deriving-from` as a value-bearing success. Rejected:
    keeping the err golden "for history"; rejected: converting it in
    place under the same `err-` name.
108. **`check-bodies` does not gain an `@test`.** It is the typed/check
    dump surface for a large body-typing corpus; `convert_wrapped` is
    already typed there. The evaluated pin for the same conversion
    shape is `check-deriving-from`. Adding `@test`/`@image` would churn
    a large `typed.txt` without adding a distinct oracle. Rejected:
    forcing every dump golden to also evaluate.
121. **An enum body is variants plus `fn` members; the distinction is
    lexical.** A variant name is an Ident; a method begins with the
    Keyword `fn`/`pub`/`async`. Reading the parser (`parse_variant` →
    `expect_ident`) established the pre-B2 miss: Keywords were reported
    as `keyword \`pub\`/\`fn\` cannot be used as a name`. Rejected:
    requiring all methods after all variants (unnecessary; interleaving
    is unambiguous); rejected: reusing `parse_member` wholesale (it
    accepts fields/`init`/`pool`).
122. **All three receiver modes and associated fns, unchanged from
    structs.** 02 §5.1 applies to every method; there is no enum-only
    subset. `mut self` may reassign the whole sum (`self = OtherVariant`
    — the same whole-self assignment structs already allow). Rejected:
    failing closed on `mut self`/`take self` "because a sum is special"
    (docs do not say that; inventing a restriction is the wrong kind of
    dumb).
123. **Enum methods may be `pub`; the type's own `pub` gates import.**
    Same as structs: importing a non-`pub` type is
    `err-import-type-not-pub`; once the type is imported, its methods
    are reachable the way struct methods already are. Rejected: a
    separate per-method import rule (not in the docs; not what structs
    do).
124. **`TypedProgram::enums` becomes `BTreeMap<String, TypedEnum>`.**
    Variant names plus `methods`/`assoc_fns` — one table, one spelling,
    the same shape as `TypedStruct`'s method maps. Rejected: a parallel
    `enum_methods` map beside the old `Vec<String>` (two tables for one
    fact); rejected: stuffing enum methods into `structs` (lies).
125. **`EnumInfo` parallels `StructInfo` in `ModuleCtx`.** Decl + AST
    members, zip for method lookup. `Deref` to `DeclEnum` keeps the
    many field-access sites honest without a mechanical rewrite.
    Rejected: leaving `mctx.enums` as bare `DeclEnum` and re-walking
    the module AST at every lookup.
126. **Unsupported enum-body shapes fail closed by name.** `init`,
    `pool`, and field-shaped `name: Type` each get their own diagnostic
    (`err-enum-init`/`err-enum-pool-member`/`err-enum-field-member`).
    Rejected: letting them fall through to the old keyword-as-name
    message; rejected: one generic "enum body only allows variants and
    fns" that does not name the construct.
127. **Variant/method name collision is a type error.** A method may
    not share a name with a variant (or another method) on the same
    enum — otherwise `Type.name` is ambiguous between construction and
    associated-fn call. Rejected: first-wins; rejected: call-shape
    disambiguation (a fieldless variant and a zero-arg assoc fn both
    use `Type.name()`).
128. **Five-consumer audit is part of the item, not a follow-up.** Sema
    resolution, both `imported` splices, local-spelling re-key,
    `lower`/`flowwir_lower`, `layout`/`codegen` — each verified run-
    and-read. The aliased-spelling bug's lesson (DD/EE/FF) is that
    landing parser+sema and leaving one consumer behind is how this
    class of bug survives. Rejected: "backend inherits for free"
    without checking.
129. **Import splice treats enums as body-bearing.** Before B2 an enum
    was a variant-name list that always crossed the typed splice; with
    methods it participates in the same shadowing/withheld rule as
    structs and is re-keyed under the local alias. Rejected: keeping
    the always-cross path (would leave aliased method bodies keyed on
    the exporter's spelling — FF's bug, one layer up).
130. **Associated fn is looked up before variant on `Type.name`.** So
    a missing/wrong name reports `no variant or associated function`,
    never "no variant" for a method the author meant. Rejected:
    variant-first (B3's measured diagnostic: `enum Outer has no variant
    \`from\``).
131. **`--stage=check` dumps enum methods.** Same surface structs
    already use; without it the check golden would pin acceptance of
    the declaration header and not the method surface. Rejected:
    leaving methods only in `mwir`/`typed` (check is the first review
    surface).
132. **B3 is not landed here.** Explicit `from` on an enum is now
    spellable and callable; `deriving(From)` still wraps structurally
    (decision 106). Generating the real method is B3's one variable.
    Rejected: folding B3 in because "it falls out" (reviewability).
133. **No traits, `impl` blocks, or dispatch.** 01 §2 forbids runtime
    dispatch/`dyn`; method resolution stays the static name-keyed
    lookup structs already use. Rejected: any contract abstraction.
134. **Generic enum methods stay at the existing generic-method
    boundary.** Same `unimplemented_at("generic instantiation is")`
    structs already hit for a method with its own `[...]`. Rejected:
    inventing a separate enum-generic story in this item.
135. **Positive goldens pin evaluated values, not mere acceptance.**
    Settles `217` / `12` / `1222` — each unreachable if a receiver mode
    is a no-op or an assoc fn returns a forged zero. Rejected: check-
    only goldens for the happy path (BB's discipline).
137. **`deriving(From)` plus an explicit `from` is a type error.**
    02 §7.5's closed list "is not a macro system"; generating and
    hand-writing the same method is two mechanisms for one name.
    Diagnostic: `deriving(From) conflicts with an explicit \`from\` on
    \`T\``. Rejected: deriving wins (silently drops the body); rejected:
    handwritten wins (silently ignores deriving); rejected: first-wins.
    Supersedes nothing of 106's substance — this is the conflict rule
    106 never needed because it generated nothing.
138. **Generate a real DeclFn + FnItem, not a TypedFn-only patch.**
    Call sites resolve through `assoc_fn`'s AST/Decl zip; `?` hung a
    CalleeKey that `resolve_fn` must find. Injecting only a TypedFn
    would leave `Wrap.from(...)` as `no associated function` while `?`
    worked — the asymmetry B3 exists to remove. Rejected: special-
    casing lookup for deriving without a DeclFn (second resolution
    path).
139. **Body is ordinary construction, typechecked like any other.**
    Struct: `return Self(field=source)`; enum: `return Self.Variant(source)`.
    No evaluator/lowerer special case for the body. Rejected:
    hand-building a TypedFn without an AST body (skips flow/access on
    the generated fn; a second body shape).
140. **`?` Calls the generated fn; the structural wrap is deleted.**
    One mechanism: `resolve_fn` → `run_call` / `Inst::Call`. A miss is
    now fail-closed by name, not a wrap. Supersedes decision 106.
    Rejected: leaving the wrap as a fallback "just in case" (that is
    the second way).
141. **Generated `from` is `pub`.** Same reachability as the hand-
    written examples and as enum methods under decision 123 — an
    imported type's generated `from` is callable. Rejected: private
    by default (would make import/alias goldens fail for a reason the
    docs do not state).
142. **Both shapes get positive value goldens.** Struct was B's finding
    #1 (untested); enum already had `?` but not a direct call. Both
    now pin distinguishable settles. Rejected: enum-only because
    "struct falls out of the same code."
143. **Import and alias are separate goldens; asm pins the alias.**
    Plain import proves Decl splice; alias + `--stage=asm` proves
    DD/EE/FF re-key reaches `bl <Box.from>`. Rejected: alias-only
    (would leave the unaliased import path unpinned for this surface).
144. **Two-variant enum rejection gets its own err golden.** The
    sibling of `err-deriving-from-two-fields`; `validate_from_shape`
    already fired, but B's write-up noted it had no separate pin.
    Rejected: relying on the struct err alone.
145. **`check-deriving-from` / `check-from-explicit` dumps may gain
    `fn from` lines; values must not move.** Expected: LoadError's
    own deriving now shows a generated `from` in check/mwir. The
    conversion settles (`1000`/`7`/`107`) stay. A value move without
    an explained cause is a stop. Rejected: freezing the old dump
    text by hiding generated fns from `--stage=check`.
146. **No doc edit.** 02 §7.5 / 05 §8 already say generating the
    `from`; code now matches. Rejected: softening the docs to bless
    the structural wrap.
147. **Re-key every type name in an imported declaration's signature,
    not only the owning type.** At both splices and in
    `install_aliased_import_layouts`, build one exporter→local map from
    the importer's bindings for that exporting module and apply it in a
    single simultaneous pass. Parameter, return, field, generic
    argument, const type, and free-fn signatures are in scope. Rejected:
    extending only the owning-type rewrite (that is the bug); rejected:
    a consumer-side fallback that tries local then exporter (DD 86 /
    FF 100 — two sources of truth).
148. **Substitution is module-scoped and simultaneous.** The map for a
    splice from module E contains only `local != target_name` bindings
    whose `target_module` is E. Each `Type::Named` is looked up once —
    never chained. Rejected: a flat cross-module map (ambiguous when
    two exporters share a name); rejected: applying `(from,to)` pairs
    sequentially (the adversarial `Src↔Inner` swap transposes).
149. **Apply even when the owning name is unaliased.** A peer in the
    signature may still be aliased. The old `if local != target_name`
    guard around the whole re-key skipped that case. Rejected: keeping
    the guard and hoping callers always alias the owner too.
150. **Free fns and consts get the same map.** They carry signatures
    and were previously installed with no re-key at all. Rejected:
    structs/enums only (leaves `twice_src(take s: Src)` broken under
    `Src as Item`).
151. **True adversarial name-swap is a typecheck golden; values live
    on the dual-alias case.** `from pk import Src as Inner` together
    with `from ret import Inner as Src` typechecks under 147–148
    (`check-import-alias-sig-swap`) but A1b decision 15 withholds
    body-bearing splices from both exporters — each local name shadows
    a declaration of the other module — so comptime cannot evaluate
    through either. Asymmetric settle `403711` is pinned by
    `check-import-alias-sig-dual` (`Src as In` / `Inner as Item`), which
    exercises the same simultaneous map without triggering the
    withhold. Rejected: forcing values through the true swap by
    weakening A1b (out of scope; wrong item); rejected: dropping the
    true-swap golden (it is the one that catches a sequential flat
    map at typecheck).
152. **Ledger extends `sema.names.imported-types`.** Still decision 9's
    one local spelling; GG is breadth inside that rule. The note
    states whole-signature explicitly and records that A1/DD/EE/FF
    were layer findings while GG is a breadth finding. Rejected: a new
    clause that would fork the spelling rule again.
153. **`--stage=asm` pins at least one peer-aliased signature.** Param
    (`Grip.make`) and dual (`Grip.make` + `Out.peel`) both carry asm
    dumps — the earlier three findings in this family each reached one
    layer further than expected. Rejected: check/test only.
154. **Unaliased peer-in-signature stays a positive golden.**
    `check-import-sig-peer` locks identity-map behaviour so a future
    "always rewrite" cannot break the case FF/`check-import-*` already
    rely on. Rejected: trusting existing import goldens alone without a
    GG-shaped peer signature.
155. **The importer's type universe is explicit imports plus every type
    reachable through them.** Close `ModuleCtx` and
    `TypedProgram::imported` over types mentioned in imported
    declarations' signatures after the explicit splice. Rejected: a
    sixth per-consumer special case (the DD/EE/FF/GG pattern that kept
    producing the next miss one step further out); rejected: requiring
    the importer to name every type a pub API returns.
156. **Chase the defining module, not merely the re-exporting one.**
    When a reachable name is present in the origin only via *its*
    imports, follow that binding so `A→B→C` with only `A` imported
    still finds `C`. Rejected: looking up peers only in the immediate
    export module (that is the bug the chain golden caught).
157. **Decision 13 / EE 90 stand.** Reachable types are installed in
    the importer's lookup tables only; they are not merged into
    `TypedProgram::{structs,fns,...}` that lower iterates for "what
    this module declares". Rejected: merging into the own-decl maps
    (would emit an imported declaration once per importer).
158. **Spelling still requires a binding.** Reachability does not
    extend `imported_type_shapes` / `symbols::resolve`. Writing
    `b: Box` without importing `Box` remains `unknown name` /
    `unknown type`. Rejected: auto-injecting reachable names into the
    spelling table (contradicts 02 §2's "compile-time name bindings").
159. **Both exits are closed by the same closure.** Sema field-lookup
    miss (`has no field`) and comptime construction miss
    (`internal error: struct not found`) are the same empty-universe
    hole at two consumers. Rejected: fixing only the field-lookup path
    (leaves the house-rule bug standing).
160. **`unresolvable` must not shadow an installed reachable type.**
    After the typed closure, skip names present in
    `imported.structs`/`enums` (or own tables) when merging A1b's
    closure-wide miss notes. Rejected: leaving the miss note in place
    (`abandon_missing` would prefer it over the real decl).
161. **Non-`pub` refusal stays at the import act.**
    `err-import-type-not-pub` unchanged. Using a *value* of a reachable
    non-`pub` type is allowed (`check-import-reachable-private`).
    Rejected: refusing value use of non-pub reachable types (would
    invent a privacy rule §2 does not state for inference); rejected:
    pulling the exporter's whole world into the importer (breaks the
    not-pub golden and makes module privacy meaningless).
162. **Aliasing cannot apply to a name the importer never wrote.**
    `Maker as Builder` re-keys the owner; reachable `Box` keeps the
    exporter spelling. Pinned by `check-import-reachable-alias`.
    Rejected: inventing an implicit alias for reachable types.
163. **Ledger extends `sema.names.imported-types`.** The rule is still
    02 §2's import-as-binding plus the universe those bindings open;
    HH is the general statement of that universe, not a distinct
    spelling rule. Rejected: a new clause that would fork "what an
    import means" again.
164. **Every positive golden pins an evaluated value, and at least
    one reaches `--stage=asm`.** Settle values `305` / `215` / `12013`
    cannot pass by mere acceptance; asm dumps pin the guest tier.
    Rejected: check-only goldens for a defect that had a guest-facing
    exit.
165. **HH finding #1 is fixed by making it work, not by failing
    closed.** Instantiation keys re-key under the same
    `alias_subs_for_exporter` map the bodies already get, so
    `Box[Item]` finds `struct:Box[Item]`. The shape is ordinary
    language (alias a peer, call an imported body that constructs a
    reachable generic) and GG's "one spelling" rule already decided the
    direction. Rejected: an `error[unimplemented]:` naming the shape —
    honest, but it would refuse a program whose unaliased twin already
    evaluates to `406`, and the re-key is the dumb small fix.
166. **Re-key both the instantiation payload and its map key.**
    Bodies alone under `Box[Item]` with a map still keyed
    `struct:Box[Src]` is exactly HH#1. Rejected: leaving keys under the
    exporter spelling and teaching `resolve_struct_by_type` a fallback
    (a second lookup scheme; DD 86 / FF 100 rejected the same idea).
167. **`CalleeKey::FnInstance` / `MethodInstance` re-key too.**
    Otherwise a method call inside a re-keyed body would look up the
    exporter key against an importer-keyed map. Rejected: leaving
    instance keys alone (the pre-II comment in `rekey_callee`).
168. **`enrich_layout_ctx_with_instantiations` folds
    `imported.instantiations`, and `diff-eval` calls it.** Own-only
    enrichment (and a `diff-eval` path that never enriched) saw the
    exporter's `Box[Src]` while the importer's typed tree asked for
    `Box[Item]`, so `--stage=asm` worked after the splice fix but
    `diff-eval` counted the case as a lowering-skip. Same hole left
    `check-import-reachable-field-generic` a skip. Rejected: leaving
    the oracle blind to a guest path `--stage=asm` already dumps.
169. **The primary ratchet is a multi-module fuzz lane, not a
    per-site unreachability census.** The four finds (A1b, EE, HH,
    HH#1) all needed an import; every existing lane is single-file.
    A written-down "why unreachable" table would have claimed all four
    unreachable and stayed green. Rejected: the annotated census as the
    *primary* guard (kept as a secondary count lock — decision 171).
170. **`fuzz imports` shapes are a fixed set filled from the seeded
    RNG, not a general program generator.** Six shapes: comptime
    construct of an imported struct; imported fields+method;
    reachable-unimported; aliased peer + reachable generic; two-deep
    chain; aliased owner. Deep default 200_000; smoke 2×1_000 in
    `check`. Rejected: mutating existing single-file corpus into
    multi-module (would not reliably hit these shapes); rejected: a
    general multi-module synthesizer (expensive, and the gate runs
    locally every change).
171. **Census locks count and per-file distribution (162 sites), not
    per-site unreachability claims.** Adding a site without updating
    the list fails `cargo test`. Rejected: one-line "why unreachable"
    annotations for all 162 — false claims are worse than none.
172. **Ledger clause `compiler.diagnostics.no-reachable-internal-error`
    anchors on 04 §7.** The house rule lives in CLAUDE.md, not
    `docs/language/`; §7 is the diagnostic surface the rule constrains.
    No normative doc edit (no contradiction to repair). Rejected:
    editing CLAUDE.md into the ledger's `doc` field; rejected: a doc
    edit to manufacture an anchor.
173–178. **Reserved for the II family** (no reachable `internal error:`).
179. **HH's reachability closure must traverse enum variant payloads.**
    A struct reachable only as `enum Res: Good(Payload)` was outside
    the importer's typed universe, so `wrela test` refused a program
    whose single-module twin evaluates to `406`. Fix by making the
    shape work (carry payload types on `TypedEnum` and walk them), not
    by polishing the refusal. Rejected: improving the A1b-shaped
    diagnostic while leaving the empty-universe hole (would refuse a
    legal program); rejected: a special case that only installs the
    matched pattern's bound type (misses construction inside the
    imported body, which is the actual eval path).
180. **`TypedEnum` carries per-variant payload types.** Decl-side
    `collect_named_types_from_decl_enum` already walked payloads for
    ModuleCtx; the typed collector could not, because `TypedEnum` only
    stored variant *names*. Parallel `variant_payload_types:
    Vec<Vec<Type>>` (empty for a unit variant; named payloads flatten
    to field types) is the dumb fix the Decl/Typed asymmetry pointed
    at. Rejected: having the typed closure re-read DeclEnum from a
    discarded ModuleCtx; rejected: walking only fn signatures of
    `make` (Payload is not in `make`'s signature — only in `Res`'s
    variants and in `make`'s body).
181. **Neighbouring type edges ride `collect_named_type_names`.** Once
    a payload type is in the walk, tuple elements, fixed-array
    elements, nested struct fields (via the existing queue), and
    generic arguments (`Good(Box[Payload])`) are already collected —
    no new collectors. Verified by hand; the enum-payload fuzz shape
    plus a generic-payload variant lock the edges that were actually
    missing. `own[P] T` in an enum payload is refused by the language
    itself (pool-binding check does not cover enum payloads — hold it
    in a struct field); not a reachability miss. Rejected: minting
    five near-duplicate goldens for edges the same walk already
    covers.
182. **The body-only private-helper diagnostic drops the stale A1b
    cite.** After HH/JJ, what remains unsupported is a declaration
    reachable *only* through an imported body's private helpers, not
    through any imported signature. Say that plainly; do not point at
    closed item A1b. The true-swap withhold (decision 15) still
    accurately cites A1b and is unchanged. Rejected: leaving the
    Finding-1 program blaming A1b; rejected: deleting the private-
    helper refusal (still a real boundary; golden
    `err-import-comptime-private-decl`).
183. **`fuzz imports` gains the enum-payload shape (and the generic-
    payload neighbour).** Fixed-shape convention, filled from the
    seeded RNG — regression locks, not discovery. Deep default and
    smoke budgets unchanged. Rejected: relying on random mutation to
    invent `enum Res: Good(Payload)` (it will not).
184. **Ledger extends `sema.names.imported-types`.** Still 02 §2's
    import-as-binding plus the universe those bindings open; JJ is the
    enum-payload edge of HH's universe, not a distinct spelling rule.
    Rejected: a near-duplicate clause.
185. **Finding 2 — recorded, not fixed.** A free generic function
    cannot forward its own type parameter into a generic type
    (`fn wrap[T](take x: T) -> Box[T]: return Box[T](v=x)` →
    `error[type]: generic parameter \`T\` requires a type argument`).
    Single-module as well, so not an import bug. Methods on a generic
    struct that use Self's `T` (`get -> T`, `set(take x: T)`) work;
    associated fns that construct `Box[T]` hit the generic-method
    boundary. Likely to block a natural writing of item F
    (collections) free helpers over `List[T]`; F may still attempt a
    method-shaped API. One variable at a time — not JJ's fix.
186. **Positive golden pins evaluated value `406`.** Mere acceptance
    is not enough; the settle value distinguishes "compiles" from
    "binds the payload and reads `.n`". At least one dump reaches
    `--stage=asm`. Rejected: check-only.
220. **`ImageReport` is a fixed-field stdlib struct, not a String-keyed
    API.** `stdlib/core/image_report.wr` exposes `machine_revision: u32`
    plus `entry` / `pages_{base,size}` / `stacks_{base,size}` /
    `code_{base,size}` as `u64`. Enough to pin real layout facts today
    without `String[..N]` (not on master; branch `m9-item-c` is busy).
    Rejected: the virtio sketch's `peak_memory` / `queue("...")` /
    `region_promotions` surface (needs strings and more reflection);
    rejected packing formatted text into a `u64` (the earlier rejected
    encoding trap).
221. **The compiler synthesizes a `Value::Struct` from the real
    `ImageLayout`; it does not hand the assert a hardcoded table.**
    Field order follows the stdlib declaration; values come from
    `wrela_machine` constants + the laid-out `code` section. Rejected:
    a partial/fake `ImageReport` (decision 10's standing refusal reason);
    rejected a compiler-side prelude type (shape decision 1).
222. **`report::render` no longer refuses registered asserts.** The
    decision-10 report-boundary diagnostic is retired; asserts run
    *after* layout via `eval::layout_assert::run`. A registered assert
    without a successful layout fails closed by name. Rejected: keeping
    the refusal once `ImageReport` exists (would leave the clause gap
    forever while lying about why).
223. **`@layout_assert` is a marker attribute with a checked signature.**
    Exactly one read param of the stdlib `ImageReport` shape, return
    `unit`, no attr args; `img.check_layout` requires the argument carry
    `@layout_assert`. Joins the `@test`/`@image` exclusive marker family.
    Rejected: validating only at report time (a plain `fn` would typecheck
    then fail late).
224. **`image-basic` drops its vacuous `check_layout` rather than
    gaining a `core` import.** Decision 84 would force a project reshape;
    the basic fixture stays flat. The clause's own pins are the two new
    goldens; `image-basic`'s report moves from the old refusal text to a
    successful full report. Rejected: converting `image-basic` to a
    project just to keep a no-op assert registration.

201. **`String[..N]` layout is one length word plus `N` byte slots**
    (`size_of = 8*(1+N)`), each a machine slot like `Bytes[N]`. Occupied
    length is a runtime `usize` at slot 0; slots `1..=N` hold UTF-8
    bytes (unoccupied padded 0). Text literals coerce when expected type
    is `String[..N]` and `len ≤ N`. Exact `String[N]` and bare `String`
    refuse by name. `Bytes[..N]` does not fall out of the same change
    and stays unimplemented. Rejected alternative: dense `align8(8+N)`
    packing — smaller, but a second layout rule beside every other
    aggregate's slot-stride `Project`/`MakeAggregate` path. Rejected:
    fat pointer / heap (capacity is a build-time fact; no allocator).
    Clause `language.types.string-bound`. (plans/M9.md item C1; opens
    the type C's packed-u64 writer stood in for — decisions 179–180 /
    200's inheritance claim are superseded by item C2 once Format is
    rewritten onto this type.)

## Work items, in order

Progress (ticked in the same commit as the work):

- [x] AA. **The intrinsic-surface closure guard** (decisions 56–59).
      Ordered before B–F deliberately: it is a **ratchet, not an audit**.
      ROADMAP M10 warns that migrating item-by-item and adding whatever
      intrinsic each item needs "designs the permanent surface by
      accretion, in the one place this project can least afford it."
      That pressure is in M9, not M10 — every stdlib item faces the
      temptation to add a compiler-recognized name instead of writing
      wrela. Shape decision 1 forbids it in prose; nothing enforces it.

      **We do not currently know how big the surface is, and it cannot
      be counted by grepping** (measured 2026-07-25 at 5de3529):
      `sema/bodies.rs` has 42 `TypedExprKind::Intrinsic` mentions and 35
      sites with a literal `key: "..."`, spelling 34 distinct names — but
      four sites build the key with `format!()` (`Image.{mname}`:3886,
      `Mmio.{op}`:4861, `InterruptCell.{method}`:7629, `Image.{name}`:9231),
      so membership depends on what those variables can hold, scattered
      through a 10,000-line file. Seven `is_*_intrinsic(key)` predicates
      (`is_mmio_access_intrinsic`, `is_untrusted_narrowing_intrinsic`,
      `is_device_transport_intrinsic`, `is_queue_op_intrinsic`,
      `is_irq_cap_intrinsic`, `is_interrupt_cell_intrinsic`,
      `is_wake_intrinsic`) then draw the consumer-side boundary by
      prefix matching. **The census is the first half of this item and
      may itself be the finding.**

      Deliverable, in order:
      1. **Enumerate every name sema treats as intrinsic by reading all
         42 sites**, not by grepping literals. A name reachable only
         through a `format!()` key counts and must be resolved to the
         concrete set its variable ranges over. If that set is not
         statically knowable, *that* is the finding — record it and say
         what would make it knowable.
      2. **One test asserting the live set equals a written-down list.**
         The list is 05 §9's image-builder surface plus a short,
         explicitly-named exception set carrying a one-line
         justification each. Adding a name then requires changing the
         list in the same commit — which is the ratchet.
      3. **A ledger clause** so the guard is spec-anchored, citing
         05-library.md §9.

      Not in scope: removing anything. AA measures and locks; item E
      does the one removal M9 has already identified (see E).

      **Landed 2026-07-25.** `crates/wrela-compiler/src/sema/intrinsics.rs`
      (the list), five unit tests (the ratchet), ledger clause
      `library.intrinsics.closed-surface`.

      **The census, as measured.** 38 construction sites in
      `sema/bodies.rs` → **44 distinct keys**. 34 literal `key: "..."`
      sites; four `format!` sites contributing 10 more, every one of
      them statically knowable (decision 56). The plan's own prior
      numbers check out: 42 `TypedExprKind::Intrinsic` mentions in
      `bodies.rs` = 38 constructions + 3 comments + 1 pattern match; the
      "35 literal sites" figure counted one of those comments, so it is
      34.

      | Surface | n | Keys |
      | --- | --- | --- |
      | 05 §9 image builder | 10 | `Image`, `Image.device/.driver/.actor/.pool/.dma_pool/.supervise/.check_layout/.seal`, `ImageDecl.handle` |
      | Time (05 §5) | 3 | `ms`, `seconds`, `now` |
      | Builder vocabulary | 1 | `RestartIntensity` |
      | Typed MMIO (03 §2) | 2 | `Mmio.read/.write` |
      | Marked values (03 §8) | 1 | `Untrusted.checked_le` |
      | Device bring-up (03 §1/§9) | 7 | `Device.claim/.map_partition/.negotiate/.read_capacity_sectors/.reset/.start/.take_irq` |
      | Queues and receipts (03 §4/§5) | 10 | `VirtQueue.claim/.configure/.drain/.prepare_block/.publish/.reclaim/.recover/.reject/.reserve_proven/.suppress_interrupts` |
      | Interrupts (03 §6) | 8 | `IrqCap.bind/.unmask`, `InterruptCell.new/.load_acquire/.store_release/.swap_acquire/.fetch_or_release`, `wake` |
      | Scoped concurrency (02 §9) | 2 | `Group.start/.join_all` |

      §9's 10 are exactly `sema::typed::is_restricted_intrinsic`'s set,
      asserted in both directions. The other 34 are the exception set,
      one justification line each.

      **Findings recorded, not acted on.**
      - `is_queue_op_deferred` accepts `VirtQueue.poll_sources` and
        `VirtQueue.completions_pending`, which **no construction site
        produces** — `check_virtqueue_method` rejects both methods with
        `unimplemented_at` first, so the consumer arms in `lower.rs`
        and `flowwir_lower.rs` are unreachable. Dead boundary, pinned
        as `UNPRODUCED_CONSUMER_KEYS`.
      - `InterruptCell.new` is the weakest exception in the list: it
        lowers to a plain `Copy`, unlike its four siblings which lower
        to dedicated ordered instructions. This is item E's
        "investigate, don't presume" question answered: the *ops* need
        compiler knowledge (no atomic/ordering expression form exists,
        and ROADMAP M10's floor says instructions with no expression
        form stay compiler-side); the *constructor* is intrinsic only
        because the type is compiler-known, and becomes a candidate for
        ordinary wrela the day `InterruptCell[T]` is a stdlib type. Do
        not act on it in E — its field-offset addressing
        (`self.<field>` only) is still a compiler contract.
      - Item E's removal is a **two-line diff**: `ms` and `seconds` are
        the first two entries of `EXCEPTIONS`, marked `ACCRETION`, under
        one comment block that names E as their executioner.

      **What the guard does not cover, stated plainly.**
      - It covers intrinsic *keys* (meaning). It does not cover
        `sema/prelude.rs`'s bare *names* (resolution); the two surfaces
        are not in bijection in either direction, and item I owns the
        prelude.
      - The four `format!` expansion sets are hand-resolved, backed by
        locking each bounding guard's exact source text. That lock is
        brittle to *reformatting* those four lines — deliberately: the
        failure is loud and says re-resolve the range.
      - **Marker-spelling evasion, found by the orchestrator and closed
        (decision 59a).** The first scan matched one exact line spelling
        and missed three others, including a construction in
        `bodies.rs` itself. The scan now matches the weak marker and
        classifies every occurrence. Residue: a *pattern* written
        `TypedExprKind::Intrinsic { key: k, .. }` — a shape no file uses
        today — would trip the "cannot resolve this key" panic and need
        either rewriting to `{ key, .. }` or a new classifier arm. That
        is a false positive, which is the safe direction, but it is a
        real cost and a later item may pay it.
      - The scan is textual. It reads what is written, not what the
        compiler builds; a macro that expanded to an intrinsic
        construction would be invisible. No such macro exists in this
        crate, and none should.
- [x] A1. **Imported types resolve in type positions** (decision 6).
      Landed 2026-07-25 (decisions 8–12). Goldens:
      `check-import-type-position` (all six positions, plus the imported
      `resource` field that proves decision 10),
      `err-import-type-not-pub`, and two the item earned:
      `check-import-type-cycle` (mutual type imports) and
      `err-import-type-cycle-sized` (a by-value cycle closing across the
      module boundary). Clause `sema.names.imported-types`.
      Found in passing, out of scope, pre-existing at `5de3529`: a
      comptime-reachable construction of a struct declared in *another*
      module reports `error[comptime]: internal error: struct \`Pair\`
      not found` — `TypedProgram::structs` is never spliced, so
      `eval::interp::resolve_struct_by_type` misses. An `internal error:`
      is a bug by house rule; it is not A1's, and A2/B onward will meet
      it the moment a stdlib type is constructed at comptime.
      Split out of A on 2026-07-25, before A was dispatched, because the
      starting-state block above measured that they do not — and no part
      of A2 (nor of B–J) can be written without it. `types::declare`'s
      module-local type-name table learns the same import bindings
      `symbols::resolve` already gets, so an imported `struct`/`enum`
      name is legal wherever a type is legal: fn param, fn return,
      struct field, `const` type, `let` annotation, and as a generic
      argument. Cycles must stay free — whatever this does, it may not
      require module A's type table to be finished before module B's can
      be built (`sema/mod.rs`'s splice note is the constraint to respect,
      not to rewrite). Fails closed on the shapes it does not do, named.
      Goldens: `check-import-type-position`, `err-import-type-not-pub`.
- [x] A1b. **The comptime evaluator sees imported declarations**
      (decisions 13–15). Landed 2026-07-25. Added to this plan by its own
      agent, in the same commit as the work: A1 found the defect in
      passing and A1's note above is its report.

      The defect, confirmed twice against a binary built at `d1292c6`
      (i.e. it predates A1): a comptime-reachable construction of a
      struct declared in *another* module abandoned with `error[comptime]:
      internal error: struct \`Pair\` not found`. An `internal error:` is
      a bug by house rule (CLAUDE.md) and `cargo xtask fuzz` treats every
      one as a find; this one has been reachable from ordinary source
      since M4 item A. **The same hole covered all four importable
      declaration kinds**, and probing each was the first half of the
      item:

      | Imported thing, reached at comptime | Before A1b | After |
      | --- | --- | --- |
      | `pub struct` (construct / field / method) | `internal error: struct \`Pair\` not found` | evaluates |
      | `pub const` (read) | `internal error: const \`K\` not found` | evaluates |
      | `pub fn` (called) | `callee \`double\` is not available ... (a generic instantiation not yet resolved)` — named, but the wrong cause | evaluates |
      | `pub enum` (variant construct / match) | `evaluating a generic enum instantiation's variant ... is not supported yet` — named, but the wrong cause | evaluates |

      All four are fixed; none was a different problem. `TypedProgram::imported`
      is the mechanism (decision 13); the fail-closed residue is decision
      15 and is pinned, not merely described.

      Goldens: `check-import-comptime-struct` (the exact repro; its
      evaluated value pinned as `Arg label=mailbox value=16`),
      `check-import-comptime-decls` (`base value=5` / `scaled value=15` /
      `weighted value=7` — one per remaining declaration kind),
      `check-import-comptime-cycle` (a type-bearing import cycle whose
      comptime evaluation crosses it in both directions,
      `Arg label=depth value=6`), `err-import-comptime-private-decl` and
      `err-import-comptime-shadowed-decl` (the two decision-15 boundaries).
      Clause `comptime.eval.import-closure`.

      **Found in passing, out of scope, recorded not fixed.** No fuzz
      lane could have found this: every lane (`lexer`, `parser`, `sema`,
      `eval`, `lower`, `async`) generates and checks a *single file*, so
      no lane can construct a multi-module closure at all, and the whole
      of `sema::check_program_typed` — imports, the two splices, the
      whole-closure classification — is unreachable from every one of
      them. That is a statement about the lanes, not about this defect,
      and it is the honest reason a reachable `internal error:` sat in
      the tree for five milestones. Widening a lane is not built here.

      Three more, all reproduced against a binary built at `2fbd6f1`
      (i.e. all predate A1b), all outside its scope, none fixed here:

      1. **`?` at comptime is wrong, and on `None` it panics the
         compiler.** `eval::value` numbers both builtin sums from zero
         (`OPTION_NONE`/`RESULT_OK` = 0, `OPTION_SOME`/`RESULT_ERR` = 1),
         and `interp.rs`'s `Try` arm asks `idx == OPTION_SOME || idx ==
         RESULT_OK` — which is true for *every* index either sum can
         carry. So `Err(e)?` yields `e` as the **success** value instead
         of propagating (a silent wrong value at comptime), and `None?`
         takes the same branch and hits `payload.pop().expect("Some/Ok
         always carries one payload value")` on an empty payload —
         `thread panicked at crates/wrela-compiler/src/eval/interp.rs`,
         from ordinary single-module source, no imports involved. The
         `Try` node knows which sum it is from `expr.ty`; the fix is to
         branch on that instead of on the index alone. This is the
         highest-value find in this item and deserves its own item:
         a panic and a silent wrong value in the same three lines, in
         the one construct 02 §7.4 and M9 item B build on.
      2. **An alias breaks an imported struct's methods and an imported
         enum's patterns.** `from lib import Pair as Duo` then
         `Duo(a=1,b=2).sum()` reports `error[type]: type \`Pair\` has no
         method \`sum\``; `from lib import Color as Hue` then
         `case Hue.Red:` reports `error[type]: expected a \`Color\`
         pattern, found \`Hue\``. Construction under the alias works, so
         the two halves disagree about which spelling is canonical —
         A1's decision 9 territory, not A1b's.

         **Status corrected 2026-07-25 (orchestrator, verifying A2): the
         two halves have come apart.** The *pattern* half is fixed and
         the *method* half is not. Measured at `df24802`:
         `from lib.color import Color as Hue` then `case Hue.Red:`
         now checks clean, and the arms are genuinely analysed rather
         than skipped — dropping `case Hue.Blue:` reports
         `error[match]: match is not exhaustive: missing .Blue`, and
         `IoError.NoSuchVariant` on a stdlib enum reports `error[type]:
         enum \`IoError\` has no variant \`NoSuchVariant\``. But
         `from lib.pair import Pair as Duo` then `Duo(a=1, b=2).sum()`
         still reports `error[type]: type \`Pair\` has no method
         \`sum\``, at both `--stage=check` and through `wrela test`.
         Recorded rather than left standing because a note that says
         "broken" about something already fixed costs the next reader a
         wasted repro, and because half a finding is a different finding:
         what remains is specifically **method lookup**, which is the one
         consumer of decision 9's canonical spelling that was never
         updated. Item DD owns it.
      3. Both alias cases are *named* diagnostics, not `internal
         error:`, which is why A1b left them: they are honest, just
         wrong.
- [x] A2. **The stdlib is loaded** (decisions 77–85). Landed 2026-07-25.
      `stdlib/core/` exists; the compiler compiles it as ordinary wrela
      source as part of the user module's import closure;
      `wrela dump --stage=check`/`typed`/`mwir` reach stdlib code.
      Clause `library.stdlib.loaded`.

      **On which name moves first (decision 7, executed as 77).** The
      plan said pick from `LITERAL_SURFACE` or record that they are all
      special-cased. Measured at `db5703f`: all three
      (`Static`/`Str`/`Bytes`) resolve to builtin compiler types and
      cannot be declared in wrela at all. Of the six
      `builtin_enum_variants` enums, `IoError` is the smallest
      genuinely-movable one (one unit variant, ordinary data, ~7 golden
      value sites, not load-bearing for `?`/`take`/`InterruptCell`).
      `Option` stays under item I.

      **Implicit vs explicit (decision 79).** Explicit. 02-language.md
      §2: the fixed prelude is `Option`/`Result`/`panic`; "Everything
      else is imported." Rejected alternative (always-load +
      auto-inject) would have preserved today's always-in-scope surface
      for `IoError` but contradicted that sentence, and would have put
      a stdlib module into every check dump.

      **Shapes audited, each run and read.** `from core.io_error import
      IoError` then `IoError.OutOfRange` at check/typed/mwir; sibling
      `stdlib/` without `core/` → named `stdlib not found`; toolchain
      `core.nothing` still → `module not found` (err-import-core-miss
      unchanged); check-receipt-handoff after project-shape conversion
      (check gains `Module path=io_error`, mwir byte-identical for the
      root); boot-receipt-handoff report gains `Input path=core/
      io_error.wr`; err-* line numbers shift by +2 (the import line);
      `check_typed` API still rejects imports
      (`unit:check_typed_rejects_imports`); corpus before/after
      unchanged at `lexed 31, parsed 24, fragments skipped 7`.

      **Goldens.** `check-stdlib-loaded` (check/typed/mwir: stdlib enum
      in the dump, `MakeEnum tag=0`); `err-stdlib-missing` (named
      `error[build]: stdlib not found: ...`). Seven IoError consumers
      converted to project shape with the import.

      **Found in passing, not fixed.** (1) `--stage=typed` dump of a
      stdlib-only module prints an empty `Program` block — `dump_typed`
      never renders `TypedProgram::enums`, only consts/fns/structs/
      instantiations; the check dump is what pins the enum. (2)
      `lower`/`flowwir_lower` did not read `imported.enums` (A1b fixed
      eval only) — fixed here as decision 82 because A2 made it
      reachable. (3) Docs corpus still only lexes/parses; item J owns
      the sema flip. (4) `docs/language/examples/virtio-storage.wr`
      still spells `IoError.OutOfRange` without an import — fine until
      corpus sema-checking.

      **What item I still unpicks.** `Option`/`Result`/`Some`/`None`/
      `Ok`/`Err`/`panic`; the five remaining `builtin_enum_variants`
      enums (`Target`/`Restart`/`BootError`/`DriverMode`/
      `CompletionOutcome`) and their per-module injection; every other
      `HARDWARE_SURFACE`/`IMAGE_BUILDER`/`ACTOR_SURFACE` name;
      `mwir::size_of`'s leftover `IoError` unit-tag arm; deleting
      `prelude.rs` in one commit.
- [x] BB. **`?` is broken at comptime** (decisions 60–63). Landed
      2026-07-25. Added to this plan by its own agent, in the same commit
      as the work. Ordered immediately before B because B builds `from`
      conversion on top of `?`, and building a feature on a `?` that
      returns the error payload as a success value would bury the defect
      under it.

      **The defect**, found by A1b in passing, reproduced independently
      by the orchestrator at `102e523`, and reproduced a third time here
      against a binary built at `27193fe`. `eval::value` numbers both
      builtin sums from zero (`OPTION_NONE`/`RESULT_OK` = 0,
      `OPTION_SOME`/`RESULT_ERR` = 1), so a variant index alone says
      nothing about which sum it belongs to. `eval::interp`'s `Try` arm
      asked exactly that — `idx == OPTION_SOME || idx == RESULT_OK`,
      true for **every** index either sum can carry. Two consequences,
      both from ordinary single-module source with no imports:

      | Shape | Before | After |
      | --- | --- | --- |
      | `Err(5)?` in a `Result`-returning fn | yields `5` as the **success** value | propagates `Err(5)`, payload intact |
      | `None?` in an `Option`-returning fn | `thread panicked at eval/interp.rs:1508` | propagates `None` |
      | `Ok(v)?` / `Some(v)?` | yields `v` | unchanged |

      The fix is decision 60: read the sum from the operand's own static
      type. The two `expect()`s became fail-closed `abandon`s in the same
      edit.

      **Shapes audited, each run and read, not assumed.** `?` on
      `Result` in a `Result`-returning fn; `?` on `Option` in an
      `Option`-returning fn; success payloads that are a scalar, a
      struct, and `unit` in statement position; `?` nested through a
      chain of calls; `?` in a `const` initializer; `?` inside a
      *statement-level* `comptime if` branch; `?` inside a fallible
      `init` and on the `Holder(...)?` call that consumes it; `?` under
      an active `defer`; `?` reached through an **imported** fn (the
      path A1b made live one item earlier — where the old code panicked
      the compiler rather than merely lying). All correct.

      Two shapes are *not* reachable and are recorded rather than
      tested. `?` in a **module-level `comptime if` condition** cannot
      exist: that condition's vocabulary has no calls at all
      (`specialize.rs`'s own module doc) and there is no enclosing fn
      for `?` to return from. `?` **with a `from` conversion** does not
      work in either tier — see decision 61.

      **The index audit (deliverable 3): clean, stated plainly.** Every
      use of `OPTION_NONE`/`OPTION_SOME`/`RESULT_OK`/`RESULT_ERR`, and
      every comparison of a builtin sum's variant index, was read.
      `interp::variant_index` and `lower::variant_index` dispatch on the
      enum *name* first; `eval::image::render_value` and
      `sema::generics::value_to_const_arg_expr` likewise; `interp::run_init`
      is guarded by the callee's declared `Type::Result`; every site in
      `lower.rs`/`flowwir_lower.rs`/`codegen.rs`/`layout.rs` is a
      `Result`-only construction in a `Result`-typed context. **No second
      instance of the confusion exists.** `TypedExprKind::Try` was the
      only place a builtin sum's index was tested without the type that
      gives it meaning.

      **The runtime question (deliverable 4): the backend was already
      right** (decision 63). `lower::lower_try_sync` and
      `flowwir_lower::lower_try_check` both destructure the operand's
      `Type::Result` before comparing the tag against `RESULT_OK`, and
      both reject an `Option` operand by name. Evidence, not just
      reading: `cargo xtask diff-eval` now boots
      `check-try-err-propagates` and `check-try-ok-unwraps` and reports
      `2 comptime test(s) agree` / `3 comptime test(s) agree` — the
      guest and the evaluator produce byte-identical report lines for
      the propagating-`Err` and unwrapping-`Ok` paths. The two `Option`
      cases print `lowering failed closed (lowering `?` on a
      non-`Result` (e.g. `Option`) value in a synchronous body is not
      implemented yet)` and are counted as lowering-skips. That
      one-sidedness is itself the finding: `diff-eval` is built for
      exactly this class and reported full agreement every run, because
      no golden carried a comptime-legal `@test` whose evaluation
      crossed a failing `?`.

      **Why fuzz missed it.** Nothing structural blocked it — the eval
      lane runs `@test` fns under `catch_unwind` and a `?` program is a
      single module, so unlike A1b's find this surface was *reachable*.
      It was unseeded. `corpus_seed_inputs` mutates every
      `tests/golden/*/input.wr` at the byte level, and no golden input
      contained a comptime-reachable `?` on a failing operand:
      `check-bodies` holds the only `?`-with-conversion shape and has no
      `@test` and no `const` that calls into it, so it never evaluates.
      Reaching the defect from a byte mutation meant assembling, by
      chance, a type-correct program with a fallible callee, a failing
      return, a `?`, and something that runs it. The honest fix is the
      seed set, and it needs no new mechanism: the four `check-try-*`
      goldens are single-file cases and are therefore fuzz seeds from
      this commit on. One cheap check was added alongside (decision 62):
      the eval lane now treats an `"internal error: "` message as a bug,
      which it uniquely did not.

      Goldens: `check-try-err-propagates` (`Arg label=settled value=1005`),
      `check-try-none-propagates` (`value=777`), `check-try-ok-unwraps`
      (`7`/`12`/`1` — scalar, struct, `unit` in statement position),
      `check-try-some-unwraps` (`30`/`20`), `check-try-import` (all four
      outcomes through an imported body: `1011`/`7`/`777`/`103`),
      `err-try-from-underived`. Every one pins an *evaluated value*
      through an `@image` argument as well as a `@test` verdict —
      `settle` maps a failure into a range success cannot reach, so
      "accepted" and "correct" are different results. Clause
      `comptime.eval.try-propagation`; notes fixed in the same commit on
      `comptime.eval.no-panics` (the lane that should have found the
      panic) and `compiler.eval.matches-backend` (the oracle that should
      have found the divergence).

      **Found in passing, out of scope, reproduced but not fixed.** All
      three predate this item and none involves `?`:

      1. **A non-receiver `mut` parameter is silently not written back
         at comptime.** `fn set(mut c: Counter): c.n = 7` called as
         `set(mut c)` leaves the caller's `c.n` at 0 — `interp::run_call`
         hands back only `self`. A *silent wrong value* at build time,
         which 02 §5.1 ("non-receiver `mut` and `take` are mirrored at
         the call site") says must not happen. The receiver path is
         correct. The backend has no non-receiver write-back mechanism
         either (`Inst::Call` carries `self_write_back` and nothing
         else), which is why `diff-eval` cannot see it: both tiers are
         wrong the same way. This is the highest-severity find of the
         item after the one it was dispatched for.
      2. **A module-level `comptime if` makes a `const` initializer that
         calls a fn fail with `unknown name`.** `const A: u32 = one(1)`
         plus `comptime if A == 2:` reports `error[name]: unknown name
         `one``, for a fn declared three lines above. `specialize.rs`
         checks every const reachable from a `comptime if` condition
         against a skeleton `ModuleCtx` with no fns in it — a real,
         disclosed boundary ("this vocabulary has no fn calls at all"),
         but reported with the wrong cause. 02 §12 permits a `const`
         initializer to call comptime-callable fns. Named but dishonest,
         the same class A1b recorded twice.
      3. **`from` cannot be spelled.** 02 §7.4 says a target error type
         "must declare a matching `from(take source)`", and `fn from(...)`
         is `error[parse]: keyword `from` cannot be used as a name`. So
         `bodies::try_from_conversion`'s explicit-assoc-fn arm is dead
         code today, and `deriving(From)` is the only possible source of
         a conversion — which generates nothing (decision 61). Item B
         owns both halves; this is a doc-vs-code disagreement where the
         doc wins.
- [x] CC. **Non-receiver `mut` is mirrored at the call site**
      (decisions 68–75). Landed 2026-07-25. Added to this plan by its
      own agent. BB found the defect in passing: `fn set(mut c:
      Counter): c.n = 7` called as `set(mut c)` left the caller's `c.n`
      at 0. 02-language.md §5.1 requires the mirroring; both tiers were
      wrong the same way, which is why `diff-eval` never saw it.

      **The defect**, reproduced at `0b732bc` against `/tmp/wprobe/cc1.wr`:
      `--stage=image` printed `Arg label=a value=0` / `b value=0` (docs
      require 7 and 42); `wrela test` failed both asserts; `--stage=mwir`
      printed `Call key=bump ... args=[t2]` with no write-back operand.
      Evaluator: `run_call` returned only `final_self`. Backend:
      `Inst::Call` carried only `self_write_back`.

      **CC1 (evaluator).** `CallOutcome` carries every non-receiver `mut`
      parameter's final value; `eval_call` and `CallValue` write each
      back through `place_mut`. A defaulted `mut` argument fails closed
      by name (decision 69). Goldens: `check-mut-param-writeback`
      (`agg=7` / `sca=42` / `nest=9` / `field=11` / `chain=8` / `mix=9`
      — no `@test`, so it stayed out of `diff-eval` while the backend
      was still wrong), `err-mut-param-defaulted-writeback`. Decisions
      68–71.

      **CC2 (backend).** `write_backs: Vec<(usize, Temp)>` replaces
      `self_write_back` (decision 72). Params carry `AccessMode`
      (decision 75). Mut params — including scalars — pass by pointer;
      the callee epilogue writes them back (decision 74). Nested
      field/index `mut` args fail closed in lower, matching `mut self`
      (decision 73); the evaluator still handles those places. Golden:
      `check-mut-param-nested` (five `@test`s: aggregate, scalar,
      nested-field mutation inside the callee, mut-to-mut chain, mut
      alongside read+take with settle `9`). Full `diff-eval` agrees on
      all five. Clause `comptime.eval.mut-param-writeback`.

      **Shapes audited, each run and read.** Aggregate `mut` param;
      scalar `mut` param; `mut` param mutated through a nested field
      inside the callee (via a one-level copy/assign round-trip —
      lower refuses multi-level field *writes*, which is a pre-existing
      boundary, not this item's); `mut` param passed onward to a second
      `mut`-taking callee; `mut` alongside `read` and `take` in one
      signature; call-site field place (`mut c.n`) in the evaluator
      only; closure/`CallValue` `mut` write-back; defaulted `mut` arg
      (fails closed).

      **Why fuzz/diff-eval missed it.** Nothing structural blocked the
      eval lane — a `mut`-param program is a single module. It was
      unseeded: no golden input contained a comptime-reachable
      non-receiver `mut` whose result was observed. `diff-eval` compared
      only `@test` fns, and every existing golden that used non-receiver
      `mut` had none — and both tiers agreed on the wrong answer anyway,
      so an oracle over the corpus could not have diverged. After CC2,
      `check-mut-param-nested`'s five `@test`s report
      `5 comptime test(s) agree`.

      **Moved goldens (mechanical, each read).** `self_write_back=tN` →
      `write_backs=[0:tN]` in `mwir-struct`. Non-`read` param modes now
      print in headers (`params=[t0:take]`, `params=[t1:take,t2:take]`)
      in `mwir-take`, `boot-dma-reclaim`, `check-device-*`,
      `check-driver-mode-*`, `check-interrupt-cell`,
      `check-completion-outcome`, `check-receipt-handoff`, and
      `check-regions-two-disciplines` (flowwir). No value-bearing line
      moved.

      **Found in passing, out of scope.** A call-site field/index `mut`
      argument (`field_bump(mut c.n)`) evaluates correctly but fails
      closed at lowering (decision 73). Pre-existing; not fixed here.

      **Orchestrator verification, and the one thing it added
      (decision 76).** Every claim above was re-probed independently
      before the merge was accepted: `/tmp/wprobe/cc1.wr` returns 7/42
      where it returned 0/0; the five `err-access-*`/`err-overlap-*`
      cases still reject by their original diagnostics, so the item
      bought write-back without weakening the exclusivity or
      explicit-place rules it rests on; `field_bump(mut c.n)` fails
      closed with `error[unimplemented]:` rather than an `internal
      error:` or a silent wrong value.

      Then ten shapes the item's own goldens do not cover were written
      as a property probe and **all ten agreed across both tiers** —
      which is what earned them a golden of their own,
      `check-mut-param-properties`. The reasoning is M8's closing
      lesson applied one item early: a directed item's default failure
      mode is closing exactly the cases it was shown, so the review's
      job is to attack the *mechanism*, not to re-run the item's own
      list. The properties are named in the clause note with the value
      each one pins; the five that matter are a callee that never
      assigns its parameter (must not clobber), an early `return`
      before the assignment (must not publish), a `defer` after it
      (must be included), two `mut` parameters (must not cross-write),
      and an unassigned sibling field (must survive). None of the five
      was in the item's own case list, and all five were already
      correct — so this is a lock on work that was right, not a repair.
      That is the honest outcome to record: the review found no defect
      and the coverage is the finding.
- [x] DD. **An aliased imported type's methods resolve** (A1b finding 2,
      half of it; decisions 86–89). Landed 2026-07-25. Ordered before B
      because `deriving(From)`/`deriving(Format)` generate methods on
      imported types, and every aliased stdlib type would inherit this
      defect the moment those land.

      **The defect**, measured by the orchestrator at `df24802` and
      reproduced here against a binary at `d6e671c`. Three files:

      ```
      # lib.pair
      pub struct Pair:
          a: u32
          b: u32
          pub fn sum(read self) -> u32:
              return self.a + self.b

      # app.alias3
      from lib.pair import Pair as Duo
      const S: u32 = Duo(a=1, b=2).sum()
      ```

      Both `--stage=check` and `wrela test` reported
      `error[type]: type \`Pair\` has no method \`sum\``. Construction
      under the alias worked; only method lookup missed. The diagnostic
      named the *unaliased* spelling — the author never wrote `Pair`.

      | Shape | Before | After |
      | --- | --- | --- |
      | `Duo(a=1,b=2).sum()` | `type \`Pair\` has no method \`sum\`` | evaluates to 3 |
      | `Cnt.zero().get()` (assoc) | `type \`Counter\` has no method \`get\`` | evaluates |
      | `c.bump()` / `c.into_n()` (mut/take) | same miss after construction | evaluates |
      | `d.nope()` on `d: Duo` | would have said `Pair` | `type \`Duo\` has no method \`nope\`` |
      | `match Hue.Red: case Hue.Red:` | `expected a \`Color\` pattern, found \`Hue\`` | checks clean |
      | `c: Hue` then `case Hue.Red:` | already clean (A1) | unchanged |

      **Root cause.** Method lookup in `bodies::check_call_by_field`
      (`mctx.structs.get(sname)` where `sname` comes from
      `Type::Named`) was already correct. Construction was not:
      `check_struct_construction` synthesized
      `Type::Named(s.decl.name, ...)` — the exporter's spelling — and
      enum construct did the same with `e.name`. The ModuleCtx splice
      installed under the local key but left `decl.name` (and every
      `Type::Named` inside the cloned declaration, and every typed
      method body) carrying the exporter's name. So `Duo(...).sum()`
      produced a value typed `Pair`, looked up `Pair` in a table that
      only held `Duo`, and reported the miss under the wrong spelling.
      An annotated receiver (`d: Duo` then `d.sum()`) already worked —
      its type came from decision 9's annotation path, not from
      construction.

      **Which change fixed the pattern half (evidence, not a guess).**
      A1 (`2fbd6f1`, decision 9) made `c: Hue` resolve to
      `Type::Named("Hue")`. Pattern checking compares the pattern's
      enum name against the *scrutinee type's* name
      (`variant_payload_types_for`), so the annotated shape
      (`fn pick(c: Hue): match c: case Hue.Red:`) started working as a
      side effect of A1 — nobody touched pattern code for aliases.
      Measured today: that shape checks clean; dropping `case Hue.Blue:`
      reports `match is not exhaustive: missing .Blue`. The
      construction-as-scrutinee shape (`match Hue.Red: case Hue.Red:`)
      still failed after A1/A2 with `expected a \`Color\` pattern, found
      \`Hue\`` — same construction bug as the method half — and is what
      A1b's original repro actually hit. Method lookup was missed by A1
      because A1 only fixed *type annotations*; construction and the
      splice's internal spellings were never on its list.

      **The fix (decisions 86–88): one spelling, at the splice.** When
      `local != target_name`, re-key the cloned `DeclStruct` /
      `DeclEnum` at the ModuleCtx splice and the cloned `TypedStruct` at
      the TypedProgram splice; construction reads the local lookup key.
      Method lookup is unchanged — it already keyed on `Type::Named`.
      No fallback that tries both spellings.

      **Shapes audited, each run and read.** `read self` under alias;
      associated fn under alias (return type re-keyed, so
      `Cnt.zero().get()` chains); `mut self` under alias; `take self`
      under alias; annotated receiver (already worked, still works);
      construction-as-scrutinee enum pattern; annotated enum pattern;
      absent method naming `Duo`; non-aliased `Pair(...).sum()`
      unchanged. Settle values pin correctness, not mere acceptance:
      method golden `217`, pattern golden `210`.

      Goldens: `check-import-alias-method` (`Arg label=settled
      value=217`), `check-import-alias-pattern` (`value=210`),
      `err-import-alias-method-missing`. Clause
      `sema.names.imported-types` extended (decision 89).

      **Found in passing, not fixed.**

      1. **`lower` cannot resolve an imported struct's methods** (aliased
         or not). `lower::resolve_fn` looks only at `prog.structs`, not
         `prog.imported.structs` — the parallel of what A2 fixed for
         `variant_index` / enums. A non-folded call
         (`fn add(p: Pair) -> u32: return p.sum()` then
         `add(Pair(a=1,b=2))` inside a `@test`) evaluates at comptime
         but `--stage=mwir` reports `error[unimplemented]: lowering
         calling a callee not resolvable at lowering time (an unresolved
         generic instantiation)`. Wrong cause; named, fail-closed.
         Comptime-folded consts hide it (the mwir is just `ConstInt`).
         Same class as A1b's eval-side hole; lower's struct half was
         never closed. Not DD's — DD's goldens evaluate at comptime.

      2. **An imported free fn whose body names the exporter's type by
         the unbound spelling is still A1b's `unresolvable` boundary**
         when only the alias is imported. Re-keying covers the spliced
         *struct*'s own body; it does not rewrite a sibling imported
         fn that spells `Pair` while the importer only bound `Duo`.
         Pre-existing A1b decision 15; not opened here.
- [x] EE. **The backend sees the import closure, and `diff-eval` stops
      hiding that it does not** (decisions 90–99). Landed 2026-07-25.
      Added to this plan by the orchestrator; executed by its agent.

      **Measured at `ab85f36`**, three shapes, all from ordinary source
      through a project root, all with a working comptime tier:

      | Shape | `wrela test` | `--stage=mwir` / `--stage=asm` before | after |
      | --- | --- | --- | --- |
      | imported struct's **method** (`p.sum()`) | ok | `error[unimplemented]: lowering calling a callee not resolvable at lowering time (an unresolved generic instantiation) not implemented yet` | lowers; Call key=`Pair.sum` |
      | imported plain **fn** (`twice(21)`) | ok | the same message | lowers; Call key=`twice` |
      | imported struct's **fields** (`p.a + p.b`) | ok | `error[unimplemented]: internal error: struct \`Pair\` not found` | lowers; Project fields |

      Three separate defects in one hole. The first two **failed closed
      but lied**: the cause was not a generic instantiation, it was that
      `lower::resolve_fn` never consulted `TypedProgram::imported`. The
      third was an `internal error:` reachable from ordinary source —
      the exact shape A1b fixed for the evaluator, never for lower. A2
      wired *enums* into `lower`/`flowwir_lower` (decision 82); structs
      and fns were left.

      **Half 1 (decisions 90–91, 96, 98–99).** Lookups read own-else-
      imported; `lower_program`/`flowwir_lower::lower_program` also emit
      from `imported` under the local spelling, without merging into the
      declaration tables (decision 13 stands). Misses name the import
      closure or `unresolvable`, never \"generic instantiation\" for a
      plain name, never `internal error: struct \`X\` not found` from
      ordinary source. Cycles stay free: read-only reuse of finished
      output. Fuzz lanes stay single-file (recorded, not built).

      **Half 2 — preferred, not minimum (decisions 92–95, 97).**
      `typecheck_for_diff_eval` loads the closure the way `bin/wrela.rs::
      check_closure` / `produce_report_and_image` do (parallel copy, not
      a third sharing convention). `build_runtime_test_image` takes the
      whole closure's modules for `merge_layout_ctx`. Summary line gains
      `import-skips`. Before EE: `37 test(s) agree across 8 case(s), 5
      lowering-skips, 6 exhaustive-skips, 1 quota-skips` — and every
      import-bearing case was a silent `continue` with no tally.
      After: `41 test(s) agree across 9 case(s), 13 lowering-skips, 6
      exhaustive-skips, 1 quota-skips, 0 import-skips`. The +4 agreeing
      tests are `check-import-lower`'s four `@test`s — the first time
      the stdlib's guest path has an oracle at all for an imported
      struct/fn. Cases silently skipped before that now contribute, by
      name: `check-import-alias-method` (5), `check-import-alias-pattern`
      (3), and `boot-receipt-handoff` / `err-boot-receipt-handoff` (now
      print `no comptime-legal @test` instead of vanishing).

      **Shapes audited, each run and read.** The three `/tmp/wa2/`
      repros at both `--stage=mwir` and `--stage=asm` (before and after);
      unaliased fields/method/fn through the new golden; aliased method
      and pattern cases through `diff-eval` (see finding below);
      `check-stdlib-loaded`'s enum path still dumps; a type-bearing
      import cycle still checks (A1b's golden, untouched).

      **Found in passing, not fixed.** An aliased imported type's
      layout is invisible to codegen: `merge_layout_ctx` keys by the
      exporter's AST name, the typed tree uses the local alias, so
      `--stage=asm` and `diff-eval`'s guest build fail closed with
      `codegen for unknown struct/enum \`Cnt\`/\`Hue\` in this layout
      context`. Newly included by half 2; counted as lowering-skips,
      not divergences. Items B–I will hit this the moment a stdlib type
      is imported under an alias into guest code. Also still standing:
      no fuzz lane constructs a multi-module closure (A1b's finding;
      lanes owned by the post-roadmap coverage pass).

      Goldens: `check-import-lower` (`mailbox=71142`; four `@test`s;
      mwir + asm dumps). Clauses `compiler.lower.import-closure` and a
      note on `compiler.eval.matches-backend`.
- [x] FF. **An aliased imported type reaches codegen** (orchestrator,
      2026-07-25, verifying item EE; decisions 100–104). Landed
      2026-07-25. The **third and last layer** of one bug: decision 9
      chose the local (possibly-aliased) spelling as canonical, and
      each layer that keyed on the exporter's spelling instead was
      found separately, after the one above it was fixed.

      | Layer | Keyed on the exporter's name | Fixed by |
      | --- | --- | --- |
      | sema: annotations | — | A1 (decision 9) |
      | sema: construction, patterns, method lookup | yes | DD (decisions 86–88) |
      | `lower`/`flowwir_lower`: fns, structs, fields | yes | EE (decision 90) |
      | `layout`/`codegen`: `merge_layout_ctx` | yes | this item (decision 100) |

      **Measured at `f3a8fb6`, reproduced before the fix.** Unaliased
      `/tmp/wa2/src/app/lowerhole.wr` at `--stage=asm` emits
      `Fn key=Pair.sum frame=64 bytes` and a real `bl <Pair.sum>`. The
      same shape under `import Pair as Duo` (`/tmp/wa2/src/app/alias3.wr`)
      reported `error[unimplemented]: codegen for unknown struct/enum
      \`Duo\` in this layout context is not implemented yet`. DD's own
      goldens matched: `check-import-alias-method` → `Cnt`,
      `check-import-alias-pattern` → `Hue`. `xtask diff-eval` before:
      `41 test(s) agree across 9 case(s), 13 lowering-skips, 6
      exhaustive-skips, 1 quota-skips, 0 import-skips` — the two alias
      cases among the lowering-skips (EE decision 95 left that visible).

      **Root cause, confirmed.** `layout::merge_layout_ctx` builds one
      `mwir::LayoutCtx` per module from that module's own AST decls, then
      unions them. The exporter contributes under `Pair`/`Counter`/
      `Color`; the importer's typed tree and MWIR (after DD/EE) use
      `Duo`/`Cnt`/`Hue`. `mwir::size_of` / codegen look up the typed
      spelling and miss. Unaliased worked because local == exporter.

      **The fix (decisions 100–101): one spelling, at the place the
      layout context is built.** After the own-decl merge,
      `install_aliased_import_layouts` walks `imported_type_targets` and,
      when `local != target_name`, copies the exporter's layout entry to
      the local key and re-keys self-`Type::Named` via
      `types::rekey_type_name`. Consumers still look up one name. No
      `get(local).or_else(|| get(exporter))` anywhere.

      **Fourth-layer audit (decision 104), method and result.** Grepped
      and read the consumers below codegen that touch type-name keys or
      `LayoutCtx`: `report.rs` (no struct/enum layout lookup — renders
      the image graph), `verify_section_sizes` (section table
      base/size/overlap only; no type names), `placement.rs`
      (`mwir::size_of` on pool payloads through the same `LayoutCtx`),
      `mwir::size_of` itself (the miss site; now finds the local key),
      boot/`diff-eval` harnesses and the two VMM unit call sites (all
      call `merge_layout_ctx`). **None** remain keyed on the exporter's
      spelling while the typed tree uses the alias. An audit that finds
      nothing is a result when it was really performed.

      **`diff-eval` after:** `49 test(s) agree across 11 case(s), 5
      lowering-skips, 6 exhaustive-skips, 1 quota-skips, 0 import-skips`.
      The two alias cases are agreements (5 + 3); lowering-skips dropped
      13 → 5.

      **Shapes audited, each run and read.** `/tmp/wa2` alias3 and
      lowerhole at `--stage=asm` (before and after); both alias goldens
      at `--stage=asm` (Cnt methods with real `bl <Cnt.bump>` /
      `bl <Cnt.get>` / `bl <Cnt.into_n>`; Hue match frames sized);
      unaliased `check-import-lower` still dumps; unit
      `merge_layout_ctx_keys_aliased_imports_under_local_spelling`.

      **Found in passing, not fixed.** No fuzz lane constructs a
      multi-module closure (A1b; coverage pass). No genuine `err-*`
      residue (decision 103). Items B–I will now reach guest code under
      an alias without rediscovering this hole — that was the point.

      Goldens: `check-import-alias-method`/`check-import-alias-pattern`
      gain `expected/asm.txt` and become `diff-eval` agreements.
      Clause `sema.names.imported-types` extended (decision 102); note
      on `compiler.lower.import-closure` that decision 95's residue is
      closed.
- [x] B. **`from` conversion and `deriving(From)`** (05 §8, 02 §7.4;
      decisions 105–108). Landed 2026-07-25. Ordered before `Format`
      because `Format`'s error paths use it; after BB because B builds
      conversion on top of a `?` that now reads the operand's type.

      **The two gaps BB measured, both closed.** (1) `fn from(...)` was
      `error[parse]: keyword \`from\` cannot be used as a name`, so
      `bodies::try_from_conversion`'s explicit-assoc-fn arm was dead
      code — a doc-vs-code disagreement where the doc wins (BB finding
      3). (2) `deriving(From)` produced nothing, so every `?` needing a
      conversion failed closed in both tiers (`err-try-from-underived`,
      BB decision 61).

      **Half A (decision 105).** `parser::expect_fn_name` accepts the
      keyword `from` after `fn` and nowhere else. Established by reading
      `parse_imports` (statement-initial `from` / `pub from`) against
      `parse_fn_item` (name only after `fn` consumed): the positions do
      not overlap, so imports stay unambiguous. Other keywords remain
      rejected. Roundtrip prints `fn from(...)` and reparses it.

      **Half B (decision 106).** No TypedFn is synthesized. On `Try`'s
      Err path: if `resolve_fn` finds an explicit associated `from`, call
      it (`take` param, no write-back); else wrap structurally — enum
      tag 0 with the source as sole payload, or a one-field struct
      aggregate. Same rule in `eval::interp`, `lower::lower_try_sync`,
      and `flowwir_lower::lower_try_check`. `mwir.txt` pins
      `MakeEnum tag=0 payload=[t6]` for deriving and
      `Call key=WrapError.from` for explicit.

      **What the docs decided, quoted.** Single-field: 05 §8
      "`deriving(From)` requires exactly one variant/field"; 02 §7.5
      "for one-variant/one-field shapes, `From`". By `take`: 02 §7.4
      table "`TargetError.from(take e)`"; 05 §8
      "`fn from(take source: Source) -> Self`". Composition with
      teardown: 02 §8.2 "`Err(e)?` runs lexical teardown, applies the
      target's `from` conversion if needed, and returns from the
      enclosing function" — teardown still runs first (BB's defer
      shape); conversion is the next step on the Err payload.

      **Shapes audited, each run and read.** Enum `deriving(From)` with
      `?` converting `LoadError` → `WrapError` (fail and Ok paths);
      explicit struct `fn from` that transforms `Bad(7)` → `code=107`;
      two-field struct `deriving(From)` rejected; two-variant enum
      `deriving(From)` rejected (sibling message, not a separate
      golden); `check-bodies`'s existing `convert_wrapped` still
      typechecks (no `@test` added — decision 108); `?` without
      conversion (BB's `check-try-*`) unchanged.

      **`err-try-from-underived` retired** (decision 107) into
      `check-deriving-from` — BB's intended signal.

      **Why prior oracles missed the gap.** Sema already accepted
      `deriving(From)` and hung a conversion key on `Try`; the
      evaluator and both lowerers then refused by name. `diff-eval`
      never saw disagreement because both tiers refused the same way
      (and no positive `@test` crossed a converting `?`). The parser
      refusal of `fn from` was a separate, earlier layer.

      Goldens: `check-deriving-from` (`converted=1000`, `ok=7`),
      `check-from-explicit` (`converted=107`),
      `err-deriving-from-two-fields`. Clause `library.from.conversion`;
      notes updated on `comptime.eval.try-propagation` (gap closed) and
      `syntax.diagnostics.parse-errors` (fourth keyword exception).

      **Found in passing, not fixed.**

      1. **Struct `deriving(From)` is untested as a positive case.** The
         wrap path handles it (`Value::Struct([e])` /
         `MakeAggregate`), and `err-deriving-from-two-fields` rejects
         the bad shape, but no golden pins a successful struct-derived
         conversion by value. Enum deriving + explicit struct `from`
         cover the two live surfaces; a one-field struct deriving case
         would be a cheap follow-up, not a blocker.
      2. **`?` + conversion + `defer` together** is still uncovered in a
         golden (BB audited defer without conversion; B audits
         conversion without combining them). 02 §8.2's order is
         teardown-then-convert; both halves exist, the composition is
         not pinned.
      3. **Fuzz parser/eval lanes** still seed only single-module
         inputs (A1b / coverage pass).

      **What C and D inherit.** `Format`/`deriving(Format)`/f-strings
      can now fail through a converted error type: an error path that
      needs `from` is no longer a named gap. They do not inherit a
      Format implementation, a bound, or f-string lowering — those are
      still dead sites in `sema/bodies.rs`.
- [x] B2. **An `enum` may declare methods and associated fns**
      (02 §5 / §7.5, 05 §6; decisions 121–135). Landed 2026-07-25.
      Ordered before C because Format-on-an-enum needs a method surface
      to declare into; before B3 because B3 generates into this surface.

      **Measured at `964bee9` / verified at `f75f625`.** An enum body
      accepted variants and nothing else:

      ```
      enum Color:
          Red
          Blue
          pub fn code(read self) -> u32: ...
      → error[parse]: keyword `pub` cannot be used as a name at 7:5
      ```

      Without `pub`: `keyword \`fn\` cannot be used as a name`. The
      variant-list parser was reading the next variant name and meeting
      a Keyword — a typo-shaped diagnostic for a missing language
      surface. Structs were unaffected (`/tmp/wprobe/b4.wr`:
      `Wrap.from(take s)` → 305). Probe `/tmp/wprobe/b5.wr` is the after
      oracle: `Color.Red.code()` → 1, through `--stage=mwir` /
      `--stage=asm`.

      **Why the docs decide this.** 05 §6: "Types implementing the
      static Format contract **declare** ..." — a declaration is a
      method surface. 02 §7.5: "`deriving(...)` on a struct **or enum**
      generates **methods**". Decision 106's "enums carry no methods"
      premise was the implementation's limit, not the doc's; B2 removes
      it for the surface, leaving B3 to generate into it.

      **Parser ambiguity (decision 121), established by reading the
      parser.** `parse_variant` starts with `expect_ident`. A method
      begins with Keyword `fn`/`pub`/`async`. Those token kinds never
      overlap, so `parse_indented_enum_body` branches on
      `at_enum_method_start` before falling through to `parse_variant`.
      `init`/`pool`/field-shaped `name: Type` refuse by name
      (decisions 126).

      **Receivers (decision 122).** `read self`, `mut self`, `take self`,
      and associated fns — all three modes structs already support, by
      02 §5.1 unchanged. `mut self` may reassign the whole sum. No mode
      fails closed; inventing an enum-only restriction would be the
      wrong kind of dumb.

      **Visibility (decision 123).** Methods may be `pub`. The type's
      own `pub` gates import (`err-import-type-not-pub`); once imported,
      methods resolve the way struct methods already do.

      **Five-consumer audit (decisions 124–129, 128), each run and
      read.**

      | Consumer | What it needed | How verified |
      | --- | --- | --- |
      | sema resolution | `EnumInfo::{method,assoc_fn}`; assoc before variant on `Type.name` | `b5.wr` typechecks; wrong name says "no variant or associated function" |
      | ModuleCtx splice | `rekey_decl_enum_name` on alias | import + alias goldens |
      | TypedProgram splice | enums body-bearing; `rekey_enum_name` | alias settle `1222` at comptime |
      | local-spelling re-key | DD/FF's rule, for enums | `Hue.code` / `Hue.flip` in mwir/asm |
      | `lower`/`flowwir_lower` | `lower_enum_members`; `resolve_fn` tries enums | `Fn key=Cell.value` / `Hue.code` in mwir; `bl` in asm |
      | `layout`/`codegen` | inherit via MWIR keys; `mut self` write-back must treat an enum as a whole aggregate (`codegen::write_back_mut_self` looked only in `structs` — `internal error: unknown struct \`Cell\``) | `--stage=asm` + `diff-eval` agree on all three positive goldens (65/16; lowering-skips stayed 5) |

      **Shapes audited, each run and read.** Local four-mode settle
      (`check-enum-methods`, `217`); import (`check-import-enum-method`,
      `12`); alias four-mode (`check-import-alias-enum-method`,
      `1222`); `init`/`field`/`pool` refusals by name; `b4.wr` still
      green; `b5.wr` after → 1 through asm.

      **Why prior oracles missed the gap.** The parser refused before
      any later stage could see an enum method, and the diagnostic
      looked like a typo. No golden spelled `fn` inside an enum body.
      `diff-eval` never disagreed because nothing reached either tier.
      Decision 106 recorded "enums carry no methods" as a premise of
      the deriving wrap — which made the missing surface look settled.

      Goldens: `check-enum-methods` (`217`), `check-import-enum-method`
      (`12`), `check-import-alias-enum-method` (`1222`),
      `err-enum-init`, `err-enum-field-member`, `err-enum-pool-member`.
      Clause `sema.types.enum-methods`.

      **Found in passing, not fixed.**

      1. **Decision 106's note still says "enums carry no methods"
         as a deriving premise.** Historically true when written; B2
         supersedes the surface half. B3 must update that note when it
         generates the real `from`. Not rewritten here — decisions are
         frozen; the supersession is recorded in decisions 121/132.
      2. **`Outer.from(take i)` on an enum with no `from` now says
         `no variant or associated function`** (decision 130), which
         is better than "no variant", but B3's measured
         `has no variant \`from\`` for a deriving-only enum is a
         different residual — deriving still does not put `from` in
         the method table.
      3. **Fuzz parser/sema/lower lanes still seed only single-module
         inputs** (A1b / coverage pass).
      4. **Struct `deriving(From)` still has no positive value golden**
         (B finding 1) — unchanged.

      **What B3 and C inherit.** B3 inherits a real method surface on
      enums: generating `from` is now "emit a TypedFn into
      `TypedEnum::assoc_fns`", not "invent the surface". It does **not**
      fall out trivially — decision 106's structural wrap is still the
      live path for `deriving(From)`, and flipping it is B3's one
      variable. C inherits the surface Format must declare into on an
      enum; it does not inherit Format, bounds, or f-strings.
- [x] B3. **`deriving(From)` generates a real `from`, not a structural
      wrap** (decisions 137–146). Landed 2026-07-25. Depends on B2 for
      the enum half; supersedes decision 106.

      **The defect, measured at `964bee9` and re-verified before the
      edit.** Item B made `?`'s conversion work in both tiers, but
      decision 106 wrapped structurally on the Err path instead of
      generating the method 02 §7.5 / 05 §8 describe. Hand-written
      `Wrap.from(take s)` worked; `deriving(From)` on a struct reported
      `type \`Wrap\` has no associated function \`from\`` (`/tmp/wprobe/b6.wr`).
      Enum deriving was not callable either. §7.4's desugaring was a
      fiction.

      **What landed.** `declare_struct`/`declare_enum` inject a DeclFn
      `from(take source: Source) -> Self` when `deriving(From)` is
      present (conflict if an explicit `from` already exists — decision
      137). `build_module_ctx` appends the matching FnItem whose body is
      ordinary construction (`Self(field=source)` / `Self.Variant(source)`).
      Body-checking yields a TypedFn in `assoc_fns` — B2's surface, not a
      new map. `try_from_conversion` now only looks up `assoc_fn("from")`
      (the deriving structural arm is gone). Eval / lower / flowwir_lower
      Call that key; `derived_from_wrap` and the MakeEnum/MakeAggregate
      fallbacks are deleted. **The structural wrap is gone** in all three
      tiers.

      **Shapes audited, each run and read.** Struct deriving `?` and
      direct call (`check-deriving-from-struct`: 1000 / 7 / 2505); enum
      deriving `?` and direct call (`check-deriving-from`: 1000 / 7 /
      2505); explicit `from` unchanged (`check-from-explicit`: 107);
      plain import (`check-import-deriving-from`: 404); alias with asm
      (`check-import-alias-deriving-from`: 504, `bl <Box.from>`);
      conflict (`err-deriving-from-conflict`); two-field struct and
      two-variant enum still rejected; `/tmp/wprobe/b6.wr` now passes
      with D=305.

      **`diff-eval`:** before `72 test(s) agree across 17 case(s), 5
      lowering-skips, 6 exhaustive-skips, 1 quota-skips, 0 import-skips`;
      after `78 test(s) agree across 20 case(s), 5 lowering-skips, 6
      exhaustive-skips, 1 quota-skips, 0 import-skips` (+6 tests / +3
      cases from the new value-bearing goldens). Existing conversion
      settles unchanged.

      Goldens: `check-deriving-from` (gains DIRECT),
      `check-deriving-from-struct`, `check-import-deriving-from`,
      `check-import-alias-deriving-from` (+asm),
      `err-deriving-from-conflict`, `err-deriving-from-two-variants`.
      Clause `library.from.conversion` updated.

      **Found in passing, not fixed.**

      1. **Aliasing both the deriving type and its source field type**
         (`Wrap as Box, Src as Item`) fails: the spliced DeclFn's
         parameter stays `Src` while the importer's local spelling is
         `Item`. Repro: `/tmp/wprobe` shape with both aliases. FF re-keys
         Self; it does not re-key peer type names in the same Decl.
         Workaround used in the alias golden: alias only Wrap, import
         Src under its exporter spelling.
      2. **`?` + conversion + `defer` together** still uncovered (B
         finding #2).
      3. **Fuzz lanes** still single-module (A1b / coverage pass).

      **What C and D inherit.** Format error paths that need `from` now
      reach a real method, including under import/alias. They do not
      inherit Format, bounds, or f-string lowering.
- [x] GG. **The local-spelling re-key covers a whole signature, not just
      the owning type** (orchestrator, 2026-07-25, verifying item B3;
      decisions 147–154). Landed 2026-07-25. The fourth finding in the
      aliased-spelling family, and the first that is not a *layer* — it
      is a **breadth** gap inside one layer.

      **Measured at `c4f7fc7`**, reproduced before the fix against
      `/tmp/wa4/` (and again here at `49956ae`):

      | Import shape | Before | After |
      | --- | --- | --- |
      | owner aliased, peer **not** (`/tmp/wa4` c) | works | unchanged |
      | peer aliased, **parameter**, generated `from` (a) | `expected \`Src\`, found \`Item\` at 8:23` | evaluates; `D == 305` |
      | peer aliased, **parameter**, hand-written (b) | same at 8:24 | evaluates; `D == 305` |
      | peer aliased, **return** (d) | `expected \`In\`, found \`Inner\` at 8:19` | evaluates; `D == 305` |

      Diagnostics named types the author never wrote — in the parameter
      case `Src`, which is not in scope under that name. The two
      positions reported the mismatch in opposite directions: one
      missing substitution, not two wrong ones. Not deriving-specific:
      hand-written `Hand.make` failed identically to generated
      `Wrap.from`.

      **Root cause.** `types::rekey_decl_struct_name` /
      `typed::rekey_struct_name` (and the layout install's single-pair
      call) remapped only `target_name → local` for the declaration
      being spliced. Peer names in the signature stayed exporter-spelled.
      Unaliased worked because local == exporter for every name, so the
      missing peer rewrite was a no-op.

      **The fix (decisions 147–150): one substitution, one place.**
      `imports::alias_subs_for_exporter` builds the non-identity map for
      the exporting module from the importer's bindings. Both splices
      and `install_aliased_import_layouts` apply it with
      `rekey_*_names` / `rekey_type_names` — one simultaneous pass per
      `Type::Named`, including free fns and consts. No
      `get(local).or_else(|| get(exporter))`.

      **Signature-shape audit, each run and read** (against
      `/tmp/gg-audit/` and the new goldens):

      | Shape | Ran | Saw |
      | --- | --- | --- |
      | parameter (hand-written) | `app/param.wr` / golden-param | check+test ok; `305` |
      | return | `app/retpos.wr` / golden-return | check+test ok; `305` |
      | generic argument `Box[Src]` | `app/gen2.wr` / golden-generic | check+test ok; `406`; asm has `peel_box` |
      | nested `Box[Box[Inner]]` | `lib.nest` + `app/nest.wr` | `error[unimplemented]: this generic argument shape is not checked yet` — pre-existing, not GG |
      | struct field (`Wrap.inner: Src`) | `app/field.wr` | check+test ok; `608` |
      | const type (`SRC_SEED: Src`) | `app/konst.wr` | check+test ok; `709` |
      | mixed (owner aliased, peer not) | `app/mixed.wr` / wa4 c | check+test ok; `305` |
      | free fn peer param/ret | `app/freefn.wr` | check+test ok; `808`; asm `double` |
      | true adversarial swap | `app/swap_check.wr` / golden-swap | typecheck clean; comptime eval withheld (A1b) |
      | dual-alias asymmetric | golden-dual | `403711`; asm `Grip.make` + `Out.peel` |
      | unaliased peer | golden-peer | `305` |

      **Is the aliased-spelling family now closed?** Closed for
      **breadth within a declaration's type names at the splice**: every
      construction of `Type::Named` reached by `rekey_decl_type` /
      `typed::rekey_type` (Named + nested targs, Array, Tuple, Option,
      Result, Own, Static, Fn) is rewritten from the same map, and each
      signature shape above was run. Not closed for (a) A1b's body
      evaluation under name shadow (true swap comptime — pinned as
      `err-import-alias-sig-swap-comptime`), (b) nested generic *type
      arguments* that still hit `this generic argument shape is not
      checked yet`, (c) an imported method body that names a
      same-module generic the importer did not import
      (`internal error: struct \`Box\` not found` on `Cell.pack` /
      `wrap_box` when only the peer+owner are imported — A1b-class,
      should be named unresolvable rather than `internal error:`).
      Established by grepping `rekey_` / `Type::Named` on the import
      path, running the shape table above, and confirming
      `check-import-*` unaliased goldens still dump.

      **`diff-eval`:** before `78 test(s) agree across 20 case(s), 5
      lowering-skips, 6 exhaustive-skips, 1 quota-skips, 0 import-skips`;
      after `84 test(s) agree across 24 case(s), 6 lowering-skips, 6
      exhaustive-skips, 1 quota-skips, 0 import-skips`. The +6 agreements
      are param/return/dual(3)/peer; the +1 lowering-skip is
      `check-import-alias-sig-generic` (`Box[Item]` has no layout
      instantiation — comptime agrees, guest fails closed by name).

      Goldens: `check-import-alias-sig-param` (`value=305`, asm),
      `check-import-alias-sig-return` (`value=305`),
      `check-import-alias-sig-generic` (`value=406`),
      `check-import-alias-sig-swap` (true name-swap typecheck),
      `check-import-alias-sig-dual` (`value=403711`, asm — cannot pass
      by symmetry: `403*1000+711` vs swapped additives/inputs),
      `check-import-sig-peer` (`value=305`),
      `err-import-alias-sig-swap-comptime` (A1b withhold). Clause
      `sema.names.imported-types` extended (decision 152).

      **Found in passing, not fixed.**

      1. **True adversarial swap cannot evaluate at comptime** (A1b
         decision 15). Exact repro: `/tmp/gg-audit/src/app/swap.wr` or
         `err-import-alias-sig-swap-comptime` —
         `error[comptime]: \`Inner\` is imported from module \`lib.pk\`,
         which declares \`Src\` — a name module \`app.main\` resolves
         differently...`. Typecheck of the same imports is clean
         (`check-import-alias-sig-swap`). Inherent to shadowing both
         exporters' names; not a GG regression.

      2. **Nested generic type arguments still unimplemented.**
         `Box[Box[Inner]]` as a field type:
         `error[unimplemented]: this generic argument shape is not
         checked yet at L:C`. Exact repro:
         `/tmp/gg-audit/src/lib/nest.wr` + `src/app/nest.wr`.

      3. **Imported body naming an unimported same-module generic is
         an `internal error:`.** `wrap_box` / `Cell.pack` constructing
         `Box[Src]` when the importer bound `Src`/`Cell` but not `Box`:
         `error[comptime]: internal error: struct \`Box\` not found`.
         Exact repro: `/tmp/gg-audit` before `gen2` (import peer+owner
         only, call `wrap_box`). A1b-class miss that should be
         `unresolvable` by name; house rule says `internal error:` from
         ordinary source is a bug.

      4. **No fuzz lane constructs a multi-module closure** (A1b;
         coverage pass). Unchanged.
- [x] HH. **An importer can use a type it did not import the name of**
      (orchestrator, 2026-07-25, verifying item GG; decisions 155–164).
      Landed 2026-07-25. The fifth and — on the evidence of how it differs
      from the other four — the *general* finding in this family. GG
      closed the re-key's breadth across a signature; this is about the
      **type universe** the importer gets at all.

      **Measured at `08dc762`**, reproduced before the fix against
      `/tmp/wa5/` (and again here at `4bf7470`):

      | Shape | Before | After |
      | --- | --- | --- |
      | import only `Maker`; `m.build().n` (`/tmp/wa5`) | `error[type]: type \`Box\` has no field \`n\`` | evaluates; `D == 305`; asm `Maker.build` |
      | import `wrap_box`/`Src`/`peel_box`, not `Box` (GG #3) | `error[comptime]: internal error: struct \`Box\` not found` | evaluates; `D == 406` |
      | chain `A→B→C`, only `A` imported | `type \`C\` has no field \`n\`` | evaluates; `D == 215`; asm `A.make`/`B.get` |
      | reachable as field + generic arg | same empty-universe miss | evaluates; `D == 12013` |
      | `Maker as Builder`; reachable `Box` | same miss under alias | evaluates; `D == 305`; asm `Builder.build` |
      | reachable non-`pub` `Hidden` as value | same miss | evaluates; `D == 305` |
      | `from lib import Hidden` (not pub) | `\`Hidden\` ... is not \`pub\`` | unchanged (`err-import-type-not-pub`) |

      **The two exits, distinguished.** (1) Sema field lookup on a value
      typed as a reachable-but-unimported name: `mctx.structs.get("Box")`
      misses and `bodies::check_field` reports the false
      `type \`Box\` has no field \`n\``. (2) Comptime evaluation of an
      imported body that *constructs* that type (`wrap_box` → `Box[Src]`):
      typecheck of the importer can pass (no field access on `Box`), then
      `eval::interp::resolve_struct_by_type` misses and
      `abandon_missing` yields `internal error: struct \`Box\` not found`
      — a bug by house rule. Same hole, two consumers; both closed by the
      reachability closure.

      **Root cause.** The ModuleCtx / TypedProgram splices installed one
      entry per *import binding*. A type that appeared only in an imported
      declaration's signature or body was absent from the importer's
      tables, so every consumer that keyed on `Type::Named` for a value
      the importer already held — field lookup, comptime construction,
      lower — saw an empty universe.

      **The docs decide it.** 02 §2: "Imports are **compile-time name
      bindings**". A binding is what you need to **spell** a name. `b.n`
      never spells `Box`. Requiring the import would make
      `pub fn build(read self) -> Box` a declaration whose caller cannot
      use its result without ceremony the language asks for nowhere else.
      "Declarations are module-private unless `pub`" governs the
      *exporter's* declarations (the import act), not the importer's
      inference over a value a pub API already handed across.

      **The fix (decisions 155–160): close the universe over reachability.**
      After each explicit-import splice, `close_mctx_type_reachability` /
      `close_typed_type_reachability` walk imported decls' signatures,
      copy missing struct/enum entries from the *defining* module's
      finished output (chasing through the origin's own import bindings
      so `A→B→C` still finds `C`), and install them under the
      importer-facing spelling. Decision 13 / EE 90 stand: reachable
      entries live only in the importer's lookup tables
      (`mctx.structs` / `imported.structs`), never merged into the
      declaration tables that `lower` emits from for "what this module
      declares". `unresolvable` skips names the closure installed, so
      eval cannot prefer a miss note over the real decl. Cycles stay
      free: read-only reuse of finished output.

      **Privacy (decision 161).** Spelling / importing a non-`pub` type
      is still refused by name at `resolve_imports`
      (`err-import-type-not-pub`). Using a *value* of a reachable
      non-`pub` type is allowed — pinned by
      `check-import-reachable-private`. Rejected: pulling the exporter's
      whole world into `imported_type_shapes` (would let the importer
      spell names it never bound, and would break the not-pub golden if
      done without the import gate).

      **Aliasing (decision 162).** Aliasing cannot apply to a name the
      importer never wrote. `Maker as Builder` re-keys the owner;
      reachable `Box` keeps the exporter spelling. Pinned by
      `check-import-reachable-alias` (`Fn key=Builder.build`, settle
      `305`).

      **Shapes audited, each run and read.** `/tmp/wa5` at check / test /
      asm (before and after); GG #3 without alias (`wrap_box`/`Src`/
      `peel_box`); chain two modules deep; field + generic argument in
      the exporter; aliased owner; non-pub value use; `err-import-type-
      not-pub` still fires; existing `check-import-*` unaliased / alias
      goldens still dump.

      **Is the aliased/imported-names family closed now?** Closed for
      the *type-universe* question: every type reachable through an
      imported declaration's signature is present in the importer's
      ModuleCtx and `TypedProgram::imported`, at both comptime and guest
      tiers, including across a defining-module chase. Established by
      running the shape table above, grepping the two splice sites for
      the closure calls, and confirming `err-import-type-not-pub` plus
      the five new value-bearing goldens. **Not** closed for: (a) a
      reachable generic instantiation whose peer type is *aliased* by
      the importer (`Src as Item` + `wrap_box` without importing `Box`
      — instantiation keys stay exporter-spelled after the fn body is
      rekeyed; recorded below); (b) nested generic type arguments
      (`Box[Box[T]]`, GG #2); (c) no fuzz lane constructs a multi-module
      closure (A1b; coverage pass); (d) A1b decision-15 true-swap
      comptime withhold.

      **`diff-eval`:** before `84 test(s) agree across 24 case(s), 6
      lowering-skips, 6 exhaustive-skips, 1 quota-skips, 0 import-skips`;
      after `88 test(s) agree across 28 case(s), 7 lowering-skips, 6
      exhaustive-skips, 1 quota-skips, 0 import-skips`. The +4 agreements
      are type/chain/alias/private; the +1 lowering-skip is
      `check-import-reachable-field-generic` (`Box[Cell]` has no layout
      instantiation in the diff-eval guest build — comptime agrees and
      `--stage=asm` dumps; same class as GG's
      `check-import-alias-sig-generic`).

      Goldens: `check-import-reachable-type` (`value=305`, asm),
      `check-import-reachable-chain` (`value=215`, asm),
      `check-import-reachable-field-generic` (`value=12013`, asm),
      `check-import-reachable-alias` (`value=305`, asm `Builder.build`),
      `check-import-reachable-private` (`value=305`); boundary
      `err-import-type-not-pub` unchanged. Clause
      `sema.names.imported-types` extended (decision 163).

      **Found in passing, not fixed.**

      1. **Reachable generic + aliased peer leaves instantiation keys
         mismatched.** Exact repro: `/tmp/wa5-gg3` —
         `from lib.pk import Src as Item` + `wrap_box` + `peel_box`
         (no `Box`) → `error[comptime]: internal error: struct \`Box\`
         not found` while evaluating `wrap_box`. Same shape without the
         alias (`/tmp/wa5-gg3b`) evaluates to `406`. The fn body is
         rekeyed to `Box[Item]`; imported instantiations remain under
         `Box[Src]`. Distinct from HH's universe hole; a GG/instantiation
         residue. **Closed by item II.**

      2. **Nested generic type arguments still unimplemented** (GG #2).
         Unchanged.

      3. **No fuzz lane constructs a multi-module closure** (A1b;
         coverage pass). Unchanged — **closed by item II's
         `fuzz imports` lane.**

      **What C–I inherit.** `Instant.since(...) -> Duration` with only
      `Instant` imported will resolve `Duration` through this closure
      rather than rediscovering the empty-universe miss. They do not
      inherit Format, time, collections, or prelude deletion.

- [x] II. **No `internal error:` is reachable from ordinary source**
      (decisions 165–172). Landed 2026-07-25. The family's other ratchet
      item (AA locked the intrinsic *keys*; this locks the producer-bug
      *prefix*). Four reachable `internal error:` finds this milestone
      alone — A1b, EE, HH, HH#1 — every one multi-module; no prior fuzz
      lane constructs a multi-module closure, so every lane's
      per-iteration check had never once seen an import.

- [x] C1. **`String[..N]` as a real bounded-occupancy type** (02 §6.2;
      decision 201). Landed 2026-07-25. A first attempt at item C wrote
      the Format writer as a packed `u64`; orchestrator review rejected
      it as a hardcoded stand-in for this missing type (shape decision
      1 / that attempt's own carry-out finding #2) and the commit was
      dropped rather than superseded, so decisions 179–200 were never
      spent. C1 builds the type; C2 builds Format on it. Does **not**
      flip `comptime.fstring.bounds`.

      **Representation (decision 201).** `{ len: usize, data: [u8; N] }`
      as `8*(1+N)` slot-stride bytes — length word plus `N` byte slots.
      Rejected: dense `align8(8+N)`; rejected: fat pointer / heap.
      `Bytes[..N]` does not fall out of the same change; left
      `unimplemented_at`.

      **Surface.** Prelude name `String`; resolve `String[..N]`; text
      literal coercion when expected; `.len` / literal `[i]`; both
      tiers via `MakeAggregate`/`Project` (not `ConstText`). Exact
      `String[N]` and bare `String` refuse by name.

      Goldens: `check-string-bound` (settle `2104105`, mwir
      `String[..8]` + Project 0/1/2, image `settled=2104105`),
      `err-string-capacity`, `err-string-exact-form`. Clause
      `language.types.string-bound`.

      **Census (item II's ratchet, first live firing).** Rebasing C1
      onto II tripped `internal_error_site_count_matches_the_written_down_census`:
      `eval/interp.rs` 70 → 72, total 162 → 164. Both new sites are the
      `String[..N]` field/value invariants in `eval_expr`. Checked for
      reachability before bumping the count — `s.capacity` on a
      `String[..8]` is refused by sema first with ``error[type]:
      `String[..8]` has field `len` only; found `capacity` ``, so
      neither site is reachable from ordinary source. The ratchet did
      the job it was built for: it forced the look.

      **What C2 inherits.** A real `String[..N]` with guest layout and
      value pins — Format's writer target and f-string product type.

      **HH finding #1, closed by making it work (decision 165).** Repro
      at `/tmp/wa5-gg3` (`Src as Item` + `wrap_box`/`peel_box`, no
      `Box`): before, `error[comptime]: internal error: struct \`Box\`
      not found`; after, evaluates; `D == 406`; asm emits `wrap_box`/
      `peel_box`. Same shape without the alias was already `406`. Root
      cause: GG re-keyed bodies under the importer's aliases but left
      instantiation map keys (and `CalleeKey` instance keys) under the
      exporter spelling; `resolve_struct_by_type` recomputes the key
      from the re-keyed `Type::Named` and misses. Fix: apply the same
      `alias_subs_for_exporter` map to instantiation payloads *and*
      keys at the typed splice; re-key `FnInstance`/`MethodInstance`
      inside bodies; fold `imported.instantiations` into
      `enrich_layout_ctx_with_instantiations` so guest sizing sees
      `Box[Item]` (without that, comptime worked and asm failed with a
      wrong-cause sizing message). Rejected: fail-closed by name — the
      unaliased twin already works, and the re-key is the dumb small
      fix GG's rule already pointed at.

      **The ratchet (decisions 169–172) — three pieces, chosen by the
      acceptance test below.**

      1. **`cargo xtask fuzz imports`** — fixed module shapes (comptime
         construct of an imported struct; imported fields+method;
         reachable-unimported; aliased peer + reachable generic;
         two-deep chain; aliased owner), filled from the seeded RNG,
         run through `check_program_typed` + `eval::run_tests` +
         `lower::lower_program`. Producer-bug prefix anywhere is a
         finding. Deep default 200_000; smoke 2×1_000 in `check`.
      2. **Census** — `internal_error_census.rs` locks count and
         per-file distribution (162 sites). Not a per-site
         "unreachable" table (decision 171).
      3. **Golden assertion** — no `expected/*.txt` may contain the
         producer-bug prefix.

      **Acceptance test — would the ratchet have caught each find?**

      | Find | Would `fuzz imports` have caught it? |
      | --- | --- |
      | A1b (`internal error: struct \`Pair\` not found` from comptime construction of an imported struct) | **Yes** — shape `import_shape_comptime_construct` is exactly that program. |
      | EE (`internal error: struct \`Pair\` not found` from lower on an imported struct's fields) | **Yes** — shape `import_shape_fields_and_method` imports a struct with fields+method and lowers the root. |
      | HH (`internal error: struct \`Box\` not found` from `resolve_struct_by_type` when an imported body constructs a reachable-unimported type) | **Yes** — shape `import_shape_reachable_unimported` (and the chain/alias-owner shapes). |
      | HH#1 (aliased peer + reachable generic; instantiation keys mismatched) | **Yes** — shape `import_shape_alias_peer_generic` is the `/tmp/wa5-gg3` repro with varying numerics. |

      The census alone would have caught **none** of the four (it locks
      growth, not reachability). The golden assertion would have caught
      none at discovery time (no golden pinned the bug yet); it stops a
      future `--update` from baking one in.

      **Goldens.** `check-import-reachable-alias-generic` (`value=406`,
      asm `wrap_box`/`peel_box`). Clause
      `compiler.diagnostics.no-reachable-internal-error` (decision 172);
      `sema.names.imported-types` extended for the golden.

      **`diff-eval`:** before `88 test(s) agree across 28 case(s), 7
      lowering-skips, ...`; after `91 test(s) agree across 31 case(s), 5
      lowering-skips, 6 exhaustive-skips, 1 quota-skips, 0 import-skips`.
      The +3 agreements / −2 lowering-skips are the new
      `check-import-reachable-alias-generic` plus the previously-skipped
      `check-import-reachable-field-generic` and
      `check-import-alias-sig-generic` — all three needed
      `enrich_layout_ctx_with_instantiations` over imported
      instantiations, which `diff-eval` now calls the same way
      `--stage=asm` already did. Remaining skips are unrelated
      (`?` on Option; struct-valued module const).

      **Found in passing, not fixed.** Nested generic type arguments
      (`Box[Box[T]]`, GG #2) still unimplemented. Format/time/
      collections/prelude deletion remain C–I. The other 161
      producer-bug sites stay as invariant assertions — converting them
      into diagnostics would hide real bugs.

- [x] JJ. **Reachability closure traverses enum variant payloads**
      (adversarial probe while verifying II; decisions 179–186).
      Landed 2026-07-25. HH closed the importer's type universe over
      reachability, but the typed half never saw enum variant payloads:
      `TypedEnum` stored only variant *names*, while Decl-side
      `collect_named_types_from_decl_enum` already walked payloads for
      ModuleCtx. A struct reachable only as `Good(Payload)` stayed
      outside `TypedProgram::imported`, so comptime evaluation of an
      imported body that constructed it refused a legal program.

      **Measured against `/tmp/wp5/p3`** (and the single-module twin):

      | Shape | Before | After |
      | --- | --- | --- |
      | `Res as R` + `make`; `match` binds `.Good(p)` → `p.n` (`/tmp/wp5/p3`) | `error[comptime]: \`Payload\` is declared in module \`lib.g\`, which module \`app.main\` does not import; ... (plans/M9.md item A1b)` | evaluates; `D == 406`; asm |
      | same program, one module | evaluates; `D == 406` | unchanged |
      | `Option[Inner]` returned from imported method, `Inner` never imported | already works (generic-arg edge) | unchanged |
      | body-only private helper (`err-import-comptime-private-decl`) | refuses, citing A1b | refuses, plain body-only wording |

      **Why this is a defect, not a limit.** (1) The single-module twin
      passes — a program legal in one module must not be refused merely
      for crossing a module boundary (HH's thesis). (2) The sibling
      `Option[Inner]` shape already works — the closure handled
      reachability through *generic arguments* but not through *enum
      variant payloads*; that asymmetry is an oversight. (3) The
      diagnostic blamed closed item A1b; HH superseded the universe
      question.

      **The fix (decisions 179–181):** `TypedEnum` carries
      `variant_payload_types: Vec<Vec<Type>>` (parallel to `variants`;
      empty for unit; named payloads flatten to field types);
      `collect_named_types_from_enum` and `rekey_enum_names` walk them;
      neighbouring edges (tuple / fixed-array / `own[P] T` / nested
      struct fields / `Good(Box[Payload])`) already ride
      `collect_named_type_names` once a payload is in the walk. Body-
      only private helpers remain refused, with a diagnostic that no
      longer cites A1b (decision 182).

      Neighbouring edges verified by hand after the fix: tuple payload
      (`Good((Payload, u32))`), fixed-array (`Good([Payload; 1])`),
      nested struct field (`Good(Outer)` with `Outer.inner: Payload`),
      and generic payload (`Good(Box[Payload])`) all evaluate. `own[P] T`
      in an enum payload is refused by the language itself (pool-binding
      check does not cover enum payloads) — not a reachability miss.

      **Goldens / fuzz / ledger.** `check-import-reachable-enum-payload`
      (`value=406`, asm). `fuzz imports` gains the enum-payload shape
      and a generic-payload neighbour (decision 183) — regression
      locks, not discovery. Clause `sema.names.imported-types`
      extended (decision 184). `err-import-comptime-private-decl`
      expected text updated.

      **Found in passing, not fixed (decision 185).** A free generic
      function cannot forward its own type parameter into a generic
      type (`fn wrap[T](take x: T) -> Box[T]` → `error[type]: generic
      parameter \`T\` requires a type argument`). Single-module as
      well. Methods on a generic struct that use Self's `T` work;
      associated constructors hit the generic-method boundary. Likely
      to block a natural writing of item F (collections) free helpers
      over `List[T]`; a method-shaped API on `List[T]` may still be
      attemptable. Not JJ's fix.

      **`diff-eval`:** before `91 test(s) agree across 31 case(s), 5
      lowering-skips, ...`; after `92 test(s) agree across 32 case(s),
      5 lowering-skips, 6 exhaustive-skips, 1 quota-skips, 0
      import-skips`. The +1 agreement is
      `check-import-reachable-enum-payload`. Census unchanged (162
      sites).

- [x] C2. **Format on top of C1** (05 §6, 02 §7.5; decisions 202–206).
      Landed 2026-07-25. Depends on C1.
      Writer writes into a `String[..N]` buffer; `max_formatted_len` is
      that bound. `deriving(Format)` handles structs with fields and
      unit enums. Empty-spec-only is the scope limit; typing the
      parameter `u64` was the rejected part, since 02 §1.1 writes it as
      spec *syntax* after a colon in `{expr:spec}`.

      **Why the first attempt was rejected, so it is not retried.** A
      `u64` writer cannot be summed into `String[..N]`, which 02 §1.1
      and 05 §6 both name as the f-string product type; a `Point`
      formatter could only `return 0x50` — the constant letter "P" —
      because writers deriving bytes from field values were refused as
      "dynamic"; and `deriving(Format)` refusing every fieldful struct
      refuses the reason deriving exists. That attempt's own carry-out
      named `String[..N]` as the missing type and then encoded around
      it, which is what shape decision 1 forbids. It was dropped from
      history rather than superseded, so its decisions 179–200 were
      never spent — JJ later reused 179–186, and C2 numbers its own
      from 202.

      **Contract (decision 202).** `max_formatted_len() -> usize`
      (associated) and `format(read self) -> String[..N]` (method),
      with `N == max_formatted_len()`. Empty-spec only. Rejected:
      runtime `spec: u64`; rejected: inventing `FormatSpec`.

      **Scalars (decision 203).** `u8`/`u16`/`u32`/`u64`/`usize`,
      signed peers, `bool`, `char` — `.format() -> String[..K]` with
      fixed `K`. Eval + `Inst::FormatScalar` in lower/codegen.

      **Concat (decision 204).** `String[..N] + String[..M] ->
      String[..N+M]` through eval and `Inst::StringConcat`. Text
      literals coerce to `String[..len]` in this position.

      **`deriving(Format)` (decision 205).** Fieldful structs generate
      `"Name(f1=" + self.f1.format() + ... + ")"`; unit enums match
      variant names. Payload enums refuse. Explicit-member conflict and
      `Secret` refusal kept (decision 206).

      **Census (item II).** No new `internal error:` producer sites —
      total stays 164. New `LowerError::internal` /
      `CodegenError::internal` *calls* reuse the existing format!
      sites; probed reachability: a scalar `.format()` whose result is
      not `String[..N]` cannot be produced by sema (it always sets
      `String[..K]` from `scalar_format_bound`), so the lower guard is
      unreachable from ordinary source. Same for codegen's non-scalar
      `FormatScalar` arm.

      Goldens: `check-format-bound` (settles above; mwir
      `FormatScalar`/`StringConcat`), `err-format-conflict`,
      `err-format-secret`, `err-format-payload-enum`,
      `check-import-deriving-format` / `check-import-alias-deriving-format`
      (`Tag(n=9)` → `8084097`). Clause `library.format.contract`.

      **What D inherits.** `format -> String[..N]`, scalar Format, and
      `String + String` — enough to sum f-string operand bounds into
      `String[..N]` without a further contract change.

- [ ] D. **f-strings** — the three dead sites in `sema/bodies.rs` come
      alive; `f"..."` sums its operands' bounds into `String[..N]`.
      **Flips `comptime.fstring.bounds`.** Depends on C2.
      Goldens: `check-fstring-bounds`, `err-fstring-unbounded-operand`,
      `err-fstring-secret` (once G decides `Secret`; otherwise the case
      is written against a type with no `Format` at all).
- [ ] E. **Time** (05 §5). `Duration` with checked constructors and
      `add`/`subtract`/`less_than`; `Instant` opaque and monotonic;
      `now()` as a sealed effect forbidden in comptime and ISR context
      and **recorded/replayed** — the recorder already has `ClockRead`
      from M6, so this item wires the stdlib surface onto an existing
      choice entry rather than inventing one.
      Goldens: `check-duration-arith`, `err-now-in-comptime`,
      `err-now-in-isr`, `boot-instant-monotonic`.

      **A confirmed doc-vs-code defect E must fix, not rediscover.**
      05 §5 says the `Duration` constructors `ns`, `us`, `ms`,
      `seconds`, `minutes`, `hours` are "ordinary phase-neutral
      functions." Today `ms` and `seconds` are **compiler intrinsic
      arms** in `sema/bodies.rs`. Ground-truth rule 1: the code is
      wrong. The set is also *incomplete* — `ns`, `us`, `minutes`,
      `hours` do not exist at all — which is the accretion signature
      item AA exists to stop: someone added the two names a golden
      needed. E deletes those two intrinsic arms and adds six ordinary
      wrela functions in `stdlib/core/time.wr`. **This is the intrinsic
      surface getting smaller, which is the point** — E is the one
      removal M9 has already identified, and AA's written-down list
      shrinks by two in the same commit.

      **Do not extend this to `now` or `wake`.** `now()` is a sealed
      recorded effect (05 §5) and `wake(Driver.method)` is a build-time
      static binding (03 §6); both legitimately need compiler
      knowledge. `InterruptCell(0)` is **investigate, don't presume**:
      check whether its publish/acquire ordering genuinely needs
      compiler knowledge or whether it could be an ordinary wrela
      struct. Record the answer either way; do not change it on a
      hunch.

      **The image-builder surface (05 §9) stays intrinsic and is not a
      hack**: it is comptime-only, emits no code, and its effects land
      on the compiler's own image graph. Making it ordinary wrela would
      mean exposing the image IR as a language type. Leave it.
- [ ] F. **Collections** (05 §7). `List[T, ..N]`, `SlotMap[T, ..N]` with
      the `(map_id, index, generation)` key discipline (foreign and stale
      keys *miss*, never alias; `GenerationExhausted` never wraps), array
      `map_take`/`try_map_take` with `try_map_take`'s unwind-and-reclaim
      on `Err`, and the two iteration forms (owned `for`, lent
      non-suspending closures).
      Goldens: `check-slotmap-key-discipline`, `err-slotmap-stale-key`,
      `check-try-map-take-unwind`, `err-each-suspends`.
- [ ] G. **Marked values** (05 §6) — `Untrusted[T]`, and the decision on
      `Validated[F, T]` / `Secret[T]` recorded either way. See the
      honest-scope note above: this item may split, and if it does, the
      split is committed to this file before the work continues.
- [x] H. **`ImageReport` reflection + `@layout_assert` runs**
      (decisions 220–224). Landed 2026-07-25. Flips
      `image.report.layout-asserts`. `stdlib/core/image_report.wr` is
      ordinary wrela; `eval::layout_assert` synthesizes a real value
      from `ImageLayout` and runs each registered fn after layout.
      Goldens: `check-layout-assert-passes` (report + `img.hex`),
      `err-layout-assert-fails` (diagnostic names the fn).
      `image-basic`'s report golden moves from the decision-10 refusal
      to a successful full report (vacuous `check_layout` removed).
- [ ] I. **Delete `sema/prelude.rs`** (shape decision 2), in one commit,
      once every name it carries has a real definition. The `IMAGE_BUILDER`
      array is the interesting half: those names must stay recognized as
      intrinsics (05 §9) while ceasing to be a hardcoded scope. Per
      decision 7 this item also owns moving `Option`/`Result` and
      unpicking `prelude::builtin_enum_variants`'s five call sites; that
      is the bulk of the item, and it is why A2 does not attempt it.
      Goldens: the whole existing suite is the oracle — nothing may move.
- [ ] J. **The corpus flip**, in the three commits shape decision 3
      requires: (J1) sema-check the doc corpus behind an off-by-default
      flag and report the disagreement count without failing; (J2) fix
      each disagreement in its own small cited commit, docs winning by
      default, REVIEW-QUEUE line for every normative edit; (J3) make
      corpus sema-checking mandatory in `cargo xtask check`.
      Depends on A–I.
- [ ] K. **Sweep + soak + finalization.** Adversarial sweep — and per
      M8's closing lesson, the sweep's targets are written as *properties*
      rather than cases, because a directed sweep's default failure mode
      is closing exactly the case it was shown. Named attack list to be
      appended as items land, seeded with: a `Format` bound that is
      provable but wrong; an f-string whose summed bound overflows; a
      `SlotMap` key forged across two maps; `now()` reached from a path
      no check covers; a `@layout_assert` that passes vacuously. Then:
      deep fuzz every live lane, 3+ fresh seeds, from a **release** build
      of the harness; write `plans/M10.md`; mark complete.

## Exit criteria (mechanically checkable)

- `cargo xtask check` green.
- `crates/wrela-compiler/src/sema/prelude.rs` **does not exist**.
- `stdlib/core/` contains compiled-and-tested wrela source; at least one
  golden dumps a stdlib-defined type through `wrela dump`.
- `comptime.fstring.bounds` flips `gap` → `test`.
- `image.report.layout-asserts` flips `gap` → `test`.
- `values.resource.protocol-consumption` re-verified `test` against the
  real stdlib types (no flip owed; see the starting-state note).
- `cargo xtask corpus` sema-checks the doc corpus and that check is
  **mandatory in `check`** — the `fragments skipped` count may be
  nonzero, but `parsed`-only must be gone.
- Deep fuzz all live lanes, 3+ fresh seeds, clean, release harness.
- `plans/M10.md` does not exist yet.

## Non-goals (M10+ territory)

SIMD and its NEON lowering contract; `race`; the `SlotMap.reserve/resolve`
service idiom that generates a turn; runtime-in-wrela (M10); the cost
contract (M11); display/input (off the ladder — ROADMAP's recorded
intentions, human-gated); a fourth vCPU; a second device family.

## Carry-forward from M8 (each named, each owned)

| Item | Origin | Owner |
| --- | --- | --- |
| `values.regions.two-binding-disciplines` stays `gap` | M8 item R, decision 16 | Its own flip condition names `with pool` landing; `with pool` is not M9 scope. Unowned here, deliberately. |
| `hardware.cancellation.recovery-turn` (gap) | M8 item F, decision 40 | M10 — it generates a turn, which is runtime territory. |
| `machine.cross-core.publish-acquire-barrier` (gap) | M8 item H attack 4 | Blocked on true concurrent vCPU execution; not a milestone item until the baton is retired. |
| `compiler.progress.wait-for-graph` (gap) | M8 item R found it had no clause | Unowned. 04 §1's Progress paragraph is unimplemented; opening it is a milestone decision, not a stdlib item. |
| `sema.bounds.loops` (gap) | pre-M8 | Unowned here. |

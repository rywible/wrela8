module image_report

pub struct ImageReport:
    pub machine_revision: u32
    pub entry: u64
    pub pages_base: u64
    pub pages_size: u64
    pub stacks_base: u64
    pub stacks_size: u64
    pub code_base: u64
    pub code_size: u64
